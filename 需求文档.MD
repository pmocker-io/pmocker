# PMocker 需求文档

> 版本：v1.0（MVP）— r3（追加团队管理模块）
> 日期：2026-08-02
> 状态：已通过设计评审（含 gva 插件化调整 + 团队管理模块追加），待用户最终审阅

---

## 1. 产品定位

### 1.1 愿景

PMocker 是**项目管理系统的"Docker"**——把不同方法论（PMBOK / PRINCE2 / ISO 21502 / 敏捷）的项目管理系统封装为可分享、可版本化、可差异升级的 `.pmi` 镜像，让个人/团队"一条命令启动一套完整的项目管理系统"。

### 1.2 目标用户

- **项目经理 / PMO 顾问**：需要快速在不同方法论模板间切换、对客户交付标准化 PM 系统
- **小型团队 / 初创**：希望以最低运维成本获得一套可定制的项目管理工具
- **企业 PMO**（后期）：通过私有 Hub 在组织内分发统一的 PM 治理模板

### 1.3 三层抽象（Docker 类比）

| 概念 | 说明 | Docker 类比 |
|------|------|-------------|
| **PMI 镜像** | 项目管理系统模板定义（模块清单、schema、菜单、权限方案、seed、主题、前端资源包），内容寻址 | Docker 镜像 |
| **PMSystem** | 运行中的 PM 系统实例，独立的 gin-vue-admin 进程 + 独立数据卷 | Docker 容器 |
| **项目** | PMSystem 内管理的具体项目数据（WBS、进度、风险…），存储于数据卷 | 容器内应用数据 |

### 1.4 MVP（v1.0）边界

**v1.0 交付：**

- 单机部署，SQLite 默认（保留 MySQL/PostgreSQL 适配层供后期切换）
- 默认提供 1 套 PMBOK 第六版混合型镜像
- 10 个插件模块：需求、范围、交付物、进度、成本、风险、问题、变更、组织级项目 EPS、团队管理
- 12 个 CLI 命令：`run` / `ps` / `start` / `stop` / `rm` / `commit` / `export` / `images` / `inspect` / `rmi` / `diff` / `upgrade`
- 独立数据卷持久化（`~/.pmocker/volumes/<id>`）
- EAV 元数据驱动统一数据结构，支持镜像 schema diff 升级
- **gva 插件化架构**：10 个 PM 模块以 gin-vue-admin 标准插件方式实现，兼容 gva 插件安装机制；插件内置 `pmocker/` 元数据子目录（schema/seed/menu/workflows）+ `InitPMocker()` 钩子灌入 EAV 元表
- 声明式工作流引擎
- 三维权限模型（gva RBAC + EPS 节点 + 实体状态）

**v1.0 不交付（后期）：**

- PMocker Hub 分享（`pull` / `push` / `login` / `search`）—— 后期 Git 仓库驱动
- PRINCE2 / ISO 21502 / 纯敏捷预置镜像（v1.3）
- 团队多用户协作（v1.2，保留 gva RBAC 框架，v1 默认单管理员 + 内置 4 角色）
- 其余 PMBOK 模块（质量、沟通、采购、干系人、整合）

---

## 2. 功能特性

### 2.1 核心特性

- **标准化 PM 模板** — 将项目管理系统封装为可复用的 `.pmi` 镜像
- **项目管理系统的多种方法论支持** — v1 默认 PMBOK 第六版混合型；PRINCE2、ISO 21502、敏捷框架作为后期独立镜像
- **gva 插件化架构** — 每个 PM 模块作为 gin-vue-admin 标准插件实现（server + web 完整目录），遵循 gva 插件规范（`plugin.go` 实现 Plugin 接口、`Register()` 注册路由）；插件目录内额外含 `pmocker/` 元数据子目录（schema.yaml/seed.yaml/menu.yaml/workflows/manifest.yaml），通过 `InitPMocker()` 钩子在 PMSystem 启动时灌入 EAV 元表。**完全兼容 gva 插件安装机制**（可走 gva 后台"插件安装"功能或 ZIP 手动部署）
- **定制算法归插件** — CPM 关键路径、EVM 挣值、变更影响分析等算法以 Go 代码形式内置于对应插件的 `service/` 包
- **基于 gin-vue-admin** — 复用 gin-vue-admin v3.0 成熟的 RBAC、菜单、审计和权限体系
- **多数据库支持** — SQLite（单机默认）/ MySQL / PostgreSQL（团队协作，后期）
- **OCI 镜像格式** — `.pmi` 文件采用简化的 OCI 镜像格式，支持内容寻址（SHA256）
- **Git 驱动的 Hub** — （后期）通过 Git 仓库分享和发现镜像
- **上游追踪** — 通过 Git Subtree 保持与 gin-vue-admin 上游的同步

### 2.2 行业最佳实践对标策略

采用 **分模块对标 + 来源标注** 策略：每个模块以一个或多个行业标杆工具为主参照，核心字段/工作流要点显式对标。

来源图例：
- **Z** = 禅道 ZenTao
- **T** = Teambition
- **MSP** = MS Project
- **HT** = 华天 PLM
- **QY** = 青翼 PLM
- **PMBOK** = PMBOK 第六版

### 2.3 v1.0 十模块对标矩阵

| 模块 | v1 主参照（★） | 核心字段 / 工作流要点 | 参照来源 |
|------|------|------|------|
| **需求管理** | ★ 禅道 需求模块 | 需求阶段（草稿/评审中/已发布/已关闭）、优先级（P0-P3）、需求分支+合并、需求基线、需求与用例/问题关联、版本变更链；需求变更走「变更管理」模块 | Z · T · PMBOK 5.2 |
| **范围管理 / WBS** | ★ MS Project WBS + 华天PLM 交付物 | WBS 分解（可到 6 层）、WBS 词典、交付物清单、范围基线（3 条基线 + 当前）；范围变更走「变更管理」模块；任务-交付物绑定见「交付物管理」 | MSP · HT · PMBOK 5.3/5.4 |
| **交付物管理** | ★ 青翼PLM + 华天PLM 交付物 | 阶段交付物清单（图纸/BOM/样品/文档）、文档生命周期（草稿→评审→发布→归档→废弃）、版本控制（大版本 V1.0 + 小版本 V0.x）、检入/检出 Check-in/Out 排他编辑、多格式附件（PDF/CAD/Office/3D 模型）、审批工作流（提交→评审→批注→签发/退回）、电子签名 + 审签记录、交付物-WBS 任务绑定、交付物完整性校验、BOM 多视图（EBOM/MBOM/PBOM/CBOM）；交付物版本变更走「变更管理」模块 | QY · HT · MSP · PMBOK 5.4 |
| **进度管理** | ★ MS Project 进度引擎 | 任务/里程碑、依赖关系（FS/SS/FF/SF）、工期（工时/人日/固定工期）、资源分配、关键路径 CPM、甘特图视图、进度基线 ×3、进度预警（超期/偏差 ≥ threshold）、看板泳道；任务完成可触发交付物签入（联动「交付物管理」）；进度基线变更走「变更管理」模块 | MSP · T · PMBOK 6.5/6.6 |
| **成本管理** | ★ MS Project 成本表 | 预算分解（按 WBS 分摊）、实际成本登记（工时 × 资源费率 + 一次性费用）、挣值（PV/EV/AC/SV/CV/SPI/CPI/EAC/ETC）、成本基线 ×3、成本超支预警、成本-采购联动；成本基线变更走「变更管理」模块 | MSP · HT · PMBOK 7.3/7.4 |
| **风险管理** | ★ PMBOK + 华天PLM 风险矩阵 | 风险 5×5 概率×影响矩阵、T/I/O 类别（威胁/机会/中性）、应对策略（回避/转移/减轻/接受 · 开拓/分享/提高/接受）、风险所有者、响应计划、触发条件、残留/次生风险、风险再评估记录、风险跟踪闭环 | HT · Z · PMBOK 11 |
| **问题管理** | ★ 禅道 Bug + Teambition 问题单 | 问题类型（技术/管理/外部）、严重度/优先级、状态流（新建→指派→处理中→待验证→已解决/已驳回）、关联需求/任务/风险、评论与附件、SLA 响应/解决时限；问题触发变更走「变更管理」模块 | Z · T · PMBOK 4.3 |
| **变更管理** | ★ 青翼PLM ECN/ECR + 华天PLM 变更 + PMBOK 整合变更控制 | **变更请求类型**：需求变更 / 设计变更 / 范围变更 / 进度变更 / 成本变更 / 文档变更（ECN）/ 工程变更请求（ECR）；**变更申请单（CR）**：申请号、申请人、变更对象、变更原因、变更前后内容；**变更影响分析**：影响范围识别（关联交付物/BOM/采购/库存/在制/已交付/进度/成本）、成本影响、交期影响评估；**变更分级**：重大变更 / 一般变更 / 紧急变更（对应不同审批流）；**变更评审流程**：提交 → 技术评审 → 影响分析 → 审批路由 → 执行跟踪 → 闭环验证；**变更前后对比**：基线对比、版本差异 diff（与「交付物管理」联动）；**变更关联**：关联需求 / 问题 / 风险 / 交付物 / 任务 / ECN/ECR；**变更执行跟踪**：执行状态、责任人、完成时间、闭环验证；**变更历史审计**：电子签名 + 审签记录 + 不可篡改操作日志 | QY · HT · Z · MSP · PMBOK 4.5/4.6 |
| **组织级项目 EPS** | ★ 华天PLM 组织级 PMO | EPS 树（集团 → 事业部 → 项目集 → 项目 → 子项目）、权限继承（下节点默认继承上级 RBAC）、跨项目看板（组合视图：进度/成本/风险汇总）、项目健康度红黄绿、PMO 标准模板下发（.pmi 镜像推送到下级实例）、项目组合管理（多项目优先级排序、资源协调、整体风险评估） | HT · MSP Server · PMBOK OPM |
| **团队管理** | ★ MS Project 资源表 + Teambition 团队成员 | **团队成员（team_member）**：成员编号、关联系统用户、项目角色（PM/BA/Dev/QA 等）、投入度%（支持多项目分摊）、技能矩阵（技能名+等级）、综合技能等级、时薪（成本核算联动「成本管理」）、是否可计费、工作模式（现场/混合/远程）、汇报对象、利用率%（auto 节点自动算：实际工时/可用工时）、状态流（候选人→已入职→在职→离职中→已离职）；**角色定义（team_role）**：角色编号、职责范围、必备技能、权限级别（高管/经理/专家/贡献者）、RACI 默认角色（R/A/C/I）、最低经验年限、最大编制人数；**培训记录（training_record）**：对标 PMBOK Develop Team 过程——课程名称、培训类型（技术/软技能/安全/认证/入职）、培训时长、费用、机构、证书编号、柯氏评估四级效果评分；**绩效评估（performance_review）**：对标 PMBOK Manage Team 过程——评估周期（季度/年度/项目结项/试用）、评估人、评级（优秀/良好/合格/待改进/不合格）、量化评分（0-100）、优势亮点、待改进项、目标达成情况、360°反馈汇总、个人发展计划（IDP）；**工作流**：入职审批流（候选人→HR 审批→已入职→在职→离职中→已离职）、绩效评估流（草稿→评估中→已完成/申诉，含 auto 节点自动汇总评分）；**跨模块联动**：团队投入度→成本核算（时薪×工时）、技能矩阵→资源分配（「进度管理」任务资源加载）、利用率→EPS 项目健康度 | MSP · T · Z · PMBOK 9 |

### 2.4 跨模块横向能力（行业最佳实践汇总）

| # | 能力 | 说明 | 涉及模块 |
|---|------|------|---------|
| ① | 任务-交付物绑定 | WBS 任务直接关联设计文件/图纸/零部件，任务完成自动触发交付物签入 | 范围 / 交付物 / 进度 |
| ② | 文档生命周期 + 检入检出 | 草稿→评审→发布→归档→废弃，Check-in/Out 排他编辑，大版本+小版本 | 交付物 |
| ③ | ECN/ECR 变更闭环 | 需求/问题/风险/交付物/基线变更通过 ECN/ECR 工作流关联，影响分析 + 审批 + 执行跟踪 + 闭环 | 变更 |
| ④ | 电子签名 + 审签记录 | 关键操作（数据修改/流程审批/变更签发）记录操作者+时间戳+操作内容，满足审计 | 交付物 / 变更 |
| ⑤ | BOM 多视图 | EBOM/MBOM/PBOM/CBOM 同根多视图，跨部门一致性（注：v1 不为 BOM 视图做特殊数据建模，作为交付物的一种属性集合） | 交付物 |
| ⑥ | 基于文档状态的权限 | 草稿态仅创建者可改、评审态仅审核人可批注、发布态只读不可改 | 权限模型横切 |
| ⑦ | 基线管理 | 范围/进度/成本各 3 条基线 + 当前，基线变更必须走变更控制 | 范围 / 进度 / 成本 / 变更 |
| ⑧ | 工作流引擎 | 图形化定义审批流（设计评审/图纸发放/样机测试/变更 ECN/ECR），节点指定负责人、时限、输入输出文件 | 横切 |
| ⑨ | RACI 矩阵 | 范围项/任务显式标注 R/A/C/I 四角色，与团队管理角色定义联动 | 范围 / 团队 |
| ⑩ | 技能矩阵 + 投入度 | 团队成员技能等级 + 多项目投入度分摊，支撑资源分配决策 | 团队 / 进度 / 成本 |
| ⑪ | 绩效闭环 | 360°反馈 + IDP 发展计划，培训记录驱动团队建设 | 团队 |

---

## 3. 技术栈

| 组件 | 技术 |
|------|------|
| CLI | Go 1.24 + Cobra |
| 后端 | Go + Gin（gin-vue-admin v3.0.0） |
| 前端 | Vue 3 + Element Plus + Pinia |
| 数据库 | SQLite（默认）/ MySQL / PostgreSQL |
| 镜像格式 | 简化 OCI 镜像格式（`.pmi`） |
| 模块管理 | Go Workspace（`go.work`） |
| 上游追踪 | Git Subtree（`--prefix=gva --squash`） |

---

## 4. `.pmi` 镜像格式（简化 OCI）

### 4.1 文件结构

`.pmi` 文件本质是一个 tar 归档，扩展名 `.pmi`，内部结构：

```
example.pmi
├── manifest.json          // 镜像清单（入口）
├── config.json            // 镜像配置：方法论、版本、入口、暴露端口
├── sha256:<layer1>.tar    // plugins 层：9 个 gva 插件完整源码（server + web + pmocker/ 元数据）
├── sha256:<layer2>.tar    // schema 聚合层：聚合 9 插件的 pmocker/schema.yaml（加速 diff，避免解 plugins 层）
├── sha256:<layer3>.tar    // theme 层：CSS/Logo/配色
├── sha256:<layer4>.tar    // assets 层：9 插件预编译前端 dist/
└── index.json             // 可选：多架构/多版本索引
```

> **注**：seed/menu/workflows 不再独立成层，物理归入 plugins 层内（保持 gva 插件 ZIP 结构完整，便于直接复制到 gva 标准目录后用 gva 后台"插件安装"功能安装）。逻辑上仍由插件 `InitPMocker()` 钩子在初始化时灌入。

### 4.2 manifest.json 示例

```json
{
  "schemaVersion": 2,
  "mediaType": "application/vnd.pmocker.manifest.v1+json",
  "config": {
    "mediaType": "application/vnd.pmocker.config.v1+json",
    "digest": "sha256:abc123...",
    "size": 1024
  },
  "layers": [
    { "mediaType": "application/vnd.pmocker.layer.plugins.v1.tar",
      "digest": "sha256:111...", "size": 40960,
      "annotations": {"pmocker.layer.type": "plugins"} },
    { "mediaType": "application/vnd.pmocker.layer.schema.v1.tar",
      "digest": "sha256:222...", "size": 8192,
      "annotations": {"pmocker.layer.type": "schema"} },
    { "digest": "sha256:333...", "annotations": {"pmocker.layer.type": "theme"} },
    { "digest": "sha256:444...", "annotations": {"pmocker.layer.type": "assets"} }
  ],
  "annotations": {
    "pmocker.image.title": "PMBOK v6 Hybrid",
    "pmocker.image.version": "1.0.0",
    "pmocker.image.methodology": "PMBOK6-Hybrid",
    "pmocker.image.created": "2026-07-31T10:00:00Z"
  }
}
```

### 4.3 config.json 示例

```json
{
  "methodology": "PMBOK6-Hybrid",
  "version": "1.0.0",
  "gvaVersion": "v3.0.0",
  "entrypoint": ["./gva-server", "--config=config.yaml"],
  "exposedPorts": { "8080/tcp": {} },
  "dbDriver": "sqlite",
  "modules": ["requirement","scope","deliverable","schedule","cost","risk","issue","change","eps"],
  "defaultAdmin": { "username": "admin", "passwordEnv": "PMOCKER_ADMIN_PASSWORD" }
}
```

### 4.4 四类层的内容定义

| 层类型 | 内容 | 用途 | 升级时的 diff 行为 |
|------|------|------|------|
| **plugins 层** | 9 个 gva 插件完整源码：`server/plugin/pmocker/<mod>/`（含 plugin.go、api/router/service/model/initialize/config/global）+ `web/src/plugin/pmocker/<mod>/`（Vue 源码）+ 插件内 `pmocker/` 子目录（schema.yaml/seed.yaml/menu.yaml/workflows/manifest.yaml） | gva 标准插件目录结构，可直接复制到 gva 项目的 `server/plugin/` 和 `web/src/plugin/` 下，用 gva 后台"插件安装"功能装载 | 模块清单 diff：新增模块 / 移除模块 / 插件源码变更（按插件 digest 比对） |
| **schema 聚合层** | 9 插件 `pmocker/schema.yaml` 的聚合快照：`pm_field_defs`（字段 key/类型/默认值/校验规则）、`pm_entity_types`（实体类型）、`pm_relation_types`（关系类型） | 加速 `pmocker diff` 命令——无需解压 plugins 层即可对比 schema 差异 | 逐字段 diff：新增字段 / 删除字段 / 类型变更 / 默认值变更，输出 migration 脚本 |
| **theme 层** | CSS 变量、Logo、登录页背景、配色方案、字体 | 主题定制，让不同镜像有视觉区分 | 完整替换（用户自定义主题独立存储，不被覆盖） |
| **assets 层** | 9 插件预编译前端 `dist/`（`assets/*.js` + 路由清单），按模块分包 | PMocker 运行时前端资源（也可直接用 gva 前端 dev 模式跑源码） | 完整替换（按模块 digest 比对，未变更模块跳过） |

> **说明**：seed/menu/workflows 逻辑上仍是独立单元（由 `InitPMocker()` 钩子分别灌入 `pm_seed_data` / `sys_base_menus` / `pm_workflow_defs` 表），但物理上打包在 plugins 层内以保持 gva 插件 ZIP 结构完整。

---

## 5. CLI 命令规范（v1 十二个命令）

| 命令 | 说明 | Docker 类比 |
|------|------|------|
| `pmocker run --image <.pmi或镜像名> [flags]` | 启动一个 PMSystem。**flags**：`-p <端口>` · `--name <实例名>` · `--db <sqlite|mysql|postgres>` · `--db-dsn <DSN>` · `-v <数据卷路径>` · `--admin-password <密码>` · `--env KEY=VAL`（可多次） | docker run |
| `pmocker ps [flags]` | 列出运行中的 PMSystem。**flags**：`-a` 显示已停止 | docker ps |
| `pmocker stop <name|id>` | 停止 PMSystem 进程，数据卷保留 | docker stop |
| `pmocker start <name|id>` | 重新启动已停止的 PMSystem | docker start |
| `pmocker rm <name|id> [-v]` | 删除 PMSystem 实例记录。**-v**：同时删除数据卷 | docker rm / docker rm -v |
| `pmocker commit <name|id> -t <新镜像名:tag>` | 从运行中或已停止的 PMSystem 导出为新的 .pmi 镜像文件。可选 `-m` 提交说明 | docker commit |
| `pmocker export <name|id> -o <file.pmi>` | 把 PMSystem 当前状态导出为 .pmi 文件（直接落盘到指定路径） | docker export |
| `pmocker images` | 列出本地镜像缓存（`~/.pmocker/images/`），显示名称、tag、digest、大小、创建时间 | docker images |
| `pmocker inspect <image|name|id>` | 查看镜像 manifest/config 或运行实例的元信息（端口、数据卷、镜像来源、启动时间） | docker inspect |
| `pmocker rmi <image:tag|digest>` | 删除本地镜像缓存（不影响已运行的 PMSystem） | docker rmi |
| `pmocker diff <旧镜像> <新镜像>` | 对比两个 .pmi 镜像的 schema/plugins/seed 差异，输出升级影响清单 | 无（PMocker 独有） |
| `pmocker upgrade <name|id> --to <新镜像>` | 将运行中 PMSystem 升级到新镜像版本：先 diff 生成 migration，再执行迁移（schema + seed upsert），保留用户数据 | 无（PMocker 独有） |

> 注：v1 不含 `pull` / `push` / `login` / `search`（PMocker Hub 后期）；v1 不含 `build`（用 `commit` / `export` 替代）。

---

## 6. 本地目录布局（`~/.pmocker/`）

```
~/.pmocker/
├── images/                      // 镜像缓存（按 digest 寻址）
│   └── sha256:abc.../
│       ├── manifest.json
│       ├── config.json
│       └── blobs/
│           └── sha256:<layer>.tar
├── instances/                   // 实例注册表（SQLite 元数据）
│   └── instances.db             // 表：id, name, image_digest, port, volume_id, status, created_at
├── volumes/                     // 数据卷（PMSystem 的应用数据）
│   └── <volume_id>/
│       ├── system.db            // gva 系统库（用户/角色/菜单/权限）
│       ├── project.db           // 项目库（pm_entities + pm_attrs EAV + 业务索引）
│       └── uploads/             // 交付物文件存储（图纸/BOM/样品附件）
├── logs/
│   └── <instance_id>.log
└── config.yaml                  // pmocker 全局配置
```

---

## 7. EAV 数据模型（统一数据结构）

9 模块的所有业务实体（需求/任务/风险/问题/交付物/变更…）共用一套 EAV 元表，字段由镜像 schema 层驱动，支持动态扩展与版本 diff。

### 7.1 元表（schema 定义表，由镜像 schema 层灌入）

```sql
-- 实体类型定义：10 个模块 → 10+ 种实体类型
CREATE TABLE pm_entity_types (
  id            BIGINT PRIMARY KEY,
  type_code     VARCHAR(64) UNIQUE,   -- 'requirement','task','risk','issue','deliverable','change_request','team_member','team_role','training_record','performance_review',...
  module_code   VARCHAR(32),          -- 'requirement','scope','deliverable','schedule','cost','risk','issue','change','eps','team'
  name          VARCHAR(128),
  icon          VARCHAR(64),
  icon_color    VARCHAR(16)
);

-- 字段定义：每个实体类型有哪些字段、字段类型、校验规则
CREATE TABLE pm_field_defs (
  id            BIGINT PRIMARY KEY,
  entity_type   VARCHAR(64),          -- → pm_entity_types.type_code
  field_key     VARCHAR(64),          -- 'priority','severity','due_date','cost_actual',...
  field_label   VARCHAR(128),
  data_type     VARCHAR(32),          -- 'string','text','int','decimal','date','datetime','bool','enum','ref','json'
  options_json  TEXT,                 -- enum 选项 / ref 指向的实体类型 / json schema
  default_value TEXT,
  validators    TEXT,                 -- JSON: {required:true, min:0, max:100, regex:'...'}
  UNIQUE(entity_type, field_key)
);

-- 关系类型定义：实体之间的关联（需求↔任务、问题↔交付物、变更↔基线…）
CREATE TABLE pm_relation_types (
  id           BIGINT PRIMARY KEY,
  type_code    VARCHAR(64) UNIQUE,    -- 'delivers','implements','blocks','changes','relates_to'
  name         VARCHAR(128),
  src_type     VARCHAR(64),
  dst_type     VARCHAR(64),
  directional  BOOLEAN DEFAULT 1
);

-- 字段版本追踪：支撑镜像 diff 与升级 migration
CREATE TABLE pm_field_versions (
  id            BIGINT PRIMARY KEY,
  field_def_id  BIGINT,
  image_digest  VARCHAR(80),           -- 来自 .pmi 镜像的 SHA256
  snapshot_json TEXT,                  -- 该版本字段定义快照
  created_at    TIMESTAMP
);
```

### 7.2 业务数据表（所有实体共用）

```sql
-- 实体主表：9 模块共用一张表，通过 entity_type 区分
CREATE TABLE pm_entities (
  id           BIGINT PRIMARY KEY,
  project_id   BIGINT NOT NULL,          -- EPS 项目节点
  entity_type  VARCHAR(64) NOT NULL,     -- 'requirement','task','risk',...
  parent_id    BIGINT,                   -- WBS / 需求分支 / 风险分解的父节点
  title        VARCHAR(255) NOT NULL,
  status       VARCHAR(32),              -- 状态机当前态（如需求：草稿/评审中/已发布/已关闭）
  owner_id     BIGINT,                   -- 责任人 → gva sys_users
  baseline_id  BIGINT,                   -- 当前关联的基线（范围/进度/成本各 3 条基线）
  seq          INT,                      -- 排序（同层）
  created_by   BIGINT,
  created_at   TIMESTAMP,
  updated_by   BIGINT,
  updated_at   TIMESTAMP,
  deleted_at   TIMESTAMP                 -- 软删除
);

-- 属性表：动态字段值（EAV 核心）
CREATE TABLE pm_attrs (
  id           BIGINT PRIMARY KEY,
  entity_id    BIGINT NOT NULL,          -- → pm_entities.id
  field_key    VARCHAR(64) NOT NULL,     -- → pm_field_defs.field_key
  val_string   TEXT,
  val_int      BIGINT,
  val_decimal  DECIMAL(18,4),
  val_date     DATE,
  val_datetime TIMESTAMP,
  val_bool     BOOLEAN,
  val_json     TEXT,                     -- 复杂值（如多选 enum、对象数组）
  val_ref      BIGINT,                   -- 引用另一个 pm_entities.id
  UNIQUE(entity_id, field_key)
);

-- 关系表：实体间关联（无向/有向）
CREATE TABLE pm_relations (
  id            BIGINT PRIMARY KEY,
  src_id        BIGINT NOT NULL,         -- → pm_entities.id
  dst_id        BIGINT NOT NULL,
  relation_type VARCHAR(64) NOT NULL,    -- → pm_relation_types.type_code
  UNIQUE(src_id, dst_id, relation_type)
);
```

### 7.3 专用表（仅放 EAV 不擅长的强结构数据）

| 表 | 用途 | 理由 |
|------|------|------|
| `pm_wbs_nodes` | WBS 树结构：node_id, parent_id, path（物化路径字符串，如 `1.3.2`）, level, deliverable_id | 闭包树/路径查询性能远胜 EAV；v1 SQLite 用字符串存储路径，后期 PG 可切换 LTREE 类型 |
| `pm_eps_tree` | EPS 树：node_id, parent_id, path（物化路径字符串）, project_type（集团/事业部/项目集/项目/子项目） | 同上，权限继承需路径查询 |
| `pm_task_links` | 任务依赖关系：src_task_id, dst_task_id, link_type (FS/SS/FF/SF), lag | 关键路径 CPM 计算专用结构 |
| `pm_baselines` | 基线快照：baseline_id, type (scope/schedule/cost), snapshot_json, created_at | 需要完整历史快照而非当前态 |
| `pm_change_logs` | 变更日志：entity_id, field_key, old_value, new_value, changed_by, changed_at, change_request_id | 变更管理与审计追溯 |
| `pm_deliverable_files` | 交付物文件：deliverable_id, file_path, checksum, size, mime, checked_out_by, version_major, version_minor | 文件元数据需要 Check-in/Out 排他锁 |
| `pm_workflow_instances` | 工作流实例：entity_id, workflow_def_id, current_node, status, started_at, completed_at | 工作流状态机 |

---

## 8. 权限模型（三维叠加）

基于 gva RBAC 扩展，叠加 EPS 节点权限 + 实体状态权限，形成三维权限矩阵。

### 8.1 三维权限

| 维度 | 名称 | 说明 |
|------|------|------|
| ① | **gva RBAC** | 用户 → 角色 → 权限标识（API/菜单/按钮级）。v1 内置 4 角色：**PMO 管理员 / 项目经理 / 团队成员 / 干系人（只读）** |
| ② | **EPS 节点权限** | 权限按 EPS 树继承：子节点默认继承父节点的角色分配，可在任意节点覆盖（华天PLM 模式）。支持"参与 A 项目但看不到 B 项目" |
| ③ | **实体状态权限** | 基于文档/实体生命周期状态：草稿态仅创建者可改、评审态仅审核人可批注、发布态只读不可改（除非走变更）、归档态仅 PMO 可恢复 |

### 8.2 权限判定

```
has_perm(user, action, entity) =
    ① gva 角色含 action 权限标识
  且 ② EPS 节点上 user 有对应角色
  且 ③ 实体当前状态允许该 action
```

---

## 9. 工作流引擎（声明式）

工作流定义存于各 gva 插件内 `pmocker/workflows/` 子目录（YAML），由插件 `InitPMocker()` 钩子在 PMSystem 启动时灌入 `pm_workflow_defs` 表，运行时由 PMocker 内核解释执行。

### 9.1 工作流定义示例（重大变更审批流）

```yaml
workflow_code: "change_request_major"
name: "重大变更审批流"
entity_type: "change_request"
trigger: "on_create"
nodes:
  - code: "submit"
    type: "start"
    assignee: "${entity.created_by}"
    actions: ["submit"]
  - code: "tech_review"
    type: "approval"
    approvers: "role:tech_lead"
    sla_hours: 48
    actions: ["approve", "reject"]
  - code: "impact_analysis"
    type: "auto"
    handler: "pmocker.change.impact_analyzer"
  - code: "ccb_approval"
    type: "approval"
    approvers: "role:ccb_member"
    quorum: 3
  - code: "execute"
    type: "task"
    assignee: "${entity.assignee}"
  - code: "verify"
    type: "approval"
    approvers: "role:pm"
  - code: "closed"
    type: "end"
transitions:
  - from: "submit", to: "tech_review", on: "submit"
  - from: "tech_review", to: "impact_analysis", on: "approve"
  - from: "tech_review", to: "closed", on: "reject", status: "rejected"
  - from: "impact_analysis", to: "ccb_approval"
  - from: "ccb_approval", to: "execute", on: "approve"
  - from: "execute", to: "verify", on: "complete"
  - from: "verify", to: "closed", on: "approve", status: "closed"
on_complete:
  action: "pmocker.change.apply"
  sign: true
```

### 9.2 v1 内置 6 类工作流模板

| 工作流 | 适用场景 | 节点链 |
|------|------|------|
| **需求评审流** | 需求从草稿到发布 | 提交 → 评审 → 批准 → 发布 |
| **交付物审批流** | 图纸/BOM/文档发布 | 提交 → 设计评审 → 工艺评审 → 签发 → 发布（含 Check-in/Out） |
| **变更控制流** | ECR/ECN 变更 | 提交 → 技术评审 → 影响分析 → CCB 审批 → 执行 → 验证 → 闭环 |
| **问题处理流** | 问题/Bug 解决 | 新建 → 指派 → 处理 → 待验证 → 已解决/已驳回 |
| **团队入职流** | 团队成员加入与退出 | 候选人 → HR 审批 → 已入职 → 在职 → 离职中 → 已离职 |
| **绩效评估流** | 团队绩效评审 | 草稿 → 评估中（评估人审批）→ 已完成/申诉（含 auto 节点汇总评分） |

### 9.3 节点类型

- `start` — 起点节点
- `approval` — 审批节点（支持 `approvers` / `sla_hours` / `quorum` 多人表决）
- `auto` — 自动节点（调用 `handler` 指定的内核函数，如影响分析）
- `task` — 任务节点（指定 `assignee` 执行具体工作）
- `end` — 终点节点

---

## 10. 10 模块落到 EAV 的实例

### 10.1 需求管理 schema（镜像 schema 层）

```yaml
entity_type: "requirement"
module: "requirement"
fields:
  - key: "priority", type: "enum"
    options: ["P0","P1","P2","P3"]
    required: true
  - key: "source", type: "string"
  - key: "accepted_date", type: "date"
  - key: "parent_req", type: "ref"
    ref_to: "requirement"
states: ["draft","reviewing","published","closed"]
workflow: "requirement_review"
```

### 10.2 变更管理 schema（镜像 schema 层）

```yaml
entity_type: "change_request"
module: "change"
fields:
  - key: "change_type", type: "enum"
    options: ["requirement","design","scope","schedule","cost","document","ecn","ecr"]
  - key: "severity", type: "enum"
    options: ["major","normal","urgent"]
  - key: "target_entity", type: "ref"
  - key: "before_snapshot", type: "json"
  - key: "after_snapshot", type: "json"
  - key: "impact_scope", type: "json"
states: ["submitted","reviewing","approved","executing","verified","closed","rejected"]
workflow: "change_request_${severity}"   # 按 severity 选不同流程
```

### 10.3 团队管理 schema（镜像 schema 层）

> 对标 PMBOK 第六版 Project Resource Management（9.0-9.6 过程）。4 个实体：团队成员 / 角色定义 / 培训记录 / 绩效评估，覆盖 Plan Resource Management / Acquire Resources / Develop Team / Manage Team / Control Resources 全闭环。

```yaml
# entity_type: team_member（团队成员）
entity_type: "team_member"
module: "team"
fields:
  - key: "code", type: "string"                  # 成员编号（如 TM-001）
  - key: "user_id", type: "ref"                  # 关联系统用户 → sys_users
  - key: "full_name", type: "string"             # 姓名（冗余便于查询）
  - key: "role", type: "enum"
    options: ["pm","sponsor","ba","dev","qa","designer","architect","sme"]
  - key: "allocation_percent", type: "decimal"   # 投入度%（0-100，支持多项目分摊）
  - key: "skill_level", type: "enum"             # 综合技能等级
    options: ["junior","mid","senior","expert"]
  - key: "skills", type: "json"                  # 技能列表（技能名+等级）
  - key: "hourly_rate", type: "decimal"          # 时薪（成本核算联动「成本管理」）
  - key: "billable", type: "bool"                # 是否可计费
  - key: "communication_mode", type: "enum"      # 工作模式
    options: ["onsite","hybrid","remote"]
  - key: "reporting_to", type: "ref"             # 汇报对象 → sys_users
  - key: "utilization_rate", type: "decimal"     # 利用率%（auto 节点自动算：实际工时/可用工时）
states: ["candidate","onboarded","active","leaving","offboarded"]
workflow: "team_member_onboard"                  # 入职审批流

# entity_type: team_role（角色定义）
entity_type: "team_role"
module: "team"
fields:
  - key: "code", type: "string"
  - key: "name", type: "string"
  - key: "responsibilities", type: "text"        # 职责范围
  - key: "required_skills", type: "json"         # 必备技能列表
  - key: "authority_level", type: "enum"         # 权限级别
    options: ["executive","manager","specialist","contributor"]
  - key: "raci_default", type: "enum"            # RACI 默认角色
    options: ["R","A","C","I"]
  - key: "min_experience_years", type: "int"     # 最低经验年限
  - key: "max_headcount", type: "int"            # 最大编制人数
states: ["active","inactive"]

# entity_type: training_record（培训记录）——对标 PMBOK Develop Team
entity_type: "training_record"
module: "team"
fields:
  - key: "member_id", type: "ref"                # 培训对象 → team_member
  - key: "course_name", type: "string"
  - key: "training_type", type: "enum"           # 培训类型
    options: ["technical","soft_skill","safety","certification","onboarding"]
  - key: "duration_hours", type: "decimal"       # 培训时长（小时）
  - key: "cost", type: "decimal"                 # 培训费用
  - key: "provider", type: "string"              # 培训机构
  - key: "certification_obtained", type: "bool"  # 是否获证
  - key: "certification_id", type: "string"      # 证书编号
  - key: "effectiveness_score", type: "decimal"  # 柯氏评估四级效果评分（1-5）
states: ["planned","in_progress","completed","cancelled"]

# entity_type: performance_review（绩效评估）——对标 PMBOK Manage Team
entity_type: "performance_review"
module: "team"
fields:
  - key: "member_id", type: "ref"                # 被评估人 → team_member
  - key: "review_period", type: "string"         # 评估周期（如 2026-Q3）
  - key: "reviewer_id", type: "ref"              # 评估人 → sys_users
  - key: "review_type", type: "enum"             # 评估类型
    options: ["quarterly","annual","project_end","probation"]
  - key: "rating", type: "enum"                  # 评级
    options: ["excellent","good","satisfactory","needs_improvement","unsatisfactory"]
  - key: "score", type: "decimal"                # 量化评分（0-100）
  - key: "strengths", type: "text"               # 优势/亮点
  - key: "improvement_areas", type: "text"       # 待改进项
  - key: "feedback", type: "text"                # 360°反馈汇总
  - key: "development_plan", type: "text"        # 个人发展计划（IDP）
states: ["draft","in_review","completed","appealed"]
workflow: "performance_review_flow"              # 绩效评估流
```

**跨模块联动：**
- 团队成员投入度 + 时薪 → 「成本管理」工时成本核算
- 技能矩阵 + 角色定义 → 「进度管理」任务资源分配
- 利用率 + 绩效评分 → 「EPS」项目健康度 RAG 指标

---

## 11. 仓库结构（单仓 + Go Workspace）

```
pmocker/
├── go.work                      // Go Workspace：声明 cli/gva/pkg 三个模块（10 个 PM 模块在 gva/server/plugin/pmocker/ 内，属 gva 模块）
├── go.work.sum
├── README.md
├── 需求文档.MD
├── Makefile                     // build/test/install/run 等快捷命令
│
├── cli/                         // pmocker CLI（Go + Cobra）
│   ├── go.mod
│   ├── cmd/
│   │   ├── root.go
│   │   ├── run.go
│   │   ├── ps.go                // ps / start / stop / rm
│   │   ├── commit.go            // commit / export
│   │   ├── images.go            // images / inspect / rmi
│   │   └── diff.go              // diff / upgrade
│   ├── internal/
│   │   ├── image/               // .pmi 镜像读写、OCI tar 解析、SHA256 digest
│   │   ├── instance/            // PMSystem 实例生命周期
│   │   ├── volume/              // 数据卷管理
│   │   └── registry/            // 本地镜像缓存
│   └── main.go
│
├── gva/                         // gin-vue-admin v3.0.0（Git Subtree）
│   ├── go.mod
│   ├── server/
│   │   ├── api/v1/              // gva 原 API + PMocker 扩展 API
│   │   ├── model/
│   │   │   ├── pmocker/         // EAV 元表/业务表/专用表 model
│   │   │   └── system/          // gva 原生 RBAC/菜单 model
│   │   ├── service/
│   │   │   ├── pmocker/         // EAV 引擎、schema 加载、工作流引擎、变更引擎
│   │   │   └── system/
│   │   ├── router/
│   │   ├── plugin/pmocker/      // ★ 10 个 PM 模块作为 gva 插件（后端）
│   │   │   ├── requirement/     // 需求管理插件
│   │   │   │   ├── plugin.go    // gva Plugin 接口 + InitPMocker() 钩子
│   │   │   │   ├── api/ router/ service/ model/ initialize/ config/ global/
│   │   │   │   └── pmocker/     // ★ PMocker 元数据扩展子目录
│   │   │   │       ├── schema.yaml      // EAV 字段定义
│   │   │   │       ├── seed.yaml        // 种子数据
│   │   │   │       ├── menu.yaml        // 菜单树
│   │   │   │       ├── workflows/       // 工作流模板
│   │   │   │       └── manifest.yaml     // 插件清单
│   │   │   ├── scope/           // （同上结构）
│   │   │   ├── deliverable/     // 含 Check-in/Out 算法 Go 代码
│   │   │   ├── schedule/        // 含 CPM 关键路径算法 Go 代码
│   │   │   ├── cost/            // 含 EVM 挣值计算 Go 代码
│   │   │   ├── risk/
│   │   │   ├── issue/
│   │   │   ├── change/          // 含变更影响分析 Go 代码
│   │   │   ├── eps/
│   │   │   └── team/            // 团队管理插件（含利用率 auto 计算 Go 代码）
│   │   ├── config.yaml
│   │   └── main.go
│   └── web/                     // gva 前端（Vue 3 + Element Plus + Pinia）
│       ├── src/
│       │   ├── api/
│       │   ├── view/
│       │   │   ├── pmocker/     // PMocker host shell + 动态模块路由
│       │   │   └── system/
│       │   ├── plugin/pmocker/  // ★ 10 个 PM 模块前端（gva 插件规范）
│       │   │   ├── requirement/  // api/ view/ components/ utils/
│       │   │   └── ... (同后端 10 模块)
│       │   └── pinia/
│       └── package.json
│
├── pkg/                         // 共享库（被 cli 和 gva 引用）
│   ├── go.mod
│   ├── pmocker/
│   │   ├── eav/                 // EAV 元表/业务表/专用表的 Go 类型 + LoadSchemaFromPluginYaml 等工具
│   │   ├── oci/                 // OCI tar 解析、manifest/config 类型
│   │   ├── workflow/            // 声明式工作流引擎解释器
│   │   ├── rbac/                // 三维权限判定器
│   │   └── plugin/             // ★ PMockerPlugin 接口 + InitAllPMockerPlugins 注册器
│   └── migrations/              // 镜像 diff → migration 脚本生成器
│
├── images/                      // 预置 .pmi 镜像源（构建产物）
│   └── pmbok6-hybrid/
│       ├── manifest.yaml
│       ├── config.yaml
│       └── build.sh             // 打包 gva/server/plugin/pmocker/* + web/src/plugin/pmocker/* + schema 聚合 + theme + assets
│
└── docs/
    ├── 需求文档.MD
    └── design/
```

### 11.1 gva 插件目录结构示例（以 requirement 为例）

后端插件遵循 gva 标准插件目录结构：

```
gva/server/plugin/pmocker/requirement/
├── plugin.go                // gva Plugin 接口实现 + PMocker InitPMocker() 钩子
├── api/
│   ├── enter.go
│   └── requirement.go       // 需求 CRUD + 状态流转 API
├── router/
│   ├── enter.go
│   └── router.go            // 注册到 /api/plugin/pmocker/requirement/*
├── service/
│   ├── enter.go
│   └── requirement.go       // 业务逻辑（调用 EAV 引擎 + 状态机）
├── model/
│   ├── model.go             // 请求/响应 struct
│   ├── request/
│   └── response/
├── initialize/
│   ├── router.go
│   ├── gorm.go
│   └── pmocker.go           // ★ 注册 schema/seed/menu/工作流到 PMocker
├── pmocker/                  // ★ PMocker 元数据扩展（gva 原生没有）
│   ├── schema.yaml          // EAV 字段定义
│   ├── seed.yaml            // 种子数据
│   ├── menu.yaml            // 菜单树
│   ├── workflows/           // 工作流模板
│   │   └── requirement_review.yaml
│   └── manifest.yaml        // 插件清单
├── config/
│   └── config.go
├── global/
│   └── global.go
└── README.md
```

前端插件：

```
gva/web/src/plugin/pmocker/requirement/
├── api/
│   └── requirement.js       // 调用后端 API
├── view/
│   ├── index.vue            // 需求列表主页面
│   ├── detail.vue           // 需求详情
│   └── components/
│       ├── ReqForm.vue
│       └── ReqStatusFlow.vue
├── components/
│   └── index.ts
└── utils/
    └── constants.ts
```

### 11.2 PMocker 插件钩子接口

PMocker 在 gva `plugin.Plugin` 接口基础上扩展 `PMockerPlugin` 接口。**gva 原生插件不实现此接口也能正常运行**（兼容）：

```go
// pkg/pmocker/plugin/plugin.go
package plugin

import "context"

// PMockerPlugin 扩展 gva plugin.Plugin 接口
// gva 原生插件不实现此接口，PMocker 会跳过元数据初始化
type PMockerPlugin interface {
    // InitPMocker 在 PMSystem 启动时由内核调用
    // 用于：灌入 schema/seed/menu/工作流到 EAV 元表 + gva 系统表
    InitPMocker(ctx context.Context) error
}

// 注册表：PMocker 启动时扫描所有已注册插件，若实现 PMockerPlugin 则调用 InitPMocker
func InitAllPMockerPlugins(ctx context.Context, plugins []interface{}) []error {
    var errs []error
    for _, p := range plugins {
        if pmockerPlugin, ok := p.(PMockerPlugin); ok {
            if err := pmockerPlugin.InitPMocker(ctx); err != nil {
                errs = append(errs, err)
            }
        }
        // gva 原生插件（未实现 PMockerPlugin）跳过，不影响运行
    }
    return errs
}
```

### 11.3 插件 plugin.go 示例（gva Plugin 接口 + PMocker 钩子）

```go
// gva/server/plugin/pmocker/requirement/plugin.go
package requirement

import (
    "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker/requirement/router"
    "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
)

type Plugin struct{}

func CreatePlugin() *Plugin { return &Plugin{} }

// ★ gva 标准 Plugin 接口：注册路由
func (*Plugin) Register(group *gin.RouterGroup) {
    router.RouterGroupApp.InitRequirement(group)
}

// ★ PMocker 扩展钩子：插件初始化时调用
func (*Plugin) InitPMocker(ctx context.Context) error {
    // 1. 读取 pmocker/schema.yaml → 灌入 EAV 元表
    if err := eav.LoadSchemaFromPluginYaml(ctx, "pmocker/requirement", "schema.yaml"); err != nil {
        return err
    }
    // 2. 灌入种子数据
    if err := eav.LoadSeedFromPluginYaml(ctx, "pmocker/requirement", "seed.yaml"); err != nil {
        return err
    }
    // 3. 注册菜单（注入 gva sys_base_menus 表）
    if err := eav.LoadMenuFromPluginYaml(ctx, "pmocker/requirement", "menu.yaml"); err != nil {
        return err
    }
    // 4. 注册工作流模板
    return eav.LoadWorkflowsFromPluginDir(ctx, "pmocker/requirement", "workflows/")
}
```

---

## 12. gin-vue-admin 上游同步（Git Subtree）

### 12.1 初始拉取

```bash
# 把 gin-vue-admin 作为 subtree 拉入 gva/
git subtree add --prefix=gva \
  https://github.com/flipped-aurora/gin-vue-admin.git \
  main --squash
```

> `--squash` 把上游历史压成一次提交，保持主仓历史干净。

### 12.2 定期同步上游

```bash
# 拉取上游最新 main 到 gva/
git subtree pull --prefix=gva \
  https://github.com/flipped-aurora/gin-vue-admin.git \
  main --squash
```

如遇冲突，在 `gva/` 内调整后提交。PMocker 自定义代码统一放在 `gva/server/model/pmocker` 与 `gva/server/service/pmocker`，避免与上游文件冲突。

---

## 13. v1.0 MVP 实现路线图（5 个里程碑）

| 里程碑 | 交付内容 | 关键任务 | 验收标准 |
|------|------|------|------|
| **M1 · 骨架** | 仓库 + go.work + gva subtree + 空 PMocker CLI | · 初始化 git 仓库<br>· go.work 声明 cli/gva/pkg 三个模块<br>· subtree 拉 gva v3.0.0<br>· `pmocker --version` 能跑 | gva 原生能启动；pmocker 命令树存在 |
| **M2 · 内核** | EAV 引擎 + 三维权限 + 工作流引擎 | · `pkg/pmocker/eav` 实现 schema 加载 + CRUD<br>· `pkg/pmocker/rbac` 三维权限判定<br>· `pkg/pmocker/workflow` 声明式解释器<br>· gva 集成 API + 前端 host shell | 能加载 schema 创建动态字段；权限三维判定生效；能跑通需求评审工作流 |
| **M3 · 9 模块** | 10 个 gva 插件（server + web + pmocker 元数据） | · 10 个 `gva/server/plugin/pmocker/<mod>/` + `gva/web/src/plugin/pmocker/<mod>/` 目录<br>· 每插件实现 `plugin.go`（gva Plugin 接口 + InitPMocker 钩子）<br>· schema/seed/menu/workflows 元数据全部就绪<br>· CPM 算法（schedule 插件内）<br>· 挣值计算（cost 插件内）<br>· Check-in/Out（deliverable 插件内）<br>· 影响分析（change 插件内）<br>· 利用率计算（team 插件内，auto 节点） | 10 插件用 gva 后台"插件安装"功能可装载；10 模块 CRUD 跑通；CPM 能算关键路径；EVM 指标正确；交付物 Check-in/Out 生效；团队利用率自动计算 |
| **M4 · 镜像** | .pmi 镜像打包 + OCI 解析 + diff/upgrade | · `pkg/pmocker/oci` 实现 tar+manifest+layers<br>· `images/pmbok6-hybrid/build.sh` 打包 .pmi<br>· `pmocker diff` 对比两镜像<br>· `pmocker upgrade` 执行 migration | 能生成 .pmi；`pmocker diff` 输出 schema 差异；`pmocker upgrade` 保留用户数据完成升级 |
| **M5 · CLI 闭环** | 12 个 CLI 命令全跑通 + 默认镜像交付 | · 12 命令全部实现<br>· 默认镜像 `pmbok6-hybrid.pmi` 交付<br>· 文档与安装脚本 | 一条命令启动完整 PM 系统；commit/export/diff/upgrade 全闭环 |

---

## 14. 后续演进（暂不实现，仅规划方向）

| 版本 | 主题 | 说明 |
|------|------|------|
| **v1.1** | PMocker Hub（Git 驱动） | `pmocker pull` / `push` / `login` / `search`，Hub = 普通 Git 仓库，按 org/repo/tag 组织镜像 |
| **v1.2** | 团队协作 | MySQL/PostgreSQL 部署、多用户协作、并发锁、WebSocket 实时通知 |
| **v1.3** | 方法论镜像 | PRINCE2 / ISO 21502 / Scrum 敏捷预置镜像，每套对应独立 plugins 配置 |
| **v2.0** | 企业 PMO | 私有 Hub、跨实例组合视图、PMO 模板下发、SSO、审计合规报表 |

---

## 15. 附录

### 15.1 来源图例

- **Z** = 禅道 ZenTao
- **T** = Teambition
- **MSP** = MS Project
- **HT** = 华天 PLM
- **QY** = 青翼 PLM
- **PMBOK** = PMBOK 第六版

### 15.2 设计决策记录

| # | 决策点 | 选择 | 理由 |
|---|------|------|------|
| 1 | MVP 部署场景 | 单机优先（SQLite） | 快速交付，降低运维门槛 |
| 2 | v1 插件模块范围 | 10 个（需求/范围/交付物/进度/成本/风险/问题/变更/EPS/团队） | 覆盖 PMBOK 核心知识域 + PLM 行业最佳实践 + 资源管理闭环 |
| 3 | 统一数据结构模型 | EAV 动态字段（主表+属性表） | 支持镜像版本 diff 与差异化升级 |
| 4 | .pmi 镜像内部格式 | OCI tar 层（manifest + 6 类层） | 内容寻址、层级 diff、与 OCI 标准兼容 |
| 5 | v1 方法论覆盖 | PMBOK 默认 + 后期多镜像 | 聚焦 v1 交付，避免多方法论并行复杂度 |
| 6 | PMSystem 与项目关系 | 多项目共享实例 | 支持 EPS 组织级项目管理 |
| 7 | 数据持久化策略 | 独立数据卷（`~/.pmocker/volumes/<id>`） | 删实例不删数据，`rm -v` 才删 |
| 8 | 仓库结构 | 单仓 + go.work | 与 Go Workspace 技术栈匹配，跨模块联调方便 |
| 9 | 插件运行时模型 | 方案 B：gva 插件 + PMocker 元数据扩展 | 完全兼容 gva 插件规范（可走 gva 后台"插件安装"），同时保留 EAV + schema diff + 镜像升级；定制算法用 Go 代码写在插件 service/ 内 |
| 10 | 行业最佳实践对标策略 | 分模块对标 + 来源标注 | 显式对齐标杆工具能力，避免脱离实际 |
| 11 | BOM 多视图建模 | 不为 BOM 视图做特殊数据建模 | v1 作为交付物的一种属性集合，简化实现 |
| 12 | seed/menu/workflows 层归属 | 物理打包到 plugins 层内，逻辑独立灌入 | 保持 gva 插件 ZIP 结构完整，便于直接用 gva 后台安装 |
| 13 | 团队管理模块追加（r3） | 4 实体 58 字段（team_member/team_role/training_record/performance_review） | 对标 PMBOK Project Resource Management 全闭环，补齐资源管理知识域；团队投入度/技能/绩效与成本、进度、EPS 模块联动 |
