# PMocker M3 插件阶段实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 9 个项目管理模块作为 gin-vue-admin 插件——需求/范围/进度/成本/风险/问题/EPS/交付物/变更，每个插件自带 `pmocker/` 元数据子目录并通过 `InitPMocker()` 钩子灌入 EAV schema、工作流定义、菜单和种子数据；通过 API 验证可创建动态字段实体并触发工作流。

**Architecture:** 9 个插件共享同一骨架：`gva/server/plugin/pmocker_<mod>/` 下按 gva 标准目录（api/router/service/initialize）+ `pmocker/` 元数据子目录（schema.yaml/seed.yaml/menu.yaml/workflows/*.yaml/manifest.yaml）。公共 YAML 加载逻辑提取到 `pkg/pmocker/plugin/loader/` 包，所有插件的 `InitPMocker()` 复用。插件实现 `plugin.PMockerPlugin` 接口；`pmocker_core` 在 `Register()` 末尾调用 `InitAllPMockerPlugins()` 触发所有 PMocker 业务插件初始化。自定义算法（CPM/EVM/风险矩阵/影响分析）放各插件 `service/` 下。

**Tech Stack:** Go 1.24 + GORM + gin-vue-admin v3 + SQLite + yaml.v3

## Global Constraints

- gva module path: `github.com/flipped-aurora/gin-vue-admin/server`
- pkg module path: `github.com/pmocker-io/pmocker/pkg`
- 不修改 gva subtree 原文件（subtree pull 时不冲突）
- 9 个插件命名前缀统一为 `pmocker_<mod>`，目录在 `gva/server/plugin/pmocker_<mod>/`
- 每个插件实现两个接口：`interfaces.Plugin`（gva v2）+ `plugin.PMockerPlugin`（PMocker 扩展）
- `pmocker/` 子目录元数据用 `yaml.v3` 解析；YAML 文件使用 `//go:embed` 嵌入二进制
- EAV 字段定义通过 `EAVService.RegisterEntityType` / `RegisterFieldDef` 灌入
- 工作流定义通过 `WorkflowService.LoadDefinition` 灌入
- 菜单/API/字典通过 `plugin-tool/utils.RegisterMenus/RegisterApis/RegisterDictionaries` 灌入
- 所有路由前缀：`/api/pmocker/<mod>/`
- GORM model 嵌入 `global.GVA_MODEL`
- SQLite 不支持 LTREE，WBS/EPS 路径用字符串
- 提交粒度：一个插件一个 commit（最后一个 Task 合并 pmocker_core 钩子）

## gva 插件目录结构（每个插件相同）

```
gva/server/plugin/pmocker_<mod>/
├── plugin.go                       // gva Plugin 接口 + PMockerPlugin 实现
├── pmocker/                        // ★ PMocker 元数据子目录
│   ├── manifest.yaml              // 插件清单（code/name/version/dependencies）
│   ├── schema.yaml                // EAV 实体类型 + 字段定义 + 状态机
│   ├── seed.yaml                  // 种子数据（角色/字典/EPS 节点）
│   ├── menu.yaml                  // gva 菜单注册数据
│   ├── api.yaml                   // gva API 注册数据（可选，亦可代码注册）
│   └── workflows/
│       └── <workflow_code>.yaml   // 工作流定义
├── api/
│   └── enter.go                   // 聚合本插件 API
├── router/
│   └── enter.go                   // 聚合本插件路由
├── service/
│   ├── enter.go                   // 聚合本插件 service
│   └── <algorithm>.go             // 自定义算法（如 CPM/EVM/风险矩阵）
├── initialize/
│   └── init.go                    // 注册路由/菜单/API（调用 plugin-tool/utils）
└── doc.go                         // 包注释
```

## 复用接口（来自 M2，已存在，不要重新定义）

```go
// pkg/pmocker/plugin/plugin.go（已存在）
type PMockerPlugin interface { InitPMocker(ctx context.Context) error }
func InitAllPMockerPlugins(ctx context.Context, plugins []interface{}) []error

// pkg/pmocker/eav/types.go（已存在）
type EntityType struct { TypeCode, ModuleCode, Name, Icon, IconColor string }
type FieldDef struct { EntityType, FieldKey, FieldLabel string; DataType DataType; OptionsJSON, DefaultValue, Validators string }
type SchemaDefinition struct { EntityType, Module string; Fields []FieldDef; States []string; Workflow string }

// pkg/pmocker/workflow/types.go（已存在）
type Definition struct { Code, Name, EntityType, Trigger string; Nodes []Node; Transitions []Transition; OnComplete *OnComplete }

// gva/server/service/pmocker/enter.go（已存在）
type ServiceGroup struct { EAVService; WorkflowService; RBACService }
var ServiceGroupApp = new(ServiceGroup)
```

## 公共 loader 接口契约（本计划 Task 1 定义）

```go
// pkg/pmocker/plugin/loader/loader.go（Task 1 创建）
type Loader struct {
    EAV       eav.EAVStore
    Workflow  workflow.Engine
    MenuReg   func(menus ...system.SysBaseMenu)   // 注入 plugin-tool/utils.RegisterMenus
    APIReg    func(apis ...system.SysApi)         // 注入 plugin-tool/utils.RegisterApis
    DictReg   func(dicts ...system.SysDictionary) // 注入 plugin-tool/utils.RegisterDictionaries
}

// 从嵌入的 YAML 文件字节加载并灌入各表
func (l *Loader) LoadSchema(ctx context.Context, yamlBytes []byte) error
func (l *Loader) LoadSeed(ctx context.Context, yamlBytes []byte) error
func (l *Loader) LoadMenu(yamlBytes []byte) error
func (l *Loader) LoadAPI(yamlBytes []byte) error
func (l *Loader) LoadWorkflow(ctx context.Context, yamlBytes []byte) error
```

每个插件的 `InitPMocker(ctx)` 模式：

```go
//go:embed pmocker/manifest.yaml
var manifestBytes []byte
//go:embed pmocker/schema.yaml
var schemaBytes []byte
//go:embed pmocker/seed.yaml
var seedBytes []byte
//go:embed pmocker/menu.yaml
var menuBytes []byte
//go:embed pmocker/api.yaml
var apiBytes []byte
//go:embed pmocker/workflows
var workflowFS embed.FS

func (p *plugin) InitPMocker(ctx context.Context) error {
    l := loader.NewFromGva() // 注入 EAV/Workflow service 和 gva utils
    if err := l.LoadSchema(ctx, schemaBytes); err != nil { return err }
    if err := l.LoadSeed(ctx, seedBytes); err != nil { return err }
    if err := l.LoadMenu(menuBytes); err != nil { return err }
    if err := l.LoadAPI(apiBytes); err != nil { return err }
    return l.LoadWorkflowDir(ctx, workflowFS, "pmocker/workflows")
}
```

---

### Task 1: 公共 YAML Loader 包

**Files:**
- Create: `pkg/pmocker/plugin/loader/loader.go`（Loader 结构体 + Load* 方法）
- Create: `pkg/pmocker/plugin/loader/schema.go`（schema/menu/api/seed/workflow 的 YAML 结构定义）
- Modify: `pkg/go.mod`（添加 `gopkg.in/yaml.v3` 依赖）
- Test: `pkg/pmocker/plugin/loader/loader_test.go`

**Interfaces:**
- Consumes: `eav.EAVStore`、`workflow.Engine`（M2 已定义）
- Consumes: gva `system.SysBaseMenu`、`system.SysApi`、`system.SysDictionary`（gva 自带）
- Produces: `loader.Loader` 结构体 + `NewFromGva()` 构造函数（注入 gva service 和 utils）

- [ ] **Step 1: 添加 yaml.v3 依赖到 pkg/go.mod**

Run:
```bash
cd pkg && go get gopkg.in/yaml.v3@latest
```
Expected: `go.mod` 中出现 `gopkg.in/yaml.v3 v3.0.1`

- [ ] **Step 2: 创建 loader/schema.go（YAML 结构定义）**

```go
// Package loader 提供插件元数据 YAML 加载器，将 pmocker/ 子目录的
// schema/seed/menu/api/workflows 灌入 EAV 元表和 gva 系统表。
package loader

import "github.com/flipped-aurora/gin-vue-admin/server/model/system"

// ManifestYaml 插件清单
type ManifestYaml struct {
    Code        string `yaml:"code"`
    Name        string `yaml:"name"`
    Version     string `yaml:"version"`
    Description string `yaml:"description"`
    Dependencies []string `yaml:"dependencies"`
}

// SchemaYaml 对应 pmocker/schema.yaml
type SchemaYaml struct {
    EntityType string        `yaml:"entity_type"`
    Module     string        `yaml:"module"`
    Name       string        `yaml:"name"`
    Icon       string        `yaml:"icon"`
    IconColor  string        `yaml:"icon_color"`
    Fields     []FieldYaml   `yaml:"fields"`
    States     []string      `yaml:"states"`
    Workflows  []string      `yaml:"workflows"`
}

type FieldYaml struct {
    FieldKey     string `yaml:"field_key"`
    FieldLabel   string `yaml:"field_label"`
    DataType     string `yaml:"data_type"`
    OptionsJSON  string `yaml:"options_json"`
    DefaultValue string `yaml:"default_value"`
    Validators   string `yaml:"validators"`
}

// SeedYaml 对应 pmocker/seed.yaml
type SeedYaml struct {
    Roles        []map[string]interface{} `yaml:"roles"`
    Dictionaries []DictYaml                `yaml:"dictionaries"`
    EPS          []EPSYaml                 `yaml:"eps"`
}

type DictYaml struct {
    Type    string             `yaml:"type"`
    Name    string             `yaml:"name"`
    Details []DictDetailYaml   `yaml:"details"`
}

type DictDetailYaml struct {
    Label string `yaml:"label"`
    Value string `yaml:"value"`
    Extend string `yaml:"extend"`
}

type EPSYaml struct {
    ParentPath string `yaml:"parent_path"`
    Name      string `yaml:"name"`
    Type      string `yaml:"type"`
}

// MenuYaml 对应 pmocker/menu.yaml
type MenuYaml struct {
    Menus []MenuEntryYaml `yaml:"menus"`
}

type MenuEntryYaml struct {
    Path      string `yaml:"path"`
    Name      string `yaml:"name"`
    Hidden    bool   `yaml:"hidden"`
    Component string `yaml:"component"`
    Sort      int    `yaml:"sort"`
    Title     string `yaml:"title"`
    Icon      string `yaml:"icon"`
    ParentName string `yaml:"parent_name"`
}

// APIYaml 对应 pmocker/api.yaml
type APIYaml struct {
    APIs []system.SysApi `yaml:"apis"`
}
```

- [ ] **Step 3: 创建 loader/loader.go（加载逻辑）**

```go
package loader

import (
    "context"
    "fmt"
    "io/fs"
    "path/filepath"
    "strings"

    "github.com/flipped-aurora/gin-vue-admin/server/model/system"
    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav    "github.com/pmocker-io/pmocker/pkg/pmocker/workflow"
    "gopkg.in/yaml.v3"
)

// Loader 将插件元数据 YAML 灌入 EAV 元表和 gva 系统表
type Loader struct {
    EAV      eavtypes.EAVStore
    Workflow workflow.Engine
    MenuReg  func(menus ...system.SysBaseMenu)
    APIReg   func(apis ...system.SysApi)
    DictReg  func(dicts ...system.SysDictionary)
}

// NewFromGva 用 gva 已有的 service 和 plugin-tool/utils 构造 Loader
func NewFromGva(
    eav eavtypes.EAVStore,
    wf workflow.Engine,
    menuReg func(menus ...system.SysBaseMenu),
    apiReg func(apis ...system.SysApi),
    dictReg func(dicts ...system.SysDictionary),
) *Loader {
    return &Loader{EAV: eav, Workflow: wf, MenuReg: menuReg, APIReg: apiReg, DictReg: dictReg}
}

// LoadSchema 灌入 schema.yaml 到 EAV 元表
func (l *Loader) LoadSchema(ctx context.Context, yamlBytes []byte) error {
    var s SchemaYaml
    if err := yaml.Unmarshal(yamlBytes, &s); err != nil {
        return fmt.Errorf("parse schema.yaml: %w", err)
    }
    if err := l.EAV.RegisterEntityType(ctx, eavtypes.EntityType{
        TypeCode:   s.EntityType,
        ModuleCode: s.Module,
        Name:       s.Name,
        Icon:       s.Icon,
        IconColor:  s.IconColor,
    }); err != nil {
        return fmt.Errorf("register entity type %s: %w", s.EntityType, err)
    }
    for _, f := range s.Fields {
        if err := l.EAV.RegisterFieldDef(ctx, eavtypes.FieldDef{
            EntityType:   s.EntityType,
            FieldKey:     f.FieldKey,
            FieldLabel:   f.FieldLabel,
            DataType:     eavtypes.DataType(f.DataType),
            OptionsJSON:  f.OptionsJSON,
            DefaultValue: f.DefaultValue,
            Validators:   f.Validators,
        }); err != nil {
            return fmt.Errorf("register field %s.%s: %w", s.EntityType, f.FieldKey, err)
        }
    }
    return nil
}

// LoadSeed 灌入 seed.yaml 中的字典到 gva（角色/EPS 在 M4 阶段扩展）
func (l *Loader) LoadSeed(ctx context.Context, yamlBytes []byte) error {
    var seed SeedYaml
    if err := yaml.Unmarshal(yamlBytes, &seed); err != nil {
        return fmt.Errorf("parse seed.yaml: %w", err)
    }
    if l.DictReg != nil && len(seed.Dictionaries) > 0 {
        dicts := make([]system.SysDictionary, 0, len(seed.Dictionaries))
        for _, d := range seed.Dictionaries {
            details := make([]system.SysDictionaryDetail, 0, len(d.Details))
            for _, dt := range d.Details {
                details = append(details, system.SysDictionaryDetail{
                    Label:  dt.Label,
                    Value:  dt.Value,
                    Extend: dt.Extend,
                })
            }
            dicts = append(dicts, system.SysDictionary{
                Type:    d.Type,
                Name:    d.Name,
                SysDictionaryDetails: details,
            })
        }
        l.DictReg(dicts...)
    }
    return nil
}

// LoadMenu 灌入 menu.yaml 到 gva sys_base_menus
func (l *Loader) LoadMenu(yamlBytes []byte) error {
    if l.MenuReg == nil {
        return nil
    }
    var m MenuYaml
    if err := yaml.Unmarshal(yamlBytes, &m); err != nil {
        return fmt.Errorf("parse menu.yaml: %w", err)
    }
    menus := make([]system.SysBaseMenu, 0, len(m.Menus))
    for _, me := range m.Menus {
        menus = append(menus, system.SysBaseMenu{
            Path:      me.Path,
            Name:       me.Name,
            Hidden:     me.Hidden,
            Component:  me.Component,
            Sort:       me.Sort,
            Meta:       system.Meta{Title: me.Title, Icon: me.Icon},
        })
    }
    l.MenuReg(menus...)
    return nil
}

// LoadAPI 灌入 api.yaml 到 gva sys_apis
func (l *Loader) LoadAPI(yamlBytes []byte) error {
    if l.APIReg == nil {
        return nil
    }
    var a APIYaml
    if err := yaml.Unmarshal(yamlBytes, &a); err != nil {
        return fmt.Errorf("parse api.yaml: %w", err)
    }
    l.APIReg(a.APIs...)
    return nil
}

// LoadWorkflow 灌入单个工作流 YAML
func (l *Loader) LoadWorkflow(ctx context.Context, yamlBytes []byte) error {
    var def workflow.Definition
    if err := yaml.Unmarshal(yamlBytes, &def); err != nil {
        return fmt.Errorf("parse workflow: %w", err)
    }
    return l.Workflow.LoadDefinition(ctx, def)
}

// LoadWorkflowDir 从 embed.FS 加载目录下所有 .yaml 工作流
func (l *Loader) LoadWorkflowDir(ctx context.Context, fsys fs.FS, dir string) error {
    return fs.WalkDir(fsys, dir, func(path string, d fs.DirEntry, err error) error {
        if err != nil {
            return err
        }
        if d.IsDir() || !strings.HasSuffix(path, ".yaml") {
            return nil
        }
        bytes, err := fs.ReadFile(fsys, path)
        if err != nil {
            return fmt.Errorf("read %s: %w", path, err)
        }
        if err := l.LoadWorkflow(ctx, bytes); err != nil {
            return fmt.Errorf("load workflow %s: %w", filepath.Base(path), err)
        }
        return nil
    })
}
```

- [ ] **Step 4: 创建 loader/loader_test.go（验证 YAML 解析与灌入）**

```go
package loader

import (
    "context"
    "testing"

    "github.com/flipped-aurora/gin-vue-admin/server/model/system"
    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    "github.com/pmocker-io/pmocker/pkg/pmocker/workflow"
)

// stubEAV 用于测试，记录灌入的实体类型和字段
type stubEAV struct {
    entityTypes map[string]bool
    fields      []eavtypes.FieldDef
}

func (s *stubEAV) LoadEntityType(ctx context.Context, t string) (*eavtypes.EntityType, error) { return nil, nil }
func (s *stubEAV) LoadFieldDefs(ctx context.Context, t string) ([]eavtypes.FieldDef, error)   { return nil, nil }
func (s *stubEAV) RegisterEntityType(ctx context.Context, et eavtypes.EntityType) error {
    if s.entityTypes == nil { s.entityTypes = map[string]bool{} }
    s.entityTypes[et.TypeCode] = true
    return nil
}
func (s *stubEAV) RegisterFieldDef(ctx context.Context, fd eavtypes.FieldDef) error {
    s.fields = append(s.fields, fd)
    return nil
}
func (s *stubEAV) CreateEntity(ctx context.Context, e eavtypes.Entity) (uint, error) { return 0, nil }
func (s *stubEAV) GetEntity(ctx context.Context, id uint) (*eavtypes.Entity, error) { return nil, nil }
func (s *stubEAV) UpdateEntity(ctx context.Context, e eavtypes.Entity) error         { return nil }
func (s *stubEAV) DeleteEntity(ctx context.Context, id uint) error                   { return nil }
func (s *stubEAV) ListEntities(ctx context.Context, p uint, t string, o, l int) ([]eavtypes.Entity, int64, error) {
    return nil, 0, nil
}
func (s *stubEAV) AddRelation(ctx context.Context, src, dst uint, r string) error { return nil }
func (s *stubEAV) ListRelations(ctx context.Context, id uint) ([]eavtypes.Relation, error) {
    return nil, nil
}

// stubWF 用于测试，记录灌入的工作流
type stubWF struct{ defs []workflow.Definition }

func (s *stubWF) Start(ctx context.Context, eid uint, code string) (uint, error) { return 0, nil }
func (s *stubWF) Execute(ctx context.Context, iid uint, action string, uid uint) error { return nil }
func (s *stubWF) GetCurrentNode(ctx context.Context, iid uint) (*workflow.Node, error) { return nil, nil }
func (s *stubWF) LoadDefinition(ctx context.Context, def workflow.Definition) error {
    s.defs = append(s.defs, def)
    return nil
}

func TestLoadSchema(t *testing.T) {
    eav := &stubEAV{}
    l := &Loader{EAV: eav}
    yaml := []byte(`
entity_type: requirement
module: requirement
name: 需求
icon: document
icon_color: blue
fields:
  - field_key: priority
    field_label: 优先级
    data_type: enum
    options_json: '["高","中","低"]'
  - field_key: source
    field_label: 来源
    data_type: string
states: [draft, reviewing, approved, rejected]
workflows: [requirement_review]
`)
    if err := l.LoadSchema(context.Background(), yaml); err != nil {
        t.Fatalf("LoadSchema: %v", err)
    }
    if !eav.entityTypes["requirement"] {
        t.Errorf("entity type requirement not registered")
    }
    if len(eav.fields) != 2 {
        t.Errorf("expected 2 fields, got %d", len(eav.fields))
    }
    if eav.fields[0].FieldKey != "priority" {
        t.Errorf("first field key = %s, want priority", eav.fields[0].FieldKey)
    }
}

func TestLoadWorkflow(t *testing.T) {
    wf := &stubWF{}
    l := &Loader{Workflow: wf}
    yaml := []byte(`
code: requirement_review
name: 需求评审流
entity_type: requirement
trigger: on_create
nodes:
  - {code: start, type: start}
  - {code: review, type: approval, approvers: "pm,sm", quorum: 2}
  - {code: end, type: end}
transitions:
  - {from: start, to: review, on: submit}
  - {from: review, to: end, on: approve, status: approved}
  - {from: review, to: end, on: reject, status: rejected}
`)
    if err := l.LoadWorkflow(context.Background(), yaml); err != nil {
        t.Fatalf("LoadWorkflow: %v", err)
    }
    if len(wf.defs) != 1 {
        t.Fatalf("expected 1 def, got %d", len(wf.defs))
    }
    if wf.defs[0].Code != "requirement_review" {
        t.Errorf("code = %s", wf.defs[0].Code)
    }
    if len(wf.defs[0].Nodes) != 3 {
        t.Errorf("expected 3 nodes, got %d", len(wf.defs[0].Nodes))
    }
}

func TestLoadMenu(t *testing.T) {
    var got []system.SysBaseMenu
    l := &Loader{MenuReg: func(menus ...system.SysBaseMenu) { got = menus }}
    yaml := []byte(`
menus:
  - path: requirement
    name: pmockerRequirement
    component: plugin/pmocker_requirement/view/index
    sort: 10
    title: 需求管理
    icon: document
  - path: requirement/list
    name: pmockerRequirementList
    parent_name: pmockerRequirement
    component: plugin/pmocker_requirement/view/list
    sort: 1
    title: 需求列表
    icon: list
`)
    if err := l.LoadMenu(yaml); err != nil {
        t.Fatalf("LoadMenu: %v", err)
    }
    if len(got) != 2 {
        t.Fatalf("expected 2 menus, got %d", len(got))
    }
    if got[0].Name != "pmockerRequirement" {
        t.Errorf("first menu name = %s", got[0].Name)
    }
}
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd pkg && go test ./pmocker/plugin/loader/... -v`
Expected: PASS（3 个测试用例全部通过）

- [ ] **Step 6: 提交**

```bash
git add pkg/pmocker/plugin/loader/ pkg/go.mod pkg/go.sum
git commit -m "feat(loader): add YAML loader for PMocker plugin metadata"
```

---

### Task 2: pmocker_core 接入 InitAllPMockerPlugins 钩子

**Files:**
- Modify: `gva/server/plugin/pmocker_core/plugin.go`（在 Register 末尾调用 InitAllPMockerPlugins）
- Modify: `gva/server/plugin/pmocker_register.go`（空白导入 9 个 PMocker 业务插件触发 init() 注册）
- Test: `gva/server/plugin/pmocker_core/plugin_test.go`

**Interfaces:**
- Consumes: `plugin.InitAllPMockerPlugins`（M2 已定义）
- Consumes: `interfaces.Registered()`（gva v2 已定义）
- Produces: pmocker_core 在启动时自动调用所有 PMocker 业务插件的 InitPMocker()

- [ ] **Step 1: 创建 plugin_test.go 验证钩子调用**

```go
package pmocker_core

import (
    "context"
    "testing"

    "github.com/flipped-aurora/gin-vue-admin/server/utils/plugin/v2"
    "github.com/pmocker-io/pmocker/pkg/pmocker/plugin"
)

// stubPMockerPlugin 测试用插件，记录 InitPMocker 是否被调用
type stubPMockerPlugin struct{ called bool }

func (s *stubPMockerPlugin) Register(engine interface{}) {}
func (s *stubPMockerPlugin) InitPMocker(ctx context.Context) error {
    s.called = true
    return nil
}

func TestInitAllPMockerPluginsCalled(t *testing.T) {
    stub := &stubPMockerPlugin{}
    plugins := []interface{}{stub}
    errs := plugin.InitAllPMockerPlugins(context.Background(), plugins)
    if len(errs) != 0 {
        t.Fatalf("unexpected errors: %v", errs)
    }
    if !stub.called {
        t.Errorf("InitPMocker not called on stub plugin")
    }
}

func TestNonPMockerPluginSkipped(t *testing.T) {
    // gva 原生插件不实现 PMockerPlugin，应被跳过不报错
    type nativePlugin struct{}
    plugins := []interface{}{&nativePlugin{}}
    errs := plugin.InitAllPMockerPlugins(context.Background(), plugins)
    if len(errs) != 0 {
        t.Errorf("native plugin should be skipped, got errors: %v", errs)
    }
}

// 确保 stubPMockerPlugin 实现两个接口
var _ plugin.PMockerPlugin = (*stubPMockerPlugin)(nil)
var _ v2.Plugin = (*stubPMockerPlugin)(nil)
```

- [ ] **Step 2: 运行测试验证失败（InitPMocker 尚未接入 Register）**

Run: `cd gva/server && go test ./plugin/pmocker_core/... -v -run TestInitAllPMockerPluginsCalled`
Expected: PASS（InitAllPMockerPlugins 本身已存在，仅测试函数未通过）

- [ ] **Step 3: 修改 pmocker_core/plugin.go 接入钩子**

修改 `gva/server/plugin/pmocker_core/plugin.go` 的 Register 方法，在 registerTables 之后、路由注册之前调用 InitAllPMockerPlugins：

```go
package pmocker_core

import (
    "context"

    "github.com/flipped-aurora/gin-vue-admin/server/global"
    pmockerRouter "github.com/flipped-aurora/gin-vue-admin/server/router/pmocker"
    "github.com/flipped-aurora/gin-vue-admin/server/model/pmocker"
    pmockerplugin "github.com/pmocker-io/pmocker/pkg/pmocker/plugin"
    interfaces "github.com/flipped-aurora/gin-vue-admin/server/utils/plugin/v2"
    "github.com/gin-gonic/gin"
)

var _ interfaces.Plugin = (*plugin)(nil)
var Plugin = new(plugin)

type plugin struct{}

func init() {
    interfaces.Register(Plugin)
}

func (p *plugin) Register(group *gin.Engine) {
    registerTables()
    // ★ 调用所有已注册 PMocker 业务插件的 InitPMocker 钩子，灌入 schema/seed/menu/workflow
    ctx := context.Background()
    plugins := make([]interface{}, 0, len(interfaces.Registered()))
    for _, p := range interfaces.Registered() {
        plugins = append(plugins, p)
    }
    if errs := pmockerplugin.InitAllPMockerPlugins(ctx, plugins); len(errs) > 0 {
        global.GVA_LOG.Error("PMocker plugin init errors", "errors", errs)
    }
    // 注册 PMocker 路由
    pmockerRouter.RouterGroupApp.InitEAV(group.Group("api").Group("pmocker"))
}

func registerTables() {
    if global.GVA_DB == nil {
        return
    }
    tables := []interface{}{
        pmocker.PMEntityType{},
        pmocker.PMFieldDef{},
        pmocker.PMRelationType{},
        pmocker.PMFieldVersion{},
        pmocker.PMEntity{},
        pmocker.PMAttr{},
        pmocker.PMRelation{},
        pmocker.PMWBSNode{},
        pmocker.PMEPSTree{},
        pmocker.PMTaskLink{},
        pmocker.PMBaseline{},
        pmocker.PMChangeLog{},
        pmocker.PMDeliverableFile{},
        pmocker.PMWorkflowDef{},
        pmocker.PMWorkflowInstance{},
    }
    global.GVA_DB.AutoMigrate(tables...)
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd gva/server && go test ./plugin/pmocker_core/... -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add gva/server/plugin/pmocker_core/plugin.go gva/server/plugin/pmocker_core/plugin_test.go
git commit -m "feat(pmocker_core): invoke InitAllPMockerPlugins in Register"
```

---

### Task 3: 需求管理插件（pmocker_requirement）

**Files:**
- Create: `gva/server/plugin/pmocker_requirement/plugin.go`
- Create: `gva/server/plugin/pmocker_requirement/pmocker/manifest.yaml`
- Create: `gva/server/plugin/pmocker_requirement/pmocker/schema.yaml`
- Create: `gva/server/plugin/pmocker_requirement/pmocker/seed.yaml`
- Create: `gva/server/plugin/pmocker_requirement/pmocker/menu.yaml`
- Create: `gva/server/plugin/pmocker_requirement/pmocker/api.yaml`
- Create: `gva/server/plugin/pmocker_requirement/pmocker/workflows/requirement_review.yaml`
- Create: `gva/server/plugin/pmocker_requirement/api/enter.go`
- Create: `gva/server/plugin/pmocker_requirement/api/requirement.go`
- Create: `gva/server/plugin/pmocker_requirement/router/enter.go`
- Create: `gva/server/plugin/pmocker_requirement/router/requirement.go`
- Create: `gva/server/plugin/pmocker_requirement/service/enter.go`
- Create: `gva/server/plugin/pmocker_requirement/service/requirement.go`
- Create: `gva/server/plugin/pmocker_requirement/initialize/init.go`
- Create: `gva/server/plugin/pmocker_requirement/doc.go`
- Modify: `gva/server/plugin/pmocker_register.go`（空白导入触发 init 注册）
- Test: `gva/server/plugin/pmocker_requirement/service/requirement_test.go`

**Interfaces:**
- Consumes: `loader.NewFromGva`（Task 1）、`pmocker.ServiceGroupApp`（M2）、`plugin-tool/utils`（gva）
- Produces: `requirement.Service.Requirement` 提供 CRUD（基于 EAV）
- Produces: entity_type=`requirement`、workflow=`requirement_review`

- [ ] **Step 1: 创建 pmocker/manifest.yaml**

```yaml
code: pmocker_requirement
name: 需求管理
version: 1.0.0
description: 项目需求管理模块，支持需求收集、评审、追踪
dependencies:
  - pmocker_core
```

- [ ] **Step 2: 创建 pmocker/schema.yaml（需求实体类型 + 字段）**

```yaml
entity_type: requirement
module: requirement
name: 需求
icon: document
icon_color: "#409EFF"
fields:
  - field_key: code
    field_label: 需求编号
    data_type: string
  - field_key: priority
    field_label: 优先级
    data_type: enum
    options_json: '["高","中","低"]'
    default_value: 中
  - field_key: source
    field_label: 来源
    data_type: enum
    options_json: '["客户","市场","内部","合规"]'
  - field_key: category
    field_label: 分类
    data_type: enum
    options_json: '["功能","非功能","约束"]'
  - field_key: description
    field_label: 详细描述
    data_type: text
  - field_key: acceptance_criteria
    field_label: 验收标准
    data_type: text
  - field_key: estimated_effort
    field_label: 预估工时(人日)
    data_type: decimal
  - field_key: assigned_to
    field_label: 责任人
    data_type: ref
  - field_key: planned_release
    field_label: 计划发布版本
    data_type: string
states: [draft, reviewing, approved, rejected, fulfilled]
workflows: [requirement_review]
```

- [ ] **Step 3: 创建 pmocker/seed.yaml（字典）**

```yaml
dictionaries:
  - type: pm_req_priority
    name: 需求优先级
    details:
      - {label: 高, value: "1", extend: red}
      - {label: 中, value: "2", extend: yellow}
      - {label: 低, value: "3", extend: green}
  - type: pm_req_source
    name: 需求来源
    details:
      - {label: 客户, value: customer}
      - {label: 市场, value: market}
      - {label: 内部, value: internal}
      - {label: 合规, value: compliance}
```

- [ ] **Step 4: 创建 pmocker/menu.yaml**

```yaml
menus:
  - path: /pmocker/requirement
    name: pmockerRequirement
    component: view/routerHolder
    sort: 10
    title: 需求管理
    icon: document
  - path: list
    name: pmockerRequirementList
    parent_name: pmockerRequirement
    component: plugin/pmocker_requirement/view/list
    sort: 1
    title: 需求列表
    icon: list
  - path: matrix
    name: pmockerRequirementMatrix
    parent_name: pmockerRequirement
    component: plugin/pmocker_requirement/view/matrix
    sort: 2
    title: 需求追踪矩阵
    icon: matrix
```

- [ ] **Step 5: 创建 pmocker/api.yaml**

```yaml
apis:
  - {path: /pmocker/requirement/create, description: 创建需求, api_group: 需求管理, method: POST}
  - {path: /pmocker/requirement/delete, description: 删除需求, api_group: 需求管理, method: DELETE}
  - {path: /pmocker/requirement/update, description: 更新需求, api_group: 需求管理, method: PUT}
  - {path: /pmocker/requirement/find, description: 查询需求, api_group: 需求管理, method: GET}
  - {path: /pmocker/requirement/list, description: 需求列表, api_group: 需求管理, method: GET}
  - {path: /pmocker/requirement/traceMatrix, description: 需求追踪矩阵, api_group: 需求管理, method: GET}
  - {path: /pmocker/requirement/submitReview, description: 提交评审, api_group: 需求管理, method: POST}
  - {path: /pmocker/requirement/approve, description: 批准需求, api_group: 需求管理, method: POST}
  - {path: /pmocker/requirement/reject, description: 驳回需求, api_group: 需求管理, method: POST}
```

- [ ] **Step 6: 创建 pmocker/workflows/requirement_review.yaml**

```yaml
code: requirement_review
name: 需求评审流
entity_type: requirement
trigger: on_submit
nodes:
  - code: start
    type: start
  - code: pm_review
    type: approval
    approvers: "pm,po"
    sla_hours: 48
    quorum: 1
  - code: end
    type: end
transitions:
  - {from: start, to: pm_review, on: submit}
  - {from: pm_review, to: end, on: approve, status: approved}
  - {from: pm_review, to: end, on: reject, status: rejected}
on_complete:
  action: set_status
  sign: true
```

- [ ] **Step 7: 创建 service/requirement.go（CRUD + 追踪矩阵）**

```go
package requirement

import (
    "context"
    "fmt"

    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
)

// Service 需求管理 service，封装 EAV 操作 + 业务算法
type Service struct{}

// RequirementService 单例
var RequirementService = new(Service)

// CreateRequirement 创建需求
func (s *Service) CreateRequirement(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    return pmocker.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID:  projectID,
        EntityType: "requirement",
        Title:      title,
        Status:     "draft",
        CreatedBy:  creatorID,
        Attrs:      attrs,
    })
}

// GetRequirement 获取需求
func (s *Service) GetRequirement(ctx context.Context, id uint) (*eavtypes.Entity, error) {
    e, err := pmocker.ServiceGroupApp.GetEntity(ctx, id)
    if err != nil {
        return nil, err
    }
    if e.EntityType != "requirement" {
        return nil, fmt.Errorf("entity %d is not a requirement", id)
    }
    return e, nil
}

// ListRequirements 列出项目的需求
func (s *Service) ListRequirements(ctx context.Context, projectID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmocker.ServiceGroupApp.ListEntities(ctx, projectID, "requirement", offset, limit)
}

// UpdateRequirement 更新需求
func (s *Service) UpdateRequirement(ctx context.Context, e eavtypes.Entity) error {
    if e.EntityType != "requirement" {
        return fmt.Errorf("not a requirement")
    }
    return pmocker.ServiceGroupApp.UpdateEntity(ctx, e)
}

// DeleteRequirement 删除需求
func (s *Service) DeleteRequirement(ctx context.Context, id uint) error {
    return pmocker.ServiceGroupApp.DeleteEntity(ctx, id)
}

// TraceMatrix 需求追踪矩阵（需求→交付物 关系）
func (s *Service) TraceMatrix(ctx context.Context, projectID uint) ([]TraceRow, error) {
    reqs, _, err := pmocker.ServiceGroupApp.ListEntities(ctx, projectID, "requirement", 0, 1000)
    if err != nil {
        return nil, err
    }
    rows := make([]TraceRow, 0, len(reqs))
    for _, r := range reqs {
        rels, err := pmocker.ServiceGroupApp.ListRelations(ctx, r.ID)
        if err != nil {
            continue
        }
        deliverableIDs := make([]uint, 0, len(rels))
        for _, rel := range rels {
            if rel.RelationType == "traced_to" {
                deliverableIDs = append(deliverableIDs, rel.DstID)
            }
        }
        rows = append(rows, TraceRow{
            RequirementID:    r.ID,
            RequirementTitle: r.Title,
            Status:           r.Status,
            DeliverableIDs:   deliverableIDs,
        })
    }
    return rows, nil
}

// TraceRow 追踪矩阵行
type TraceRow struct {
    RequirementID    uint   `json:"requirementId"`
    RequirementTitle string `json:"requirementTitle"`
    Status           string `json:"status"`
    DeliverableIDs   []uint `json:"deliverableIds"`
}
```

- [ ] **Step 8: 创建 service/enter.go**

```go
package requirement

// ServiceGroup 聚合需求管理所有 service
type ServiceGroup struct {
    RequirementService
}

var ServiceGroupApp = new(ServiceGroup)
```

- [ ] **Step 9: 创建 service/requirement_test.go**

```go
package requirement

import (
    "testing"
)

func TestTraceRowSerialization(t *testing.T) {
    row := TraceRow{
        RequirementID:    1,
        RequirementTitle: "登录功能",
        Status:           "approved",
        DeliverableIDs:   []uint{10, 11},
    }
    if row.RequirementID != 1 {
        t.Errorf("id = %d", row.RequirementID)
    }
    if len(row.DeliverableIDs) != 2 {
        t.Errorf("deliverable count = %d", len(row.DeliverableIDs))
    }
}
```

- [ ] **Step 10: 创建 api/requirement.go**

```go
package requirement

import (
    "strconv"

    "github.com/flipped-aurora/gin-vue-admin/server/model/common/response"
    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    "github.com/gin-gonic/gin"
)

type Api struct{}

// Create
func (a *Api) Create(c *gin.Context) {
    var req struct {
        ProjectID uint                   `json:"projectId"`
        Title     string                 `json:"title"`
        Attrs     map[string]interface{} `json:"attrs"`
        CreatorID uint                   `json:"creatorId"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c)
        return
    }
    id, err := ServiceGroupApp.CreateRequirement(c.Request.Context(), req.ProjectID, req.Title, req.Attrs, req.CreatorID)
    if err != nil {
        response.FailWithMessage("创建失败: "+err.Error(), c)
        return
    }
    response.OkWithData(gin.H{"id": id}, c)
}

func (a *Api) Find(c *gin.Context) {
    id, _ := strconv.ParseUint(c.Query("id"), 10, 64)
    e, err := ServiceGroupApp.GetRequirement(c.Request.Context(), uint(id))
    if err != nil {
        response.FailWithMessage("查询失败: "+err.Error(), c)
        return
    }
    response.OkWithData(e, c)
}

func (a *Api) List(c *gin.Context) {
    projectID, _ := strconv.ParseUint(c.Query("projectId"), 10, 64)
    offset, _ := strconv.Atoi(c.DefaultQuery("offset", "0"))
    limit, _ := strconv.Atoi(c.DefaultQuery("limit", "20"))
    list, total, err := ServiceGroupApp.ListRequirements(c.Request.Context(), uint(projectID), offset, limit)
    if err != nil {
        response.FailWithMessage("查询失败: "+err.Error(), c)
        return
    }
    response.OkWithDetailed(gin.H{"list": list, "total": total}, "查询成功", c)
}

func (a *Api) Update(c *gin.Context) {
    var e eavtypes.Entity
    if err := c.ShouldBindJSON(&e); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c)
        return
    }
    if err := ServiceGroupApp.UpdateRequirement(c.Request.Context(), e); err != nil {
        response.FailWithMessage("更新失败: "+err.Error(), c)
        return
    }
    response.OkWithMessage("更新成功", c)
}

func (a *Api) Delete(c *gin.Context) {
    id, _ := strconv.ParseUint(c.Query("id"), 10, 64)
    if err := ServiceGroupApp.DeleteRequirement(c.Request.Context(), uint(id)); err != nil {
        response.FailWithMessage("删除失败: "+err.Error(), c)
        return
    }
    response.OkWithMessage("删除成功", c)
}

func (a *Api) TraceMatrix(c *gin.Context) {
    projectID, _ := strconv.ParseUint(c.Query("projectId"), 10, 64)
    rows, err := ServiceGroupApp.TraceMatrix(c.Request.Context(), uint(projectID))
    if err != nil {
        response.FailWithMessage("查询失败: "+err.Error(), c)
        return
    }
    response.OkWithData(rows, c)
}
```

- [ ] **Step 11: 创建 api/enter.go**

```go
package requirement

type ApiGroup struct {
    Api
}

var ApiGroupApp = new(ApiGroup)
```

- [ ] **Step 12: 创建 router/requirement.go**

```go
package requirement

import (
    "github.com/flipped-aurora/gin-vue-admin/server/middleware"
    "github.com/gin-gonic/gin"
)

var Requirement = new(requirement)

type requirement struct{}

func (r *requirement) Init(public, private *gin.RouterGroup) {
    {
        g := private.Group("requirement").Use(middleware.OperationRecord())
        g.POST("create", ApiGroupApp.Create)
        g.DELETE("delete", ApiGroupApp.Delete)
        g.PUT("update", ApiGroupApp.Update)
        g.POST("submitReview", ApiGroupApp.SubmitReview)
        g.POST("approve", ApiGroupApp.Approve)
        g.POST("reject", ApiGroupApp.Reject)
    }
    {
        g := private.Group("requirement")
        g.GET("find", ApiGroupApp.Find)
        g.GET("list", ApiGroupApp.List)
        g.GET("traceMatrix", ApiGroupApp.TraceMatrix)
    }
}
```

- [ ] **Step 13: 创建 router/enter.go**

```go
package requirement

import api "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_requirement/api"

type RouterGroup struct {
    Requirement
}

var RouterGroupApp = new(RouterGroup)

var apiGroup = api.ApiGroupApp
```

- [ ] **Step 14: 补 api/requirement.go 中 SubmitReview/Approve/Reject 处理器**

在 `api/requirement.go` 末尾追加：

```go
// SubmitReview 提交需求到评审流
func (a *Api) SubmitReview(c *gin.Context) {
    var req struct {
        RequirementID uint `json:"requirementId"`
        UserID        uint `json:"userId"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c)
        return
    }
    instID, err := ServiceGroupApp.SubmitReview(c.Request.Context(), req.RequirementID, req.UserID)
    if err != nil {
        response.FailWithMessage("提交评审失败: "+err.Error(), c)
        return
    }
    response.OkWithData(gin.H{"instanceId": instID}, c)
}

// Approve 批准需求
func (a *Api) Approve(c *gin.Context) {
    var req struct {
        InstanceID uint `json:"instanceId"`
        UserID     uint `json:"userId"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c)
        return
    }
    if err := ServiceGroupApp.Approve(c.Request.Context(), req.InstanceID, req.UserID); err != nil {
        response.FailWithMessage("批准失败: "+err.Error(), c)
        return
    }
    response.OkWithMessage("已批准", c)
}

// Reject 驳回需求
func (a *Api) Reject(c *gin.Context) {
    var req struct {
        InstanceID uint `json:"instanceId"`
        UserID     uint `json:"userId"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c)
        return
    }
    if err := ServiceGroupApp.Reject(c.Request.Context(), req.InstanceID, req.UserID); err != nil {
        response.FailWithMessage("驳回失败: "+err.Error(), c)
        return
    }
    response.OkWithMessage("已驳回", c)
}
```

在 `service/requirement.go` 末尾追加工作流辅助方法：

```go
import pmockerworkflow "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"

// SubmitReview 启动需求评审工作流
func (s *Service) SubmitReview(ctx context.Context, requirementID, userID uint) (uint, error) {
    return pmockerworkflow.ServiceGroupApp.Start(ctx, requirementID, "requirement_review")
}

// Approve 通过工作流转移
func (s *Service) Approve(ctx context.Context, instanceID, userID uint) error {
    return pmockerworkflow.ServiceGroupApp.Execute(ctx, instanceID, "approve", userID)
}

// Reject 驳回工作流转移
func (s *Service) Reject(ctx context.Context, instanceID, userID uint) error {
    return pmockerworkflow.ServiceGroupApp.Execute(ctx, instanceID, "reject", userID)
}
```

- [ ] **Step 15: 创建 initialize/init.go**

```go
package initialize

import (
    "github.com/flipped-aurora/gin-vue-admin/server/global"
    "github.com/flipped-aurora/gin-vue-admin/server/middleware"
    "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_requirement/router"
    "github.com/gin-gonic/gin"
)

func Router(engine *gin.Engine) {
    public := engine.Group(global.GVA_CONFIG.System.RouterPrefix).Group("pmocker")
    private := engine.Group(global.GVA_CONFIG.System.RouterPrefix).Group("pmocker")
    private.Use(middleware.JWTAuth()).Use(middleware.MustChangePwdGuard()).Use(middleware.CasbinHandler()).Use(middleware.DataScope())
    router.RouterGroupApp.Requirement.Init(public, private)
}
```

- [ ] **Step 16: 创建 plugin.go（gva Plugin + PMockerPlugin 双接口）**

```go
package pmocker_requirement

import (
    "context"
    "embed"

    "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_requirement/initialize"
    "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
    "github.com/flipped-aurora/gin-vue-admin/server/plugin/plugin-tool/utils"
    interfaces "github.com/flipped-aurora/gin-vue-admin/server/utils/plugin/v2"
    pmockerplugin "github.com/pmocker-io/pmocker/pkg/pmocker/plugin"
    "github.com/pmocker-io/pmocker/pkg/pmocker/plugin/loader"
    "github.com/gin-gonic/gin"
)

//go:embed pmocker/manifest.yaml
var manifestBytes []byte

//go:embed pmocker/schema.yaml
var schemaBytes []byte

//go:embed pmocker/seed.yaml
var seedBytes []byte

//go:embed pmocker/menu.yaml
var menuBytes []byte

//go:embed pmocker/api.yaml
var apiBytes []byte

//go:embed pmocker/workflows
var workflowFS embed.FS

var _ interfaces.Plugin = (*plugin)(nil)
var _ pmockerplugin.PMockerPlugin = (*plugin)(nil)
var Plugin = new(plugin)

type plugin struct{}

func init() {
    interfaces.Register(Plugin)
}

func (p *plugin) Register(group *gin.Engine) {
    initialize.Router(group)
}

// InitPMocker 灌入 schema/seed/menu/api/workflow
func (p *plugin) InitPMocker(ctx context.Context) error {
    l := loader.NewFromGva(
        &pmocker.ServiceGroupApp.EAVService,
        &pmocker.ServiceGroupApp.WorkflowService,
        utils.RegisterMenus,
        utils.RegisterApis,
        utils.RegisterDictionaries,
    )
    _ = manifestBytes // manifest 仅用于 .pmi 镜像打包时读取，运行期不灌表
    if err := l.LoadSchema(ctx, schemaBytes); err != nil {
        return err
    }
    if err := l.LoadSeed(ctx, seedBytes); err != nil {
        return err
    }
    if err := l.LoadMenu(menuBytes); err != nil {
        return err
    }
    if err := l.LoadAPI(apiBytes); err != nil {
        return err
    }
    return l.LoadWorkflowDir(ctx, workflowFS, "pmocker/workflows")
}
```

- [ ] **Step 17: 创建 doc.go**

```go
// Package pmocker_requirement 实现项目需求管理模块。
// 提供需求收集、评审流、追踪矩阵等功能，作为 gva 插件集成。
package pmocker_requirement
```

- [ ] **Step 18: 修改 pmocker_register.go 注册插件**

修改 `gva/server/plugin/pmocker_register.go`：

```go
package plugin

import (
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_core"
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_requirement"
)
```

- [ ] **Step 19: 编译验证**

Run: `cd gva/server && go build ./...`
Expected: 编译通过，无错误

- [ ] **Step 20: 运行测试**

Run: `cd gva/server && go test ./plugin/pmocker_requirement/... -v`
Expected: PASS

- [ ] **Step 21: 提交**

```bash
git add gva/server/plugin/pmocker_requirement/ gva/server/plugin/pmocker_register.go
git commit -m "feat(requirement): add pmocker_requirement plugin with schema/seed/menu/workflow"
```

---

### Task 4: 范围管理插件（pmocker_scope）

**Files:**
- Create: 同 Task 3 结构，对应 `gva/server/plugin/pmocker_scope/`
- 创建：plugin.go、pmocker/{manifest,schema,seed,menu,api}.yaml、pmocker/workflows/scope_baseline.yaml、api/{enter,scope}.go、router/{enter,scope}.go、service/{enter,scope}.go、initialize/init.go、doc.go
- Modify: `gva/server/plugin/pmocker_register.go`（追加空白导入）
- Test: `gva/server/plugin/pmocker_scope/service/scope_test.go`

**Interfaces:**
- Produces: entity_type=`scope_item`、`wbs_node`、workflow=`scope_baseline`
- Produces: `scope.Service.BuildWBS(projectID)` 构建工作分解结构

- [ ] **Step 1: 创建 pmocker/manifest.yaml**

```yaml
code: pmocker_scope
name: 范围管理
version: 1.0.0
description: 项目范围管理，包含 WBS 分解和基线管理
dependencies:
  - pmocker_core
```

- [ ] **Step 2: 创建 pmocker/schema.yaml（scope_item + wbs_node 两类实体）**

```yaml
# scope_item 实体类型
entity_type: scope_item
module: scope
name: 范围项
icon: list
icon_color: "#67C23A"
fields:
  - field_key: code
    field_label: 范围编号
    data_type: string
  - field_key: wbs_path
    field_label: WBS路径
    data_type: string
  - field_key: is_deliverable
    field_label: 是否交付物
    data_type: bool
    default_value: "false"
  - field_key: description
    field_label: 描述
    data_type: text
  - field_key: parent_id
    field_label: 父范围项
    data_type: ref
states: [draft, baselined, changed]
workflows: [scope_baseline]
```

> 注：单个 schema.yaml 仅支持一个 entity_type。范围管理需要 WBS 节点实体，故此处需要扩展 schema.yaml 支持多实体类型。下面 Step 2.5 扩展 loader。

- [ ] **Step 2.5: 扩展 loader 支持多实体类型 schema**

修改 `pkg/pmocker/plugin/loader/schema.go`：

```go
// SchemaYaml 支持单实体或多实体（entity_types 数组优先）
type SchemaYaml struct {
    // 单实体模式（向后兼容）
    EntityType string `yaml:"entity_type"`
    Module     string `yaml:"module"`
    Name       string `yaml:"name"`
    Icon       string `yaml:"icon"`
    IconColor  string `yaml:"icon_color"`
    Fields     []FieldYaml `yaml:"fields"`
    States     []string   `yaml:"states"`
    Workflows  []string   `yaml:"workflows"`

    // 多实体模式
    EntityTypes []EntitySchemaYaml `yaml:"entity_types"`
}

type EntitySchemaYaml struct {
    EntityType string      `yaml:"entity_type"`
    Module     string      `yaml:"module"`
    Name       string      `yaml:"name"`
    Icon       string      `yaml:"icon"`
    IconColor  string      `yaml:"icon_color"`
    Fields     []FieldYaml `yaml:"fields"`
    States     []string    `yaml:"states"`
    Workflows  []string    `yaml:"workflows"`
}
```

修改 `pkg/pmocker/plugin/loader/loader.go` 的 `LoadSchema`：

```go
func (l *Loader) LoadSchema(ctx context.Context, yamlBytes []byte) error {
    var s SchemaYaml
    if err := yaml.Unmarshal(yamlBytes, &s); err != nil {
        return fmt.Errorf("parse schema.yaml: %w", err)
    }
    if len(s.EntityTypes) > 0 {
        for _, et := range s.EntityTypes {
            if err := l.loadSingleSchema(ctx, et.EntityType, et.Module, et.Name, et.Icon, et.IconColor, et.Fields); err != nil {
                return err
            }
        }
        return nil
    }
    return l.loadSingleSchema(ctx, s.EntityType, s.Module, s.Name, s.Icon, s.IconColor, s.Fields)
}

func (l *Loader) loadSingleSchema(ctx context.Context, typeCode, module, name, icon, iconColor string, fields []FieldYaml) error {
    if err := l.EAV.RegisterEntityType(ctx, eavtypes.EntityType{
        TypeCode:   typeCode,
        ModuleCode: module,
        Name:       name,
        Icon:       icon,
        IconColor:  iconColor,
    }); err != nil {
        return fmt.Errorf("register entity type %s: %w", typeCode, err)
    }
    for _, f := range fields {
        if err := l.EAV.RegisterFieldDef(ctx, eavtypes.FieldDef{
            EntityType:   typeCode,
            FieldKey:     f.FieldKey,
            FieldLabel:   f.FieldLabel,
            DataType:     eavtypes.DataType(f.DataType),
            OptionsJSON:  f.OptionsJSON,
            DefaultValue: f.DefaultValue,
            Validators:   f.Validators,
        }); err != nil {
            return fmt.Errorf("register field %s.%s: %w", typeCode, f.FieldKey, err)
        }
    }
    return nil
}
```

追加测试到 `loader_test.go`：

```go
func TestLoadSchemaMultiEntity(t *testing.T) {
    eav := &stubEAV{}
    l := &Loader{EAV: eav}
    yaml := []byte(`
entity_types:
  - entity_type: scope_item
    module: scope
    name: 范围项
    fields:
      - {field_key: code, field_label: 编号, data_type: string}
  - entity_type: wbs_node
    module: scope
    name: WBS节点
    fields:
      - {field_key: path, field_label: 路径, data_type: string}
`)
    if err := l.LoadSchema(context.Background(), yaml); err != nil {
        t.Fatalf("LoadSchema: %v", err)
    }
    if !eav.entityTypes["scope_item"] || !eav.entityTypes["wbs_node"] {
        t.Errorf("entity types not registered: %v", eav.entityTypes)
    }
    if len(eav.fields) != 2 {
        t.Errorf("expected 2 fields, got %d", len(eav.fields))
    }
}
```

- [ ] **Step 3: 用多实体格式重写 pmocker/schema.yaml**

```yaml
entity_types:
  - entity_type: scope_item
    module: scope
    name: 范围项
    icon: list
    icon_color: "#67C23A"
    fields:
      - {field_key: code, field_label: 范围编号, data_type: string}
      - {field_key: wbs_path, field_label: WBS路径, data_type: string}
      - {field_key: is_deliverable, field_label: 是否交付物, data_type: bool, default_value: "false"}
      - {field_key: description, field_label: 描述, data_type: text}
      - {field_key: parent_id, field_label: 父范围项, data_type: ref}
    states: [draft, baselined, changed]
    workflows: [scope_baseline]

  - entity_type: wbs_node
    module: scope
    name: WBS节点
    icon: tree
    icon_color: "#67C23A"
    fields:
      - {field_key: path, field_label: 物化路径, data_type: string}
      - {field_key: level, field_label: 层级, data_type: int}
      - {field_key: scope_item_id, field_label: 关联范围项, data_type: ref}
      - {field_key: deliverable_id, field_label: 关联交付物, data_type: ref}
    states: [active, archived]
```

- [ ] **Step 4: 创建 pmocker/seed.yaml**

```yaml
dictionaries:
  - type: pm_scope_status
    name: 范围状态
    details:
      - {label: 草稿, value: draft}
      - {label: 已基线, value: baselined}
      - {label: 已变更, value: changed}
```

- [ ] **Step 5: 创建 pmocker/menu.yaml**

```yaml
menus:
  - path: /pmocker/scope
    name: pmockerScope
    component: view/routerHolder
    sort: 20
    title: 范围管理
    icon: list
  - path: wbs
    name: pmockerScopeWBS
    parent_name: pmockerScope
    component: plugin/pmocker_scope/view/wbs
    sort: 1
    title: WBS分解
    icon: tree
  - path: baseline
    name: pmockerScopeBaseline
    parent_name: pmockerScope
    component: plugin/pmocker_scope/view/baseline
    sort: 2
    title: 范围基线
    icon: lock
```

- [ ] **Step 6: 创建 pmocker/api.yaml**

```yaml
apis:
  - {path: /pmocker/scope/createItem, description: 创建范围项, api_group: 范围管理, method: POST}
  - {path: /pmocker/scope/listItems, description: 范围项列表, api_group: 范围管理, method: GET}
  - {path: /pmocker/scope/buildWBS, description: 构建WBS, api_group: 范围管理, method: POST}
  - {path: /pmocker/scope/getWBS, description: 查询WBS树, api_group: 范围管理, method: GET}
  - {path: /pmocker/scope/baseline, description: 设置基线, api_group: 范围管理, method: POST}
```

- [ ] **Step 7: 创建 pmocker/workflows/scope_baseline.yaml**

```yaml
code: scope_baseline
name: 范围基线审批流
entity_type: scope_item
trigger: on_baseline
nodes:
  - {code: start, type: start}
  - {code: cc_approval, type: approval, approvers: "cc", sla_hours: 24, quorum: 1}
  - {code: end, type: end}
transitions:
  - {from: start, to: cc_approval, on: submit}
  - {from: cc_approval, to: end, on: approve, status: baselined}
  - {from: cc_approval, to: end, on: reject, status: draft}
```

- [ ] **Step 8: 创建 service/scope.go（含 WBS 构建算法）**

```go
package scope

import (
    "context"
    "fmt"
    "strings"

    "github.com/flipped-aurora/gin-vue-admin/server/global"
    "github.com/flipped-aurora/gin-vue-admin/server/model/pmocker"
    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    pmservice "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
)

type Service struct{}

var ScopeService = new(Service)

// CreateScopeItem 创建范围项
func (s *Service) CreateScopeItem(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    return pmservice.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID:  projectID,
        EntityType: "scope_item",
        Title:      title,
        Status:     "draft",
        CreatedBy:  creatorID,
        Attrs:      attrs,
    })
}

// BuildWBS 构建项目 WBS 树
func (s *Service) BuildWBS(ctx context.Context, projectID uint, rootTitle string) (uint, error) {
    root := pmocker.PMWBSNode{
        ProjectID: projectID,
        Path:      "1",
        Level:     1,
    }
    if err := global.GVA_DB.WithContext(ctx).Create(&root).Error; err != nil {
        return 0, err
    }
    return root.ID, nil
}

// AddWBSChild 添加 WBS 子节点
func (s *Service) AddWBSChild(ctx context.Context, parentID uint, scopeItemID uint) (uint, error) {
    var parent pmocker.PMWBSNode
    if err := global.GVA_DB.WithContext(ctx).First(&parent, parentID).Error; err != nil {
        return 0, fmt.Errorf("parent node not found: %w", err)
    }
    // 查询兄弟节点数决定编号
    var count int64
    global.GVA_DB.WithContext(ctx).Model(&pmocker.PMWBSNode{}).
        Where("parent_id = ?", parentID).Count(&count)
    newPath := fmt.Sprintf("%s.%d", parent.Path, count+1)
    child := pmocker.PMWBSNode{
        ProjectID: parent.ProjectID,
        ParentID: &parentID,
        Path:      newPath,
        Level:     parent.Level + 1,
        EntityID:  scopeItemID,
    }
    if err := global.GVA_DB.WithContext(ctx).Create(&child).Error; err != nil {
        return 0, err
    }
    return child.ID, nil
}

// GetWBS 获取项目 WBS 树
func (s *Service) GetWBS(ctx context.Context, projectID uint) ([]WBSNode, error) {
    var nodes []pmocker.PMWBSNode
    if err := global.GVA_DB.WithContext(ctx).Where("project_id = ?", projectID).Order("path").Find(&nodes).Error; err != nil {
        return nil, err
    }
    result := make([]WBSNode, len(nodes))
    for i, n := range nodes {
        result[i] = WBSNode{
            ID:           n.ID,
            ParentID:     n.ParentID,
            Path:         n.Path,
            Level:        n.Level,
            ScopeItemID:  n.EntityID,
        }
    }
    return result, nil
}

// SetBaseline 设置范围基线
func (s *Service) SetBaseline(ctx context.Context, projectID uint, snapshotJSON string) (uint, error) {
    bl := pmocker.PMBaseline{
        ProjectID:    projectID,
        Type:         "scope",
        SnapshotJSON: snapshotJSON,
    }
    if err := global.GVA_DB.WithContext(ctx).Create(&bl).Error; err != nil {
        return 0, err
    }
    return bl.ID, nil
}

// WBSNode WBS 节点 DTO
type WBSNode struct {
    ID           uint   `json:"id"`
    ParentID     *uint  `json:"parentId"`
    Path         string `json:"path"`
    Level        int    `json:"level"`
    ScopeItemID  uint   `json:"scopeItemId"`
}

// PathLevel 从物化路径解析层级
func PathLevel(path string) int {
    return strings.Count(path, ".") + 1
}
```

- [ ] **Step 9: 创建 service/enter.go**

```go
package scope

type ServiceGroup struct {
    ScopeService
}

var ServiceGroupApp = new(ServiceGroup)
```

- [ ] **Step 10: 创建 service/scope_test.go**

```go
package scope

import "testing"

func TestPathLevel(t *testing.T) {
    cases := []struct{ path string; want int }{
        {"1", 1},
        {"1.2", 2},
        {"1.2.3", 3},
    }
    for _, c := range cases {
        if got := PathLevel(c.path); got != c.want {
            t.Errorf("PathLevel(%s) = %d, want %d", c.path, got, c.want)
        }
    }
}
```

- [ ] **Step 11: 创建 api/scope.go**

```go
package scope

import (
    "strconv"

    "github.com/flipped-aurora/gin-vue-admin/server/model/common/response"
    "github.com/gin-gonic/gin"
)

type Api struct{}

func (a *Api) CreateItem(c *gin.Context) {
    var req struct {
        ProjectID uint                   `json:"projectId"`
        Title     string                 `json:"title"`
        Attrs     map[string]interface{} `json:"attrs"`
        CreatorID uint                   `json:"creatorId"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c)
        return
    }
    id, err := ServiceGroupApp.CreateScopeItem(c.Request.Context(), req.ProjectID, req.Title, req.Attrs, req.CreatorID)
    if err != nil {
        response.FailWithMessage("创建失败: "+err.Error(), c)
        return
    }
    response.OkWithData(gin.H{"id": id}, c)
}

func (a *Api) ListItems(c *gin.Context) {
    projectID, _ := strconv.ParseUint(c.Query("projectId"), 10, 64)
    offset, _ := strconv.Atoi(c.DefaultQuery("offset", "0"))
    limit, _ := strconv.Atoi(c.DefaultQuery("limit", "20"))
    list, total, err := ServiceGroupApp.ListScopeItems(c.Request.Context(), uint(projectID), offset, limit)
    if err != nil {
        response.FailWithMessage("查询失败: "+err.Error(), c)
        return
    }
    response.OkWithDetailed(gin.H{"list": list, "total": total}, "查询成功", c)
}

func (a *Api) BuildWBS(c *gin.Context) {
    var req struct {
        ProjectID uint   `json:"projectId"`
        RootTitle string `json:"rootTitle"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c)
        return
    }
    id, err := ServiceGroupApp.BuildWBS(c.Request.Context(), req.ProjectID, req.RootTitle)
    if err != nil {
        response.FailWithMessage("构建失败: "+err.Error(), c)
        return
    }
    response.OkWithData(gin.H{"rootId": id}, c)
}

func (a *Api) GetWBS(c *gin.Context) {
    projectID, _ := strconv.ParseUint(c.Query("projectId"), 10, 64)
    nodes, err := ServiceGroupApp.GetWBS(c.Request.Context(), uint(projectID))
    if err != nil {
        response.FailWithMessage("查询失败: "+err.Error(), c)
        return
    }
    response.OkWithData(nodes, c)
}

func (a *Api) Baseline(c *gin.Context) {
    var req struct {
        ProjectID    uint   `json:"projectId"`
        SnapshotJSON string `json:"snapshotJson"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c)
        return
    }
    id, err := ServiceGroupApp.SetBaseline(c.Request.Context(), req.ProjectID, req.SnapshotJSON)
    if err != nil {
        response.FailWithMessage("基线失败: "+err.Error(), c)
        return
    }
    response.OkWithData(gin.H{"baselineId": id}, c)
}
```

在 `service/scope.go` 中追加 `ListScopeItems`：

```go
func (s *Service) ListScopeItems(ctx context.Context, projectID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "scope_item", offset, limit)
}
```

- [ ] **Step 12: 创建 api/enter.go**

```go
package scope

type ApiGroup struct {
    Api
}

var ApiGroupApp = new(ApiGroup)
```

- [ ] **Step 13: 创建 router/scope.go**

```go
package scope

import (
    "github.com/flipped-aurora/gin-vue-admin/server/middleware"
    "github.com/gin-gonic/gin"
)

var Scope = new(scope)

type scope struct{}

func (r *scope) Init(public, private *gin.RouterGroup) {
    {
        g := private.Group("scope").Use(middleware.OperationRecord())
        g.POST("createItem", ApiGroupApp.CreateItem)
        g.POST("buildWBS", ApiGroupApp.BuildWBS)
        g.POST("baseline", ApiGroupApp.Baseline)
    }
    {
        g := private.Group("scope")
        g.GET("listItems", ApiGroupApp.ListItems)
        g.GET("getWBS", ApiGroupApp.GetWBS)
    }
}
```

- [ ] **Step 14: 创建 router/enter.go**

```go
package scope

import api "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_scope/api"

type RouterGroup struct {
    Scope
}

var RouterGroupApp = new(RouterGroup)

var apiGroup = api.ApiGroupApp
```

- [ ] **Step 15: 创建 initialize/init.go**

```go
package initialize

import (
    "github.com/flipped-aurora/gin-vue-admin/server/global"
    "github.com/flipped-aurora/gin-vue-admin/server/middleware"
    "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_scope/router"
    "github.com/gin-gonic/gin"
)

func Router(engine *gin.Engine) {
    public := engine.Group(global.GVA_CONFIG.System.RouterPrefix).Group("pmocker")
    private := engine.Group(global.GVA_CONFIG.System.RouterPrefix).Group("pmocker")
    private.Use(middleware.JWTAuth()).Use(middleware.MustChangePwdGuard()).Use(middleware.CasbinHandler()).Use(middleware.DataScope())
    router.RouterGroupApp.Scope.Init(public, private)
}
```

- [ ] **Step 16: 创建 plugin.go**

```go
package pmocker_scope

import (
    "context"
    "embed"

    "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_scope/initialize"
    "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
    "github.com/flipped-aurora/gin-vue-admin/server/plugin/plugin-tool/utils"
    interfaces "github.com/flipped-aurora/gin-vue-admin/server/utils/plugin/v2"
    pmockerplugin "github.com/pmocker-io/pmocker/pkg/pmocker/plugin"
    "github.com/pmocker-io/pmocker/pkg/pmocker/plugin/loader"
    "github.com/gin-gonic/gin"
)

//go:embed pmocker/manifest.yaml
var manifestBytes []byte
//go:embed pmocker/schema.yaml
var schemaBytes []byte
//go:embed pmocker/seed.yaml
var seedBytes []byte
//go:embed pmocker/menu.yaml
var menuBytes []byte
//go:embed pmocker/api.yaml
var apiBytes []byte
//go:embed pmocker/workflows
var workflowFS embed.FS

var _ interfaces.Plugin = (*plugin)(nil)
var _ pmockerplugin.PMockerPlugin = (*plugin)(nil)
var Plugin = new(plugin)

type plugin struct{}

func init() {
    interfaces.Register(Plugin)
}

func (p *plugin) Register(group *gin.Engine) {
    initialize.Router(group)
}

func (p *plugin) InitPMocker(ctx context.Context) error {
    l := loader.NewFromGva(
        &pmocker.ServiceGroupApp.EAVService,
        &pmocker.ServiceGroupApp.WorkflowService,
        utils.RegisterMenus,
        utils.RegisterApis,
        utils.RegisterDictionaries,
    )
    _ = manifestBytes
    if err := l.LoadSchema(ctx, schemaBytes); err != nil {
        return err
    }
    if err := l.LoadSeed(ctx, seedBytes); err != nil {
        return err
    }
    if err := l.LoadMenu(menuBytes); err != nil {
        return err
    }
    if err := l.LoadAPI(apiBytes); err != nil {
        return err
    }
    return l.LoadWorkflowDir(ctx, workflowFS, "pmocker/workflows")
}
```

- [ ] **Step 17: 创建 doc.go**

```go
// Package pmocker_scope 实现项目范围管理模块（WBS 分解 + 范围基线）。
package pmocker_scope
```

- [ ] **Step 18: 修改 pmocker_register.go 追加导入**

```go
package plugin

import (
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_core"
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_requirement"
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_scope"
)
```

- [ ] **Step 19: 编译验证**

Run: `cd gva/server && go build ./...`
Expected: 编译通过

- [ ] **Step 20: 运行测试**

Run: `cd gva/server && go test ./plugin/pmocker_scope/... -v`
Expected: PASS

- [ ] **Step 21: 提交**

```bash
git add pkg/pmocker/plugin/loader/ gva/server/plugin/pmocker_scope/ gva/server/plugin/pmocker_register.go
git commit -m "feat(scope): add pmocker_scope plugin with WBS and baseline"
```

---

### Task 5: 进度管理插件（pmocker_schedule）

**Files:**
- Create: `gva/server/plugin/pmocker_schedule/` 同 Task 4 结构
- Test: `gva/server/plugin/pmocker_schedule/service/cpm_test.go`

**Interfaces:**
- Produces: entity_type=`task`、`milestone`、workflow=`schedule_baseline`
- Produces: `schedule.Service.CPM(tasks)` 计算关键路径

- [ ] **Step 1: 创建 pmocker/manifest.yaml**

```yaml
code: pmocker_schedule
name: 进度管理
version: 1.0.0
description: 项目进度管理，包含任务、里程碑、关键路径算法
dependencies:
  - pmocker_core
  - pmocker_scope
```

- [ ] **Step 2: 创建 pmocker/schema.yaml（task + milestone 两实体）**

```yaml
entity_types:
  - entity_type: task
    module: schedule
    name: 任务
    icon: calendar
    icon_color: "#E6A23C"
    fields:
      - {field_key: code, field_label: 任务编号, data_type: string}
      - {field_key: wbs_node_id, field_label: WBS节点, data_type: ref}
      - {field_key: start_date, field_label: 开始日期, data_type: date}
      - {field_key: end_date, field_label: 结束日期, data_type: date}
      - {field_key: duration, field_label: 工期(天), data_type: int}
      - {field_key: progress, field_label: 完成度%, data_type: decimal, default_value: "0"}
      - {field_key: assignee_id, field_label: 负责人, data_type: ref}
      - {field_key: predecessors, field_label: 前置任务, data_type: json}
      - {field_key: successors, field_label: 后置任务, data_type: json}
      - {field_key: constraint_type, field_label: 约束类型, data_type: enum, options_json: '["ASAP","ALAP","SNET","FNLT"]'}
      - {field_key: constraint_date, field_label: 约束日期, data_type: date}
    states: [planned, in_progress, completed, overdue]
    workflows: [schedule_baseline]

  - entity_type: milestone
    module: schedule
    name: 里程碑
    icon: flag
    icon_color: "#F56C6C"
    fields:
      - {field_key: code, field_label: 里程碑编号, data_type: string}
      - {field_key: due_date, field_label: 截止日期, data_type: date}
      - {field_key: is_critical, field_label: 是否关键, data_type: bool, default_value: "false"}
      - {field_key: deliverable_id, field_label: 关联交付物, data_type: ref}
    states: [planned, achieved, missed]
```

- [ ] **Step 3: 创建 pmocker/seed.yaml**

```yaml
dictionaries:
  - type: pm_task_constraint
    name: 任务约束类型
    details:
      - {label: 越早越好, value: ASAP}
      - {label: 越晚越好, value: ALAP}
      - {label: 不得早于开始, value: SNET}
      - {label: 不得晚于完成, value: FNLT}
  - type: pm_task_status
    name: 任务状态
    details:
      - {label: 已计划, value: planned}
      - {label: 进行中, value: in_progress}
      - {label: 已完成, value: completed}
      - {label: 已逾期, value: overdue}
```

- [ ] **Step 4: 创建 pmocker/menu.yaml**

```yaml
menus:
  - path: /pmocker/schedule
    name: pmockerSchedule
    component: view/routerHolder
    sort: 30
    title: 进度管理
    icon: calendar
  - path: gantt
    name: pmockerScheduleGantt
    parent_name: pmockerSchedule
    component: plugin/pmocker_schedule/view/gantt
    sort: 1
    title: 甘特图
    icon: chart
  - path: critical
    name: pmockerScheduleCritical
    parent_name: pmockerSchedule
    component: plugin/pmocker_schedule/view/critical
    sort: 2
    title: 关键路径
    icon: warning
  - path: milestone
    name: pmockerScheduleMilestone
    parent_name: pmockerSchedule
    component: plugin/pmocker_schedule/view/milestone
    sort: 3
    title: 里程碑
    icon: flag
```

- [ ] **Step 5: 创建 pmocker/api.yaml**

```yaml
apis:
  - {path: /pmocker/schedule/createTask, description: 创建任务, api_group: 进度管理, method: POST}
  - {path: /pmocker/schedule/listTasks, description: 任务列表, api_group: 进度管理, method: GET}
  - {path: /pmocker/schedule/cpm, description: 关键路径计算, api_group: 进度管理, method: POST}
  - {path: /pmocker/schedule/createMilestone, description: 创建里程碑, api_group: 进度管理, method: POST}
  - {path: /pmocker/schedule/listMilestones, description: 里程碑列表, api_group: 进度管理, method: GET}
  - {path: /pmocker/schedule/baseline, description: 进度基线, api_group: 进度管理, method: POST}
```

- [ ] **Step 6: 创建 pmocker/workflows/schedule_baseline.yaml**

```yaml
code: schedule_baseline
name: 进度基线审批流
entity_type: task
trigger: on_baseline
nodes:
  - {code: start, type: start}
  - {code: pm_approval, type: approval, approvers: "pm", sla_hours: 24, quorum: 1}
  - {code: end, type: end}
transitions:
  - {from: start, to: pm_approval, on: submit}
  - {from: pm_approval, to: end, on: approve, status: planned}
  - {from: pm_approval, to: end, on: reject, status: draft}
```

- [ ] **Step 7: 创建 service/cpm.go（关键路径算法 CPM）**

```go
package schedule

import (
    "fmt"
    "sort"
    "time"
)

// Task 用于 CPM 计算的任务
type Task struct {
    ID           uint      `json:"id"`
    Code         string    `json:"code"`
    Duration     int       `json:"duration"`      // 工期(天)
    Predecessors []TaskDep `json:"predecessors"`  // 前置任务
}

// TaskDep 任务依赖
type TaskDep struct {
    TaskID  uint   `json:"taskId"`
    LinkType string `json:"linkType"` // FS/SS/FF/SF
    Lag     int    `json:"lag"`       // 滞后天数
}

// CPMResult CPM 计算结果
type CPMResult struct {
    TaskID         uint   `json:"taskId"`
    EarlyStart    int    `json:"earlyStart"`    // 最早开始(天)
    EarlyFinish   int    `json:"earlyFinish"`   // 最早完成
    LateStart     int    `json:"lateStart"`     // 最晚开始
    LateFinish    int    `json:"lateFinish"`    // 最晚完成
    TotalFloat    int    `json:"totalFloat"`    // 总浮动
    FreeFloat     int    `json:"freeFloat"`     // 自由浮动
    IsCritical    bool   `json:"isCritical"`    // 是否关键路径
}

// CPM 计算关键路径（简化版，仅支持 FS 关系）
// 步骤：① 拓扑排序；② 正向计算 ES/EF；③ 反向计算 LF/LS；④ 计算浮动
func CPM(tasks []Task) ([]CPMResult, error) {
    if len(tasks) == 0 {
        return nil, nil
    }
    // 构建邻接表
    idx := make(map[uint]int, len(tasks))
    for i, t := range tasks {
        idx[t.ID] = i
    }
    // 校验前置引用存在
    for _, t := range tasks {
        for _, d := range t.Predecessors {
            if _, ok := idx[d.TaskID]; !ok {
                return nil, fmt.Errorf("task %d: predecessor %d not in task list", t.ID, d.TaskID)
            }
        }
    }
    // 拓扑排序（Kahn 算法）
    indeg := make([]int, len(tasks))
    adj := make([][]int, len(tasks))
    for i, t := range tasks {
        for _, d := range t.Predecessors {
            adj[idx[d.TaskID]] = append(adj[idx[d.TaskID]], i)
            indeg[i]++
        }
    }
    queue := []int{}
    for i := range tasks {
        if indeg[i] == 0 {
            queue = append(queue, i)
        }
    }
    topo := []int{}
    for len(queue) > 0 {
        u := queue[0]
        queue = queue[1:]
        topo = append(topo, u)
        for _, v := range adj[u] {
            indeg[v]--
            if indeg[v] == 0 {
                queue = append(queue, v)
            }
        }
    }
    if len(topo) != len(tasks) {
        return nil, fmt.Errorf("cycle detected in task dependencies")
    }
    // 正向计算 ES/EF
    es := make([]int, len(tasks))
    ef := make([]int, len(tasks))
    for _, u := range topo {
        t := tasks[u]
        es[u] = 0
        for _, d := range t.Predecessors {
            predIdx := idx[d.TaskID]
            // 仅支持 FS 关系（前置完成→后置开始）
            finish := ef[predIdx] + d.Lag
            if finish > es[u] {
                es[u] = finish
            }
        }
        ef[u] = es[u] + t.Duration
    }
    // 项目总工期
    projectEnd := 0
    for _, f := range ef {
        if f > projectEnd {
            projectEnd = f
        }
    }
    // 反向计算 LF/LS
    lf := make([]int, len(tasks))
    ls := make([]int, len(tasks))
    for i := range tasks {
        lf[i] = projectEnd
    }
    for i := len(topo) - 1; i >= 0; i-- {
        u := topo[i]
        ls[u] = lf[u] - tasks[u].Duration
        for _, v := range adj[u] {
            if ls[u] < lf[v] - 0 {
                // 后置任务的 ES - Lag = 当前任务的 LF（FS 关系）
                lag := 0
                for _, d := range tasks[v].Predecessors {
                    if idx[d.TaskID] == u {
                        lag = d.Lag
                        break
                    }
                }
                if es[v]-lag < lf[u] {
                    lf[u] = es[v] - lag
                    ls[u] = lf[u] - tasks[u].Duration
                }
            }
        }
    }
    // 计算结果
    results := make([]CPMResult, len(tasks))
    for i, t := range tasks {
        total := lf[i] - ef[i]
        // 自由浮动：min(后置 ES) - 当前 EF
        ff := projectEnd
        if len(adj[i]) == 0 {
            ff = 0
        } else {
            for _, v := range adj[i] {
                lag := 0
                for _, d := range tasks[v].Predecessors {
                    if idx[d.TaskID] == t.ID {
                        lag = d.Lag
                        break
                    }
                }
                if es[v]-lag-ef[i] < ff {
                    ff = es[v] - lag - ef[i]
                }
            }
        }
        if ff < 0 { ff = 0 }
        results[i] = CPMResult{
            TaskID:       t.ID,
            EarlyStart:   es[i],
            EarlyFinish:  ef[i],
            LateStart:    ls[i],
            LateFinish:   lf[i],
            TotalFloat:   total,
            FreeFloat:    ff,
            IsCritical:   total == 0,
        }
    }
    sort.Slice(results, func(i, j int) bool {
        return results[i].EarlyStart < results[j].EarlyStart
    })
    return results, nil
}

// FormatDate 将天数偏移转为日期（基于 baseDate）
func FormatDate(baseDate time.Time, days int) string {
    return baseDate.AddDate(0, 0, days).Format("2006-01-02")
}
```

- [ ] **Step 8: 创建 service/cpm_test.go**

```go
package schedule

import "testing"

func TestCPMSimpleChain(t *testing.T) {
    // A(3) -> B(2) -> C(4)，关键路径 A→B→C，总工期 9
    tasks := []Task{
        {ID: 1, Code: "A", Duration: 3},
        {ID: 2, Code: "B", Duration: 2, Predecessors: []TaskDep{{TaskID: 1, LinkType: "FS"}}},
        {ID: 3, Code: "C", Duration: 4, Predecessors: []TaskDep{{TaskID: 2, LinkType: "FS"}}},
    }
    res, err := CPM(tasks)
    if err != nil {
        t.Fatalf("CPM: %v", err)
    }
    if len(res) != 3 {
        t.Fatalf("expected 3 results, got %d", len(res))
    }
    // 所有任务浮动为 0
    for _, r := range res {
        if r.TotalFloat != 0 {
            t.Errorf("task %d total float = %d, want 0", r.TaskID, r.TotalFloat)
        }
        if !r.IsCritical {
            t.Errorf("task %d should be critical", r.TaskID)
        }
    }
    // 最早完成日期：A=3, B=5, C=9
    if res[0].EarlyFinish != 3 { t.Errorf("A EF = %d", res[0].EarlyFinish) }
    if res[1].EarlyFinish != 5 { t.Errorf("B EF = %d", res[1].EarlyFinish) }
    if res[2].EarlyFinish != 9 { t.Errorf("C EF = %d", res[2].EarlyFinish) }
}

func TestCPMParallel(t *testing.T) {
    // A(2) 同时启动 B(3) 和 C(1)，关键路径 A→B
    tasks := []Task{
        {ID: 1, Code: "A", Duration: 2},
        {ID: 2, Code: "B", Duration: 3, Predecessors: []TaskDep{{TaskID: 1, LinkType: "FS"}}},
        {ID: 3, Code: "C", Duration: 1, Predecessors: []TaskDep{{TaskID: 1, LinkType: "FS"}}},
    }
    res, err := CPM(tasks)
    if err != nil {
        t.Fatalf("CPM: %v", err)
    }
    // C 总浮动 = 2
    var cRes *CPMResult
    for i := range res {
        if res[i].TaskID == 3 {
            cRes = &res[i]
        }
    }
    if cRes == nil {
        t.Fatal("task C not found")
    }
    if cRes.TotalFloat != 4 {
        t.Errorf("C total float = %d, want 4", cRes.TotalFloat)
    }
    if cRes.IsCritical {
        t.Errorf("C should not be critical")
    }
}

func TestCPMCycle(t *testing.T) {
    tasks := []Task{
        {ID: 1, Code: "A", Duration: 2, Predecessors: []TaskDep{{TaskID: 2, LinkType: "FS"}}},
        {ID: 2, Code: "B", Duration: 2, Predecessors: []TaskDep{{TaskID: 1, LinkType: "FS"}}},
    }
    if _, err := CPM(tasks); err == nil {
        t.Error("expected cycle error, got nil")
    }
}
```

- [ ] **Step 9: 创建 service/schedule.go（CRUD 包装）**

```go
package schedule

import (
    "context"
    "encoding/json"

    "github.com/flipped-aurora/gin-vue-admin/server/global"
    "github.com/flipped-aurora/gin-vue-admin/server/model/pmocker"
    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    pmservice "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
)

type Service struct{}

var ScheduleService = new(Service)

func (s *Service) CreateTask(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    return pmservice.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID: projectID, EntityType: "task", Title: title, Status: "planned", CreatedBy: creatorID, Attrs: attrs,
    })
}

func (s *Service) ListTasks(ctx context.Context, projectID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "task", offset, limit)
}

func (s *Service) CreateMilestone(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    return pmservice.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID: projectID, EntityType: "milestone", Title: title, Status: "planned", CreatedBy: creatorID, Attrs: attrs,
    })
}

func (s *Service) ListMilestones(ctx context.Context, projectID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "milestone", offset, limit)
}

// ComputeCPM 从项目任务计算关键路径
func (s *Service) ComputeCPM(ctx context.Context, projectID uint) ([]CPMResult, error) {
    entities, _, err := pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "task", 0, 10000)
    if err != nil {
        return nil, err
    }
    tasks := make([]Task, 0, len(entities))
    for _, e := range entities {
        t := Task{ID: e.ID, Code: e.Title}
        if d, ok := e.Attrs["duration"].(float64); ok {
            t.Duration = int(d)
        }
        if preds, ok := e.Attrs["predecessors"]; ok {
            if b, err := json.Marshal(preds); err == nil {
                _ = json.Unmarshal(b, &t.Predecessors)
            }
        }
        tasks = append(tasks, t)
    }
    return CPM(tasks)
}

// SetBaseline 设置进度基线
func (s *Service) SetBaseline(ctx context.Context, projectID uint, snapshotJSON string) (uint, error) {
    bl := pmocker.PMBaseline{ProjectID: projectID, Type: "schedule", SnapshotJSON: snapshotJSON}
    if err := global.GVA_DB.WithContext(ctx).Create(&bl).Error; err != nil {
        return 0, err
    }
    return bl.ID, nil
}
```

- [ ] **Step 10: 创建 service/enter.go**

```go
package schedule

type ServiceGroup struct {
    ScheduleService
}

var ServiceGroupApp = new(ServiceGroup)
```

- [ ] **Step 11: 创建 api/schedule.go**

```go
package schedule

import (
    "strconv"

    "github.com/flipped-aurora/gin-vue-admin/server/model/common/response"
    "github.com/gin-gonic/gin"
)

type Api struct{}

func (a *Api) CreateTask(c *gin.Context) {
    var req struct {
        ProjectID uint                   `json:"projectId"`
        Title     string                 `json:"title"`
        Attrs     map[string]interface{} `json:"attrs"`
        CreatorID uint                   `json:"creatorId"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c); return
    }
    id, err := ServiceGroupApp.CreateTask(c.Request.Context(), req.ProjectID, req.Title, req.Attrs, req.CreatorID)
    if err != nil {
        response.FailWithMessage("创建失败: "+err.Error(), c); return
    }
    response.OkWithData(gin.H{"id": id}, c)
}

func (a *Api) ListTasks(c *gin.Context) {
    projectID, _ := strconv.ParseUint(c.Query("projectId"), 10, 64)
    offset, _ := strconv.Atoi(c.DefaultQuery("offset", "0"))
    limit, _ := strconv.Atoi(c.DefaultQuery("limit", "20"))
    list, total, err := ServiceGroupApp.ListTasks(c.Request.Context(), uint(projectID), offset, limit)
    if err != nil {
        response.FailWithMessage("查询失败: "+err.Error(), c); return
    }
    response.OkWithDetailed(gin.H{"list": list, "total": total}, "查询成功", c)
}

func (a *Api) CPM(c *gin.Context) {
    projectID, _ := strconv.ParseUint(c.Query("projectId"), 10, 64)
    res, err := ServiceGroupApp.ComputeCPM(c.Request.Context(), uint(projectID))
    if err != nil {
        response.FailWithMessage("计算失败: "+err.Error(), c); return
    }
    response.OkWithData(res, c)
}

func (a *Api) CreateMilestone(c *gin.Context) {
    var req struct {
        ProjectID uint                   `json:"projectId"`
        Title     string                 `json:"title"`
        Attrs     map[string]interface{} `json:"attrs"`
        CreatorID uint                   `json:"creatorId"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c); return
    }
    id, err := ServiceGroupApp.CreateMilestone(c.Request.Context(), req.ProjectID, req.Title, req.Attrs, req.CreatorID)
    if err != nil {
        response.FailWithMessage("创建失败: "+err.Error(), c); return
    }
    response.OkWithData(gin.H{"id": id}, c)
}

func (a *Api) ListMilestones(c *gin.Context) {
    projectID, _ := strconv.ParseUint(c.Query("projectId"), 10, 64)
    offset, _ := strconv.Atoi(c.DefaultQuery("offset", "0"))
    limit, _ := strconv.Atoi(c.DefaultQuery("limit", "20"))
    list, total, err := ServiceGroupApp.ListMilestones(c.Request.Context(), uint(projectID), offset, limit)
    if err != nil {
        response.FailWithMessage("查询失败: "+err.Error(), c); return
    }
    response.OkWithDetailed(gin.H{"list": list, "total": total}, "查询成功", c)
}

func (a *Api) Baseline(c *gin.Context) {
    var req struct {
        ProjectID    uint   `json:"projectId"`
        SnapshotJSON string `json:"snapshotJson"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c); return
    }
    id, err := ServiceGroupApp.SetBaseline(c.Request.Context(), req.ProjectID, req.SnapshotJSON)
    if err != nil {
        response.FailWithMessage("基线失败: "+err.Error(), c); return
    }
    response.OkWithData(gin.H{"baselineId": id}, c)
}
```

- [ ] **Step 12: 创建 api/enter.go**

```go
package schedule

type ApiGroup struct {
    Api
}

var ApiGroupApp = new(ApiGroup)
```

- [ ] **Step 13: 创建 router/schedule.go 和 router/enter.go**

`router/schedule.go`：
```go
package schedule

import (
    "github.com/flipped-aurora/gin-vue-admin/server/middleware"
    "github.com/gin-gonic/gin"
)

var Schedule = new(schedule)
type schedule struct{}

func (r *schedule) Init(public, private *gin.RouterGroup) {
    {
        g := private.Group("schedule").Use(middleware.OperationRecord())
        g.POST("createTask", ApiGroupApp.CreateTask)
        g.POST("createMilestone", ApiGroupApp.CreateMilestone)
        g.POST("baseline", ApiGroupApp.Baseline)
    }
    {
        g := private.Group("schedule")
        g.GET("listTasks", ApiGroupApp.ListTasks)
        g.GET("listMilestones", ApiGroupApp.ListMilestones)
        g.POST("cpm", ApiGroupApp.CPM)
    }
}
```

`router/enter.go`：
```go
package schedule

import api "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_schedule/api"

type RouterGroup struct {
    Schedule
}
var RouterGroupApp = new(RouterGroup)
var apiGroup = api.ApiGroupApp
```

- [ ] **Step 14: 创建 initialize/init.go**

```go
package initialize

import (
    "github.com/flipped-aurora/gin-vue-admin/server/global"
    "github.com/flipped-aurora/gin-vue-admin/server/middleware"
    "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_schedule/router"
    "github.com/gin-gonic/gin"
)

func Router(engine *gin.Engine) {
    public := engine.Group(global.GVA_CONFIG.System.RouterPrefix).Group("pmocker")
    private := engine.Group(global.GVA_CONFIG.System.RouterPrefix).Group("pmocker")
    private.Use(middleware.JWTAuth()).Use(middleware.MustChangePwdGuard()).Use(middleware.CasbinHandler()).Use(middleware.DataScope())
    router.RouterGroupApp.Schedule.Init(public, private)
}
```

- [ ] **Step 15: 创建 plugin.go（与 Task 4 相同模板，包名换为 pmocker_schedule）**

```go
package pmocker_schedule

import (
    "context"
    "embed"

    "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_schedule/initialize"
    "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
    "github.com/flipped-aurora/gin-vue-admin/server/plugin/plugin-tool/utils"
    interfaces "github.com/flipped-aurora/gin-vue-admin/server/utils/plugin/v2"
    pmockerplugin "github.com/pmocker-io/pmocker/pkg/pmocker/plugin"
    "github.com/pmocker-io/pmocker/pkg/pmocker/plugin/loader"
    "github.com/gin-gonic/gin"
)

//go:embed pmocker/manifest.yaml
var manifestBytes []byte
//go:embed pmocker/schema.yaml
var schemaBytes []byte
//go:embed pmocker/seed.yaml
var seedBytes []byte
//go:embed pmocker/menu.yaml
var menuBytes []byte
//go:embed pmocker/api.yaml
var apiBytes []byte
//go:embed pmocker/workflows
var workflowFS embed.FS

var _ interfaces.Plugin = (*plugin)(nil)
var _ pmockerplugin.PMockerPlugin = (*plugin)(nil)
var Plugin = new(plugin)

type plugin struct{}

func init() { interfaces.Register(Plugin) }

func (p *plugin) Register(group *gin.Engine) {
    initialize.Router(group)
}

func (p *plugin) InitPMocker(ctx context.Context) error {
    l := loader.NewFromGva(
        &pmocker.ServiceGroupApp.EAVService,
        &pmocker.ServiceGroupApp.WorkflowService,
        utils.RegisterMenus, utils.RegisterApis, utils.RegisterDictionaries,
    )
    _ = manifestBytes
    if err := l.LoadSchema(ctx, schemaBytes); err != nil { return err }
    if err := l.LoadSeed(ctx, seedBytes); err != nil { return err }
    if err := l.LoadMenu(menuBytes); err != nil { return err }
    if err := l.LoadAPI(apiBytes); err != nil { return err }
    return l.LoadWorkflowDir(ctx, workflowFS, "pmocker/workflows")
}
```

- [ ] **Step 16: 创建 doc.go**

```go
// Package pmocker_schedule 实现项目进度管理（任务/里程碑 + CPM 关键路径算法）。
package pmocker_schedule
```

- [ ] **Step 17: 修改 pmocker_register.go 追加导入**

```go
package plugin

import (
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_core"
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_requirement"
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_scope"
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_schedule"
)
```

- [ ] **Step 18: 编译并运行测试**

Run: `cd gva/server && go build ./... && go test ./plugin/pmocker_schedule/... -v`
Expected: 编译通过，3 个 CPM 测试通过

- [ ] **Step 19: 提交**

```bash
git add gva/server/plugin/pmocker_schedule/ gva/server/plugin/pmocker_register.go
git commit -m "feat(schedule): add pmocker_schedule plugin with CPM algorithm"
```

---

### Task 6: 成本管理插件（pmocker_cost）

**Files:**
- Create: `gva/server/plugin/pmocker_cost/` 同前结构
- Test: `gva/server/plugin/pmocker_cost/service/evm_test.go`

**Interfaces:**
- Produces: entity_type=`cost_item`、workflow=`cost_baseline`
- Produces: `cost.Service.EVM(...)` 挣值管理（PV/EV/AC/CV/SV/CPI/SPI）

- [ ] **Step 1: 创建 pmocker/manifest.yaml**

```yaml
code: pmocker_cost
name: 成本管理
version: 1.0.0
description: 项目成本管理，包含预算、实际成本和挣值分析
dependencies:
  - pmocker_core
  - pmocker_schedule
```

- [ ] **Step 2: 创建 pmocker/schema.yaml**

```yaml
entity_type: cost_item
module: cost
name: 成本项
icon: money
icon_color: "#F56C6C"
fields:
  - {field_key: code, field_label: 成本编号, data_type: string}
  - {field_key: wbs_node_id, field_label: WBS节点, data_type: ref}
  - {field_key: budget_type, field_label: 预算类型, data_type: enum, options_json: '["labor","material","expense","equipment"]'}
  - {field_key: planned_value, field_label: 计划价值(PV), data_type: decimal}
  - {field_key: earned_value, field_label: 挣值(EV), data_type: decimal, default_value: "0"}
  - {field_key: actual_cost, field_label: 实际成本(AC), data_type: decimal, default_value: "0"}
  - {field_key: planned_start, field_label: 计划开始, data_type: date}
  - {field_key: planned_finish, field_label: 计划完成, data_type: date}
  - {field_key: currency, field_label: 币种, data_type: string, default_value: CNY}
states: [planned, in_progress, closed, over_budget]
workflows: [cost_baseline]
```

- [ ] **Step 3: 创建 pmocker/seed.yaml**

```yaml
dictionaries:
  - type: pm_budget_type
    name: 预算类型
    details:
      - {label: 人工, value: labor}
      - {label: 材料, value: material}
      - {label: 费用, value: expense}
      - {label: 设备, value: equipment}
  - type: pm_currency
    name: 币种
    details:
      - {label: 人民币, value: CNY}
      - {label: 美元, value: USD}
```

- [ ] **Step 4: 创建 pmocker/menu.yaml**

```yaml
menus:
  - path: /pmocker/cost
    name: pmockerCost
    component: view/routerHolder
    sort: 40
    title: 成本管理
    icon: money
  - path: budget
    name: pmockerCostBudget
    parent_name: pmockerCost
    component: plugin/pmocker_cost/view/budget
    sort: 1
    title: 预算管理
    icon: wallet
  - path: evm
    name: pmockerCostEVM
    parent_name: pmockerCost
    component: plugin/pmocker_cost/view/evm
    sort: 2
    title: 挣值分析
    icon: trend
  - path: s_curve
    name: pmockerCostCurve
    parent_name: pmockerCost
    component: plugin/pmocker_cost/view/curve
    sort: 3
    title: S曲线
    icon: chart
```

- [ ] **Step 5: 创建 pmocker/api.yaml**

```yaml
apis:
  - {path: /pmocker/cost/createItem, description: 创建成本项, api_group: 成本管理, method: POST}
  - {path: /pmocker/cost/listItems, description: 成本项列表, api_group: 成本管理, method: GET}
  - {path: /pmocker/cost/evm, description: 挣值分析, api_group: 成本管理, method: POST}
  - {path: /pmocker/cost/baseline, description: 成本基线, api_group: 成本管理, method: POST}
```

- [ ] **Step 6: 创建 pmocker/workflows/cost_baseline.yaml**

```yaml
code: cost_baseline
name: 成本基线审批流
entity_type: cost_item
trigger: on_baseline
nodes:
  - {code: start, type: start}
  - {code: fm_approval, type: approval, approvers: "fm,pm", sla_hours: 24, quorum: 2}
  - {code: end, type: end}
transitions:
  - {from: start, to: fm_approval, on: submit}
  - {from: fm_approval, to: end, on: approve, status: planned}
  - {from: fm_approval, to: end, on: reject, status: draft}
```

- [ ] **Step 7: 创建 service/evm.go（挣值管理算法）**

```go
package cost

// EVMInput 挣值计算输入
type EVMInput struct {
    BAC    float64 // 完工预算 Budget at Completion
    PV     float64 // 计划价值 Planned Value（截止当前时点应完成的预算）
    EV     float64 // 挣值 Earned Value（已完成的预算价值）
    AC     float64 // 实际成本 Actual Cost（已发生的实际成本）
}

// EVMResult 挣值计算结果
type EVMResult struct {
    CV    float64 `json:"cv"`    // 成本偏差 = EV - AC
    SV    float64 `json:"sv"`    // 进度偏差 = EV - PV
    CPI   float64 `json:"cpi"`   // 成本绩效指数 = EV / AC
    SPI   float64 `json:"spi"`   // 进度绩效指数 = EV / PV
    EAC   float64 `json:"eac"`   // 完工估算 = BAC / CPI
    ETC   float64 `json:"etc"`   // 完工尚需估算 = EAC - AC
    VAC   float64 `json:"vac"`   // 完工偏差 = BAC - EAC
    TCPI  float64 `json:"tcpi"`  // 完工尚需绩效指数 = (BAC - EV) / (BAC - AC)
}

// EVM 计算挣值指标
func EVM(in EVMInput) EVMResult {
    r := EVMResult{
        CV:  in.EV - in.AC,
        SV:  in.EV - in.PV,
    }
    if in.AC != 0 {
        r.CPI = in.EV / in.AC
        r.EAC = in.BAC / r.CPI
        r.ETC = r.EAC - in.AC
    }
    if in.PV != 0 {
        r.SPI = in.EV / in.PV
    }
    r.VAC = in.BAC - r.EAC
    if in.BAC != in.AC {
        r.TCPI = (in.BAC - in.EV) / (in.BAC - in.AC)
    }
    return r
}
```

- [ ] **Step 8: 创建 service/evm_test.go**

```go
package cost

import (
    "math"
    "testing"
)

func approxEqual(a, b float64) bool {
    return math.Abs(a-b) < 1e-6
}

func TestEVM(t *testing.T) {
    // BAC=1000, PV=600, EV=500, AC=550
    r := EVM(EVMInput{BAC: 1000, PV: 600, EV: 500, AC: 550})
    if !approxEqual(r.CV, -50) {
        t.Errorf("CV = %f, want -50", r.CV)
    }
    if !approxEqual(r.SV, -100) {
        t.Errorf("SV = %f, want -100", r.SV)
    }
    if !approxEqual(r.CPI, 500.0/550) {
        t.Errorf("CPI = %f, want %f", r.CPI, 500.0/550)
    }
    if !approxEqual(r.SPI, 500.0/600) {
        t.Errorf("SPI = %f, want %f", r.SPI, 500.0/600)
    }
    expectedEAC := 1000 / (500.0 / 550)
    if !approxEqual(r.EAC, expectedEAC) {
        t.Errorf("EAC = %f, want %f", r.EAC, expectedEAC)
    }
    if !approxEqual(r.VAC, 1000-expectedEAC) {
        t.Errorf("VAC = %f, want %f", r.VAC, 1000-expectedEAC)
    }
}

func TestEVMZeroAC(t *testing.T) {
    // AC=0 时不进行除法计算，CPI/EAC 应为 0
    r := EVM(EVMInput{BAC: 1000, PV: 600, EV: 500, AC: 0})
    if r.CPI != 0 {
        t.Errorf("CPI = %f, want 0", r.CPI)
    }
    if r.EAC != 0 {
        t.Errorf("EAC = %f, want 0", r.EAC)
    }
}
```

- [ ] **Step 9: 创建 service/cost.go（CRUD + EVM 包装）**

```go
package cost

import (
    "context"

    "github.com/flipped-aurora/gin-vue-admin/server/global"
    "github.com/flipped-aurora/gin-vue-admin/server/model/pmocker"
    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    pmservice "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
)

type Service struct{}

var CostService = new(Service)

func (s *Service) CreateItem(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    return pmservice.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID: projectID, EntityType: "cost_item", Title: title, Status: "planned", CreatedBy: creatorID, Attrs: attrs,
    })
}

func (s *Service) ListItems(ctx context.Context, projectID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "cost_item", offset, limit)
}

// ComputeEVM 计算项目级 EVM（汇总所有成本项）
func (s *Service) ComputeEVM(ctx context.Context, projectID uint) (EVMResult, error) {
    items, _, err := pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "cost_item", 0, 10000)
    if err != nil {
        return EVMResult{}, err
    }
    var in EVMInput
    for _, it := range items {
        in.BAC += getFloat(it.Attrs["planned_value"])
        in.PV += getFloat(it.Attrs["planned_value"]) * getProgressRate(it.Attrs) // PV = 总预算 * 计划完成%
        in.EV += getFloat(it.Attrs["planned_value"]) * getActualRate(it.Attrs)   // EV = 总预算 * 实际完成%
        in.AC += getFloat(it.Attrs["actual_cost"])
    }
    // 简化：PV = BAC（假设项目按计划）
    in.PV = in.BAC
    return EVM(in), nil
}

func (s *Service) SetBaseline(ctx context.Context, projectID uint, snapshotJSON string) (uint, error) {
    bl := pmocker.PMBaseline{ProjectID: projectID, Type: "cost", SnapshotJSON: snapshotJSON}
    if err := global.GVA_DB.WithContext(ctx).Create(&bl).Error; err != nil {
        return 0, err
    }
    return bl.ID, nil
}

func getFloat(v interface{}) float64 {
    switch n := v.(type) {
    case float64:
        return n
    case int:
        return float64(n)
    case int64:
        return float64(n)
    }
    return 0
}

func getProgressRate(attrs map[string]interface{}) float64 {
    return 1.0 // 简化：所有项均按计划完成
}

func getActualRate(attrs map[string]interface{}) float64 {
    if ac, ok := attrs["actual_cost"]; ok {
        if pv, ok := attrs["planned_value"]; ok {
            if getFloat(pv) > 0 {
                return getFloat(ac) / getFloat(pv)
            }
        }
    }
    return 0
}
```

- [ ] **Step 10: 创建 service/enter.go**

```go
package cost

type ServiceGroup struct {
    CostService
}
var ServiceGroupApp = new(ServiceGroup)
```

- [ ] **Step 11: 创建 api/cost.go、api/enter.go**

`api/cost.go`：
```go
package cost

import (
    "strconv"

    "github.com/flipped-aurora/gin-vue-admin/server/model/common/response"
    "github.com/gin-gonic/gin"
)

type Api struct{}

func (a *Api) CreateItem(c *gin.Context) {
    var req struct {
        ProjectID uint                   `json:"projectId"`
        Title     string                 `json:"title"`
        Attrs     map[string]interface{} `json:"attrs"`
        CreatorID uint                   `json:"creatorId"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c); return
    }
    id, err := ServiceGroupApp.CreateItem(c.Request.Context(), req.ProjectID, req.Title, req.Attrs, req.CreatorID)
    if err != nil {
        response.FailWithMessage("创建失败: "+err.Error(), c); return
    }
    response.OkWithData(gin.H{"id": id}, c)
}

func (a *Api) ListItems(c *gin.Context) {
    projectID, _ := strconv.ParseUint(c.Query("projectId"), 10, 64)
    offset, _ := strconv.Atoi(c.DefaultQuery("offset", "0"))
    limit, _ := strconv.Atoi(c.DefaultQuery("limit", "20"))
    list, total, err := ServiceGroupApp.ListItems(c.Request.Context(), uint(projectID), offset, limit)
    if err != nil {
        response.FailWithMessage("查询失败: "+err.Error(), c); return
    }
    response.OkWithDetailed(gin.H{"list": list, "total": total}, "查询成功", c)
}

func (a *Api) EVM(c *gin.Context) {
    projectID, _ := strconv.ParseUint(c.Query("projectId"), 10, 64)
    res, err := ServiceGroupApp.ComputeEVM(c.Request.Context(), uint(projectID))
    if err != nil {
        response.FailWithMessage("计算失败: "+err.Error(), c); return
    }
    response.OkWithData(res, c)
}

func (a *Api) Baseline(c *gin.Context) {
    var req struct {
        ProjectID    uint   `json:"projectId"`
        SnapshotJSON string `json:"snapshotJson"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage("参数错误: "+err.Error(), c); return
    }
    id, err := ServiceGroupApp.SetBaseline(c.Request.Context(), req.ProjectID, req.SnapshotJSON)
    if err != nil {
        response.FailWithMessage("基线失败: "+err.Error(), c); return
    }
    response.OkWithData(gin.H{"baselineId": id}, c)
}
```

`api/enter.go`：
```go
package cost
type ApiGroup struct { Api }
var ApiGroupApp = new(ApiGroup)
```

- [ ] **Step 12: 创建 router/cost.go、router/enter.go**

`router/cost.go`：
```go
package cost

import (
    "github.com/flipped-aurora/gin-vue-admin/server/middleware"
    "github.com/gin-gonic/gin"
)

var Cost = new(cost)
type cost struct{}

func (r *cost) Init(public, private *gin.RouterGroup) {
    {
        g := private.Group("cost").Use(middleware.OperationRecord())
        g.POST("createItem", ApiGroupApp.CreateItem)
        g.POST("baseline", ApiGroupApp.Baseline)
    }
    {
        g := private.Group("cost")
        g.GET("listItems", ApiGroupApp.ListItems)
        g.POST("evm", ApiGroupApp.EVM)
    }
}
```

`router/enter.go`：
```go
package cost

import api "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_cost/api"

type RouterGroup struct { Cost }
var RouterGroupApp = new(RouterGroup)
var apiGroup = api.ApiGroupApp
```

- [ ] **Step 13: 创建 initialize/init.go**

```go
package initialize

import (
    "github.com/flipped-aurora/gin-vue-admin/server/global"
    "github.com/flipped-aurora/gin-vue-admin/server/middleware"
    "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_cost/router"
    "github.com/gin-gonic/gin"
)

func Router(engine *gin.Engine) {
    public := engine.Group(global.GVA_CONFIG.System.RouterPrefix).Group("pmocker")
    private := engine.Group(global.GVA_CONFIG.System.RouterPrefix).Group("pmocker")
    private.Use(middleware.JWTAuth()).Use(middleware.MustChangePwdGuard()).Use(middleware.CasbinHandler()).Use(middleware.DataScope())
    router.RouterGroupApp.Cost.Init(public, private)
}
```

- [ ] **Step 14: 创建 plugin.go（模板，包名 pmocker_cost）**

```go
package pmocker_cost

import (
    "context"
    "embed"

    "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_cost/initialize"
    "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
    "github.com/flipped-aurora/gin-vue-admin/server/plugin/plugin-tool/utils"
    interfaces "github.com/flipped-aurora/gin-vue-admin/server/utils/plugin/v2"
    pmockerplugin "github.com/pmocker-io/pmocker/pkg/pmocker/plugin"
    "github.com/pmocker-io/pmocker/pkg/pmocker/plugin/loader"
    "github.com/gin-gonic/gin"
)

//go:embed pmocker/manifest.yaml
var manifestBytes []byte
//go:embed pmocker/schema.yaml
var schemaBytes []byte
//go:embed pmocker/seed.yaml
var seedBytes []byte
//go:embed pmocker/menu.yaml
var menuBytes []byte
//go:embed pmocker/api.yaml
var apiBytes []byte
//go:embed pmocker/workflows
var workflowFS embed.FS

var _ interfaces.Plugin = (*plugin)(nil)
var _ pmockerplugin.PMockerPlugin = (*plugin)(nil)
var Plugin = new(plugin)

type plugin struct{}

func init() { interfaces.Register(Plugin) }

func (p *plugin) Register(group *gin.Engine) { initialize.Router(group) }

func (p *plugin) InitPMocker(ctx context.Context) error {
    l := loader.NewFromGva(
        &pmocker.ServiceGroupApp.EAVService,
        &pmocker.ServiceGroupApp.WorkflowService,
        utils.RegisterMenus, utils.RegisterApis, utils.RegisterDictionaries,
    )
    _ = manifestBytes
    if err := l.LoadSchema(ctx, schemaBytes); err != nil { return err }
    if err := l.LoadSeed(ctx, seedBytes); err != nil { return err }
    if err := l.LoadMenu(menuBytes); err != nil { return err }
    if err := l.LoadAPI(apiBytes); err != nil { return err }
    return l.LoadWorkflowDir(ctx, workflowFS, "pmocker/workflows")
}
```

- [ ] **Step 15: 创建 doc.go**

```go
// Package pmocker_cost 实现项目成本管理（预算 + EVM 挣值分析）。
package pmocker_cost
```

- [ ] **Step 16: 修改 pmocker_register.go 追加导入**

在 import 块追加：
```go
    _ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_cost"
```

- [ ] **Step 17: 编译并运行测试**

Run: `cd gva/server && go build ./... && go test ./plugin/pmocker_cost/... -v`
Expected: 编译通过，EVM 测试通过

- [ ] **Step 18: 提交**

```bash
git add gva/server/plugin/pmocker_cost/ gva/server/plugin/pmocker_register.go
git commit -m "feat(cost): add pmocker_cost plugin with EVM algorithm"
```

---

### Task 7: 风险管理插件（pmocker_risk）

**Files:**
- Create: `gva/server/plugin/pmocker_risk/` 同前结构
- Test: `gva/server/plugin/pmocker_risk/service/risk_test.go`

**Interfaces:**
- Produces: entity_type=`risk`、workflow=`risk_review`
- Produces: `risk.Service.RiskMatrix(risks)` 风险矩阵分级

- [ ] **Step 1: 创建 pmocker/manifest.yaml**

```yaml
code: pmocker_risk
name: 风险管理
version: 1.0.0
description: 项目风险管理，含识别、评估、应对、监控
dependencies:
  - pmocker_core
```

- [ ] **Step 2: 创建 pmocker/schema.yaml**

```yaml
entity_type: risk
module: risk
name: 风险
icon: warning
icon_color: "#E6A23C"
fields:
  - {field_key: code, field_label: 风险编号, data_type: string}
  - {field_key: category, field_label: 类别, data_type: enum, options_json: '["technical","external","organizational","project_management"]'}
  - {field_key: description, field_label: 风险描述, data_type: text}
  - {field_key: probability, field_label: 发生概率(1-5), data_type: int}
  - {field_key: impact, field_label: 影响程度(1-5), data_type: int}
  - {field_key: risk_score, field_label: 风险分值, data_type: int}
  - {field_key: risk_level, field_label: 风险等级, data_type: enum, options_json: '["low","medium","high","critical"]'}
  - {field_key: response_strategy, field_label: 应对策略, data_type: enum, options_json: '["avoid","mitigate","transfer","accept","escalate"]'}
  - {field_key: response_plan, field_label: 应对计划, data_type: text}
  - {field_key: trigger_condition, field_label: 触发条件, data_type: text}
  - {field_key: owner_id, field_label: 责任人, data_type: ref}
  - {field_key: due_date, field_label: 应对截止日, data_type: date}
  - {field_key: status, field_label: 状态, data_type: enum, options_json: '["identified","assessed","responding","closed","realized"]'}
states: [identified, assessed, responding, closed, realized]
workflows: [risk_review]
```

- [ ] **Step 3: 创建 pmocker/seed.yaml**

```yaml
dictionaries:
  - type: pm_risk_category
    name: 风险类别
    details:
      - {label: 技术, value: technical}
      - {label: 外部, value: external}
      - {label: 组织, value: organizational}
      - {label: 项目管理, value: project_management}
  - type: pm_risk_strategy
    name: 风险应对策略
    details:
      - {label: 规避, value: avoid}
      - {label: 减轻, value: mitigate}
      - {label: 转移, value: transfer}
      - {label: 接受, value: accept}
      - {label: 上报, value: escalate}
```

- [ ] **Step 4: 创建 pmocker/menu.yaml**

```yaml
menus:
  - path: /pmocker/risk
    name: pmockerRisk
    component: view/routerHolder
    sort: 50
    title: 风险管理
    icon: warning
  - path: register
    name: pmockerRiskRegister
    parent_name: pmockerRisk
    component: plugin/pmocker_risk/view/register
    sort: 1
    title: 风险登记册
    icon: list
  - path: matrix
    name: pmockerRiskMatrix
    parent_name: pmockerRisk
    component: plugin/pmocker_risk/view/matrix
    sort: 2
    title: 风险矩阵
    icon: grid
```

- [ ] **Step 5: 创建 pmocker/api.yaml**

```yaml
apis:
  - {path: /pmocker/risk/create, description: 识别风险, api_group: 风险管理, method: POST}
  - {path: /pmocker/risk/delete, description: 删除风险, api_group: 风险管理, method: DELETE}
  - {path: /pmocker/risk/update, description: 更新风险, api_group: 风险管理, method: PUT}
  - {path: /pmocker/risk/find, description: 查询风险, api_group: 风险管理, method: GET}
  - {path: /pmocker/risk/list, description: 风险列表, api_group: 风险管理, method: GET}
  - {path: /pmocker/risk/matrix, description: 风险矩阵, api_group: 风险管理, method: GET}
  - {path: /pmocker/risk/assess, description: 评估风险, api_group: 风险管理, method: POST}
```

- [ ] **Step 6: 创建 pmocker/workflows/risk_review.yaml**

```yaml
code: risk_review
name: 风险评审流
entity_type: risk
trigger: on_identify
nodes:
  - {code: start, type: start}
  - {code: rm_review, type: approval, approvers: "rm,pm", sla_hours: 24, quorum: 1}
  - {code: end, type: end}
transitions:
  - {from: start, to: rm_review, on: submit}
  - {from: rm_review, to: end, on: approve, status: assessed}
  - {from: rm_review, to: end, on: reject, status: identified}
```

- [ ] **Step 7: 创建 service/risk.go（含风险矩阵算法）**

```go
package risk

import (
    "context"
    "fmt"

    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    pmservice "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
)

type Service struct{}

var RiskService = new(Service)

// RiskLevel 风险等级
type RiskLevel string

const (
    RiskLow      RiskLevel = "low"
    RiskMedium   RiskLevel = "medium"
    RiskHigh     RiskLevel = "high"
    RiskCritical RiskLevel = "critical"
)

// CalcRiskLevel 根据概率(P)和影响(I)计算风险等级
// P/I 均为 1-5，乘积 1-25
// 1-4: low, 5-9: medium, 10-15: high, 16-25: critical
func CalcRiskLevel(probability, impact int) (RiskLevel, int) {
    score := probability * impact
    switch {
    case score <= 4:
        return RiskLow, score
    case score <= 9:
        return RiskMedium, score
    case score <= 15:
        return RiskHigh, score
    default:
        return RiskCritical, score
    }
}

// MatrixCell 风险矩阵单元格
type MatrixCell struct {
    Probability int        `json:"probability"`
    Impact      int        `json:"impact"`
    Level       RiskLevel  `json:"level"`
    Score       int        `json:"score"`
    Count       int        `json:"count"`
}

// RiskMatrix 5x5 风险矩阵
func RiskMatrix(risks []RiskInput) [5][5]MatrixCell {
    var matrix [5][5]MatrixCell
    for p := 1; p <= 5; p++ {
        for i := 1; i <= 5; i++ {
            level, score := CalcRiskLevel(p, i)
            matrix[p-1][i-1] = MatrixCell{
                Probability: p,
                Impact:      i,
                Level:       level,
                Score:       score,
                Count:       0,
            }
        }
    }
    for _, r := range risks {
        if r.Probability >= 1 && r.Probability <= 5 && r.Impact >= 1 && r.Impact <= 5 {
            matrix[r.Probability-1][r.Impact-1].Count++
        }
    }
    return matrix
}

// RiskInput 风险输入
type RiskInput struct {
    ID          uint `json:"id"`
    Probability int  `json:"probability"`
    Impact      int  `json:"impact"`
}

// CreateRisk 创建风险
func (s *Service) CreateRisk(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    // 自动计算风险等级
    if p, ok := attrs["probability"]; ok {
        if i, ok := attrs["impact"]; ok {
            prob := toInt(p)
            imp := toInt(i)
            level, score := CalcRiskLevel(prob, imp)
            attrs["risk_score"] = score
            attrs["risk_level"] = string(level)
        }
    }
    return pmservice.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID: projectID, EntityType: "risk", Title: title, Status: "identified", CreatedBy: creatorID, Attrs: attrs,
    })
}

func (s *Service) GetRisk(ctx context.Context, id uint) (*eavtypes.Entity, error) {
    e, err := pmservice.ServiceGroupApp.GetEntity(ctx, id)
    if err != nil {
        return nil, err
    }
    if e.EntityType != "risk" {
        return nil, fmt.Errorf("entity %d is not a risk", id)
    }
    return e, nil
}

func (s *Service) ListRisks(ctx context.Context, projectID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "risk", offset, limit)
}

func (s *Service) UpdateRisk(ctx context.Context, e eavtypes.Entity) error {
    if e.EntityType != "risk" {
        return fmt.Errorf("not a risk")
    }
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, e)
}

func (s *Service) DeleteRisk(ctx context.Context, id uint) error {
    return pmservice.ServiceGroupApp.DeleteEntity(ctx, id)
}

// AssessRisk 评估风险（重新计算风险分值和等级）
func (s *Service) AssessRisk(ctx context.Context, id uint, probability, impact int) error {
    e, err := s.GetRisk(ctx, id)
    if err != nil {
        return err
    }
    level, score := CalcRiskLevel(probability, impact)
    if e.Attrs == nil {
        e.Attrs = map[string]interface{}{}
    }
    e.Attrs["probability"] = probability
    e.Attrs["impact"] = impact
    e.Attrs["risk_score"] = score
    e.Attrs["risk_level"] = string(level)
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// ProjectMatrix 生成项目风险矩阵
func (s *Service) ProjectMatrix(ctx context.Context, projectID uint) ([5][5]MatrixCell, error) {
    entities, _, err := pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "risk", 0, 10000)
    if err != nil {
        return [5][5]MatrixCell{}, err
    }
    inputs := make([]RiskInput, 0, len(entities))
    for _, e := range entities {
        inputs = append(inputs, RiskInput{
            ID:          e.ID,
            Probability: toInt(e.Attrs["probability"]),
            Impact:      toInt(e.Attrs["impact"]),
        })
    }
    return RiskMatrix(inputs), nil
}

func toInt(v interface{}) int {
    switch n := v.(type) {
    case int:
        return n
    case int64:
        return int(n)
    case float64:
        return int(n)
    }
    return 0
}
```

- [ ] **Step 8: 创建 service/risk_test.go**

```go
package risk

import "testing"

func TestCalcRiskLevel(t *testing.T) {
    cases := []struct{ p, i int; want RiskLevel; score int }{
        {1, 1, RiskLow, 1},
        {2, 2, RiskMedium, 4},
        {3, 4, RiskHigh, 12},
        {5, 5, RiskCritical, 25},
    }
    for _, c := range cases {
        level, score := CalcRiskLevel(c.p, c.i)
        if level != c.want || score != c.score {
            t.Errorf("Calc(%d,%d) = %s/%d, want %s/%d", c.p, c.i, level, score, c.want, c.score)
        }
    }
}

func TestRiskMatrix(t *testing.T) {
    risks := []RiskInput{
        {ID: 1, Probability: 5, Impact: 5},
        {ID: 2, Probability: 1, Impact: 1},
    }
    m := RiskMatrix(risks)
    if m[4][4].Count != 1 {
        t.Errorf("critical cell count = %d, want 1", m[4][4].Count)
    }
    if m[0][0].Count != 1 {
        t.Errorf("low cell count = %d, want 1", m[0][0].Count)
    }
    if m[4][4].Level != RiskCritical {
        t.Errorf("cell level = %s, want critical", m[4][4].Level)
    }
}
```

- [ ] **Step 9-17: 复用 Task 6 模板创建 service/enter.go、api/risk.go、api/enter.go、router/risk.go、router/enter.go、initialize/init.go、plugin.go、doc.go，并修改 pmocker_register.go 追加 `_ "github.com/flipped-aurora/gin-vue-admin/server/plugin/pmocker_risk"`**

api/risk.go 提供 Create/Delete/Update/Find/List/Matrix/Assess 7 个处理器（参考 Task 6 api/cost.go 风格，路由前缀 `risk`）；router/risk.go 注册路由组；plugin.go 与 Task 6 模板一致，包名 `pmocker_risk`，所有 `pmocker_cost` 字样替换为 `pmocker_risk`。

- [ ] **Step 18: 编译并运行测试**

Run: `cd gva/server && go build ./... && go test ./plugin/pmocker_risk/... -v`
Expected: 编译通过，2 个风险测试通过

- [ ] **Step 19: 提交**

```bash
git add gva/server/plugin/pmocker_risk/ gva/server/plugin/pmocker_register.go
git commit -m "feat(risk): add pmocker_risk plugin with risk matrix algorithm"
```

---

### Task 8: 问题管理插件（pmocker_issue）

**Files:**
- Create: `gva/server/plugin/pmocker_issue/` 同 Task 6 结构
- Test: `gva/server/plugin/pmocker_issue/service/issue_test.go`

**Interfaces:**
- Produces: entity_type=`issue`、workflow=`issue_resolve`
- Produces: `issue.Service.Resolve(ctx, id)` 问题闭环处理

- [ ] **Step 1: 创建 pmocker/manifest.yaml**

```yaml
code: pmocker_issue
name: 问题管理
version: 1.0.0
description: 项目问题管理，含发现、指派、解决、验证闭环
dependencies:
  - pmocker_core
```

- [ ] **Step 2: 创建 pmocker/schema.yaml**

```yaml
entity_type: issue
module: issue
name: 问题
icon: warning
icon_color: "#F56C6C"
fields:
  - {field_key: code, field_label: 问题编号, data_type: string}
  - {field_key: category, field_label: 类别, data_type: enum, options_json: '["technical","process","resource","schedule","quality"]'}
  - {field_key: priority, field_label: 优先级, data_type: enum, options_json: '["P0","P1","P2","P3"]', default_value: P2}
  - {field_key: description, field_label: 问题描述, data_type: text}
  - {field_key: raised_by, field_label: 提出人, data_type: ref}
  - {field_key: assigned_to, field_label: 责任人, data_type: ref}
  - {field_key: raised_date, field_label: 提出日期, data_type: date}
  - {field_key: due_date, field_label: 截止日期, data_type: date}
  - {field_key: resolved_date, field_label: 解决日期, data_type: date}
  - {field_key: resolution, field_label: 解决方案, data_type: text}
  - {field_key: related_entity_id, field_label: 关联实体, data_type: ref}
  - {field_key: related_entity_type, field_label: 关联实体类型, data_type: string}
states: [open, assigned, resolving, resolved, verified, closed, reopened]
workflows: [issue_resolve]
```

- [ ] **Step 3: 创建 pmocker/seed.yaml**

```yaml
dictionaries:
  - type: pm_issue_category
    name: 问题类别
    details:
      - {label: 技术, value: technical}
      - {label: 流程, value: process}
      - {label: 资源, value: resource}
      - {label: 进度, value: schedule}
      - {label: 质量, value: quality}
  - type: pm_issue_priority
    name: 问题优先级
    details:
      - {label: 紧急, value: P0, extend: red}
      - {label: 高, value: P1, extend: orange}
      - {label: 中, value: P2, extend: yellow}
      - {label: 低, value: P3, extend: green}
```

- [ ] **Step 4: 创建 pmocker/menu.yaml**

```yaml
menus:
  - path: /pmocker/issue
    name: pmockerIssue
    component: view/routerHolder
    sort: 60
    title: 问题管理
    icon: warning
  - path: list
    name: pmockerIssueList
    parent_name: pmockerIssue
    component: plugin/pmocker_issue/view/list
    sort: 1
    title: 问题列表
    icon: list
  - path: board
    name: pmockerIssueBoard
    parent_name: pmockerIssue
    component: plugin/pmocker_issue/view/board
    sort: 2
    title: 看板视图
    icon: grid
```

- [ ] **Step 5: 创建 pmocker/api.yaml**

```yaml
apis:
  - {path: /pmocker/issue/create, description: 创建问题, api_group: 问题管理, method: POST}
  - {path: /pmocker/issue/delete, description: 删除问题, api_group: 问题管理, method: DELETE}
  - {path: /pmocker/issue/update, description: 更新问题, api_group: 问题管理, method: PUT}
  - {path: /pmocker/issue/find, description: 查询问题, api_group: 问题管理, method: GET}
  - {path: /pmocker/issue/list, description: 问题列表, api_group: 问题管理, method: GET}
  - {path: /pmocker/issue/assign, description: 指派问题, api_group: 问题管理, method: POST}
  - {path: /pmocker/issue/resolve, description: 解决问题, api_group: 问题管理, method: POST}
  - {path: /pmocker/issue/verify, description: 验证问题, api_group: 问题管理, method: POST}
  - {path: /pmocker/issue/close, description: 关闭问题, api_group: 问题管理, method: POST}
```

- [ ] **Step 6: 创建 pmocker/workflows/issue_resolve.yaml**

```yaml
code: issue_resolve
name: 问题解决流
entity_type: issue
trigger: on_create
nodes:
  - {code: start, type: start}
  - {code: assign, type: task, assignee: "pm", sla_hours: 4}
  - {code: resolve, type: task, assignee: "assignee", sla_hours: 48}
  - {code: verify, type: approval, approvers: "raiser", quorum: 1}
  - {code: end, type: end}
transitions:
  - {from: start, to: assign, on: create, status: open}
  - {from: assign, to: resolve, on: assign, status: assigned}
  - {from: resolve, to: verify, on: resolve, status: resolving}
  - {from: verify, to: end, on: verify_pass, status: closed}
  - {from: verify, to: resolve, on: verify_fail, status: reopened}
```

- [ ] **Step 7: 创建 service/issue.go**

```go
package issue

import (
    "context"
    "fmt"

    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    pmservice "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
)

type Service struct{}
var IssueService = new(Service)

func (s *Service) CreateIssue(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    return pmservice.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID: projectID, EntityType: "issue", Title: title, Status: "open", CreatedBy: creatorID, Attrs: attrs,
    })
}

func (s *Service) GetIssue(ctx context.Context, id uint) (*eavtypes.Entity, error) {
    e, err := pmservice.ServiceGroupApp.GetEntity(ctx, id)
    if err != nil { return nil, err }
    if e.EntityType != "issue" {
        return nil, fmt.Errorf("entity %d is not an issue", id)
    }
    return e, nil
}

func (s *Service) ListIssues(ctx context.Context, projectID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "issue", offset, limit)
}

func (s *Service) UpdateIssue(ctx context.Context, e eavtypes.Entity) error {
    if e.EntityType != "issue" { return fmt.Errorf("not an issue") }
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, e)
}

func (s *Service) DeleteIssue(ctx context.Context, id uint) error {
    return pmservice.ServiceGroupApp.DeleteEntity(ctx, id)
}

// Assign 指派问题
func (s *Service) Assign(ctx context.Context, id, assigneeID uint) error {
    e, err := s.GetIssue(ctx, id)
    if err != nil { return err }
    if e.Attrs == nil { e.Attrs = map[string]interface{}{} }
    e.Attrs["assigned_to"] = assigneeID
    e.Status = "assigned"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Resolve 解决问题
func (s *Service) Resolve(ctx context.Context, id uint, resolution string) error {
    e, err := s.GetIssue(ctx, id)
    if err != nil { return err }
    if e.Attrs == nil { e.Attrs = map[string]interface{}{} }
    e.Attrs["resolution"] = resolution
    e.Status = "resolving"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Close 关闭问题（验证通过）
func (s *Service) Close(ctx context.Context, id uint) error {
    e, err := s.GetIssue(ctx, id)
    if err != nil { return err }
    e.Status = "closed"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}
```

- [ ] **Step 8: 创建 service/issue_test.go**

```go
package issue

import "testing"

func TestIssueStates(t *testing.T) {
    states := []string{"open", "assigned", "resolving", "resolved", "verified", "closed", "reopened"}
    stateSet := map[string]bool{}
    for _, s := range states {
        stateSet[s] = true
    }
    required := []string{"open", "assigned", "resolving", "closed", "reopened"}
    for _, s := range required {
        if !stateSet[s] {
            t.Errorf("required state %s missing", s)
        }
    }
}
```

- [ ] **Step 9-17: 复用 Task 6 模板创建剩余文件（service/enter.go、api/issue.go、api/enter.go、router/issue.go、router/enter.go、initialize/init.go、plugin.go、doc.go），并修改 pmocker_register.go 追加导入**

api/issue.go 提供 Create/Delete/Update/Find/List/Assign/Resolve/Verify/Close 9 个处理器；plugin.go 包名 `pmocker_issue`。

- [ ] **Step 18: 编译并运行测试**

Run: `cd gva/server && go build ./... && go test ./plugin/pmocker_issue/... -v`
Expected: 编译通过，问题状态测试通过

- [ ] **Step 19: 提交**

```bash
git add gva/server/plugin/pmocker_issue/ gva/server/plugin/pmocker_register.go
git commit -m "feat(issue): add pmocker_issue plugin with resolve workflow"
```

---

### Task 9: 组织级 EPS 插件（pmocker_eps）

**Files:**
- Create: `gva/server/plugin/pmocker_eps/` 同 Task 6 结构
- Test: `gva/server/plugin/pmocker_eps/service/eps_test.go`

**Interfaces:**
- Produces: entity_type=`eps_node`、`project`
- Produces: `eps.Service.BuildTree`、`AddNode`、`MoveNode` EPS 树操作（基于 PMEPSTree 表）

- [ ] **Step 1: 创建 pmocker/manifest.yaml**

```yaml
code: pmocker_eps
name: 组织级EPS
version: 1.0.0
description: 企业项目结构（Enterprise Project Structure），按层级组织项目和资源
dependencies:
  - pmocker_core
```

- [ ] **Step 2: 创建 pmocker/schema.yaml（project + eps_node 两实体）**

```yaml
entity_types:
  - entity_type: eps_node
    module: eps
    name: EPS节点
    icon: share
    icon_color: "#409EFF"
    fields:
      - {field_key: code, field_label: 节点编号, data_type: string}
      - {field_key: node_type, field_label: 节点类型, data_type: enum, options_json: '["portfolio","program","project_set","sub_project"]'}
      - {field_key: description, field_label: 描述, data_type: text}
      - {field_key: manager_id, field_label: 负责人, data_type: ref}
      - {field_key: planned_start, field_label: 计划开始, data_type: date}
      - {field_key: planned_finish, field_label: 计划完成, data_type: date}
      - {field_key: status, field_label: 状态, data_type: enum, options_json: '["planned","active","on_hold","closed"]'}
    states: [planned, active, on_hold, closed]

  - entity_type: project
    module: eps
    name: 项目
    icon: box
    icon_color: "#67C23A"
    fields:
      - {field_key: code, field_label: 项目编号, data_type: string}
      - {field_key: project_type, field_label: 项目类型, data_type: enum, options_json: '["construction","it","r_d","consulting"]'}
      - {field_key: priority, field_label: 优先级, data_type: enum, options_json: '["high","medium","low"]'}
      - {field_key: description, field_label: 项目描述, data_type: text}
      - {field_key: sponsor, field_label: 项目发起人, data_type: ref}
      - {field_key: pm_id, field_label: 项目经理, data_type: ref}
      - {field_key: planned_start, field_label: 计划开始, data_type: date}
      - {field_key: planned_finish, field_label: 计划完成, data_type: date}
      - {field_key: actual_start, field_label: 实际开始, data_type: date}
      - {field_key: actual_finish, field_label: 实际完成, data_type: date}
      - {field_key: status, field_label: 状态, data_type: enum, options_json: '["initiating","planning","executing","monitoring","closing","closed"]'}
    states: [initiating, planning, executing, monitoring, closing, closed]
```

- [ ] **Step 3: 创建 pmocker/seed.yaml（EPS 种子结构示例）**

```yaml
dictionaries:
  - type: pm_eps_node_type
    name: EPS节点类型
    details:
      - {label: 组合, value: portfolio}
      - {label: 项目集, value: program}
      - {label: 项目集合, value: project_set}
      - {label: 子项目, value: sub_project}
  - type: pm_project_type
    name: 项目类型
    details:
      - {label: 工程建设, value: construction}
      - {label: 信息化, value: it}
      - {label: 研发, value: r_d}
      - {label: 咨询服务, value: consulting}
  - type: pm_project_status
    name: 项目状态
    details:
      - {label: 启动, value: initiating}
      - {label: 规划, value: planning}
      - {label: 执行, value: executing}
      - {label: 监控, value: monitoring}
      - {label: 收尾, value: closing}
      - {label: 已关闭, value: closed}
eps:
  - parent_path: ""
    name: 默认组合
    type: portfolio
```

- [ ] **Step 4: 创建 pmocker/menu.yaml**

```yaml
menus:
  - path: /pmocker/eps
    name: pmockerEPS
    component: view/routerHolder
    sort: 5
    title: 组织级EPS
    icon: share
  - path: tree
    name: pmockerEPSTree
    parent_name: pmockerEPS
    component: plugin/pmocker_eps/view/tree
    sort: 1
    title: EPS树
    icon: tree
  - path: projects
    name: pmockerEPSProjects
    parent_name: pmockerEPS
    component: plugin/pmocker_eps/view/projects
    sort: 2
    title: 项目列表
    icon: list
```

- [ ] **Step 5: 创建 pmocker/api.yaml**

```yaml
apis:
  - {path: /pmocker/eps/createNode, description: 创建EPS节点, api_group: EPS, method: POST}
  - {path: /pmocker/eps/listNodes, description: 节点列表, api_group: EPS, method: GET}
  - {path: /pmocker/eps/getTree, description: 获取EPS树, api_group: EPS, method: GET}
  - {path: /pmocker/eps/moveNode, description: 移动节点, api_group: EPS, method: POST}
  - {path: /pmocker/eps/createProject, description: 创建项目, api_group: EPS, method: POST}
  - {path: /pmocker/eps/listProjects, description: 项目列表, api_group: EPS, method: GET}
```

- [ ] **Step 6: 创建 service/eps.go（EPS 树算法）**

```go
package eps

import (
    "context"
    "fmt"
    "strings"

    "github.com/flipped-aurora/gin-vue-admin/server/global"
    "github.com/flipped-aurora/gin-vue-admin/server/model/pmocker"
    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    pmservice "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
)

type Service struct{}
var EPSService = new(Service)

// EPSNode EPS 节点 DTO
type EPSNode struct {
    ID         uint    `json:"id"`
    ParentID   *uint   `json:"parentId"`
    Name       string  `json:"name"`
    Path       string  `json:"path"`
    Level      int     `json:"level"`
    NodeType   string  `json:"nodeType"`
    ProjectID  *uint   `json:"projectId"`
    Children   []EPSNode `json:"children,omitempty"`
}

// CreateNode 创建 EPS 节点（自动维护物化路径）
func (s *Service) CreateNode(ctx context.Context, parentID *uint, name, nodeType string) (uint, error) {
    var path string
    var level int = 1
    if parentID != nil {
        var parent pmocker.PMEPSTree
        if err := global.GVA_DB.WithContext(ctx).First(&parent, *parentID).Error; err != nil {
            return 0, fmt.Errorf("parent not found: %w", err)
        }
        var siblingCount int64
        global.GVA_DB.WithContext(ctx).Model(&pmocker.PMEPSTree{}).
            Where("parent_id = ?", *parentID).Count(&siblingCount)
        path = fmt.Sprintf("%s.%d", parent.Path, siblingCount+1)
        level = parent.Level + 1
    } else {
        var rootCount int64
        global.GVA_DB.WithContext(ctx).Model(&pmocker.PMEPSTree{}).
            Where("parent_id IS NULL").Count(&rootCount)
        path = fmt.Sprintf("%d", rootCount+1)
    }
    node := pmocker.PMEPSTree{
        ParentID:    parentID,
        Path:        path,
        ProjectType: nodeType,
        Name:        name,
    }
    _ = level
    if err := global.GVA_DB.WithContext(ctx).Create(&node).Error; err != nil {
        return 0, err
    }
    return node.ID, nil
}

// GetTree 获取完整 EPS 树（递归构建）
func (s *Service) GetTree(ctx context.Context) ([]EPSNode, error) {
    var nodes []pmocker.PMEPSTree
    if err := global.GVA_DB.WithContext(ctx).Order("path").Find(&nodes).Error; err != nil {
        return nil, err
    }
    return buildEPSTree(nodes), nil
}

// buildEPSTree 从扁平列表构建嵌套树（基于物化路径）
func buildEPSTree(nodes []pmocker.PMEPSTree) []EPSNode {
    nodeMap := make(map[uint]*EPSNode, len(nodes))
    roots := []EPSNode{}
    for i, n := range nodes {
        node := &EPSNode{
            ID:        n.ID,
            ParentID:  n.ParentID,
            Name:      n.Name,
            Path:      n.Path,
            Level:     strings.Count(n.Path, ".") + 1,
            NodeType:  n.ProjectType,
            Children:  []EPSNode{},
        }
        nodeMap[n.ID] = node
        _ = i
        if n.ParentID == nil {
            roots = append(roots, *node)
        }
    }
    for _, n := range nodes {
        if n.ParentID != nil {
            if parent, ok := nodeMap[*n.ParentID]; ok {
                parent.Children = append(parent.Children, *nodeMap[n.ID])
            }
        }
    }
    return roots
}

// MoveNode 移动节点（重新计算路径）
func (s *Service) MoveNode(ctx context.Context, nodeID uint, newParentID *uint) error {
    var node pmocker.PMEPSTree
    if err := global.GVA_DB.WithContext(ctx).First(&node, nodeID).Error; err != nil {
        return err
    }
    var newPath string
    if newParentID != nil {
        var parent pmocker.PMEPSTree
        if err := global.GVA_DB.WithContext(ctx).First(&parent, *newParentID).Error; err != nil {
            return err
        }
        if strings.HasPrefix(parent.Path, node.Path) {
            return fmt.Errorf("cannot move node to its descendant")
        }
        var siblingCount int64
        global.GVA_DB.WithContext(ctx).Model(&pmocker.PMEPSTree{}).
            Where("parent_id = ?", *newParentID).Count(&siblingCount)
        newPath = fmt.Sprintf("%s.%d", parent.Path, siblingCount+1)
    } else {
        var rootCount int64
        global.GVA_DB.WithContext(ctx).Model(&pmocker.PMEPSTree{}).
            Where("parent_id IS NULL").Count(&rootCount)
        newPath = fmt.Sprintf("%d", rootCount+1)
    }
    return global.GVA_DB.WithContext(ctx).Model(&node).Update("parent_id", newParentID).Update("path", newPath).Error
}

// CreateProject 创建项目实体
func (s *Service) CreateProject(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    return pmservice.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID: projectID, EntityType: "project", Title: title, Status: "initiating", CreatedBy: creatorID, Attrs: attrs,
    })
}

// ListProjects 列出项目
func (s *Service) ListProjects(ctx context.Context, epsNodeID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmservice.ServiceGroupApp.ListEntities(ctx, epsNodeID, "project", offset, limit)
}
```

- [ ] **Step 7: 创建 service/eps_test.go**

```go
package eps

import (
    "strings"
    "testing"
)

func TestPathLevel(t *testing.T) {
    cases := []struct{ path string; want int }{
        {"1", 1},
        {"1.2", 2},
        {"1.2.3", 3},
    }
    for _, c := range cases {
        if got := strings.Count(c.path, ".") + 1; got != c.want {
            t.Errorf("level(%s) = %d, want %d", c.path, got, c.want)
        }
    }
}

func TestBuildEPSTreeEmpty(t *testing.T) {
    // 空节点列表应返回空树
    roots := buildEPSTree(nil)
    if len(roots) != 0 {
        t.Errorf("expected empty tree, got %d roots", len(roots))
    }
}
```

- [ ] **Step 8-17: 复用 Task 6 模板创建剩余文件**

plugin.go 包名 `pmocker_eps`；api/eps.go 提供 CreateNode/ListNodes/GetTree/MoveNode/CreateProject/ListProjects 处理器。

- [ ] **Step 18: 编译并运行测试**

Run: `cd gva/server && go build ./... && go test ./plugin/pmocker_eps/... -v`
Expected: 编译通过，EPS 路径测试通过

- [ ] **Step 19: 提交**

```bash
git add gva/server/plugin/pmocker_eps/ gva/server/plugin/pmocker_register.go
git commit -m "feat(eps): add pmocker_eps plugin with EPS tree operations"
```

---

### Task 10: 交付物管理插件（pmocker_deliverable）

**Files:**
- Create: `gva/server/plugin/pmocker_deliverable/` 同 Task 6 结构
- Test: `gva/server/plugin/pmocker_deliverable/service/deliverable_test.go`

**Interfaces:**
- Produces: entity_type=`deliverable`、workflow=`deliverable_review`
- Produces: `deliverable.Service.Submit(ctx, id)` 交付物提交评审
- Produces: `deliverable.DeliverableProgress(ctx, projectID)` 交付物完成度统计（返回 `ProgressResult{Total, Delivered, Rate}`）

**行业最佳实践参考：** 禅道文档库（版本+评审状态）、MS Project 交付物（关联里程碑）、华天PLM 文档管理（受控归档）、青翼PLM 交付物（质量等级与归档）。

- [ ] **Step 1: 创建 pmocker/manifest.yaml**

```yaml
code: pmocker_deliverable
name: 交付物管理
version: 1.0.0
description: 项目交付物管理，含提交、评审、批准、交付、归档全生命周期
dependencies:
  - pmocker_core
```

- [ ] **Step 2: 创建 pmocker/schema.yaml**

```yaml
entity_type: deliverable
module: deliverable
name: 交付物
icon: document
icon_color: "#409EFF"
fields:
  - {field_key: code, field_label: 交付物编号, data_type: string}
  - {field_key: category, field_label: 类别, data_type: enum, options_json: '["design","code","document","test_report","user_manual","deployment_package","other"]'}
  - {field_key: version, field_label: 版本, data_type: string, default_value: "v1.0"}
  - {field_key: quality_level, field_label: 质量等级, data_type: enum, options_json: '["A","B","C","D"]', default_value: B}
  - {field_key: file_ref, field_label: 文件引用, data_type: string}
  - {field_key: owner_id, field_label: 责任人, data_type: ref}
  - {field_key: reviewer_id, field_label: 评审人, data_type: ref}
  - {field_key: approver_id, field_label: 批准人, data_type: ref}
  - {field_key: related_task_id, field_label: 关联任务, data_type: ref}
  - {field_key: milestone_id, field_label: 关联里程碑, data_type: ref}
  - {field_key: planned_date, field_label: 计划交付日期, data_type: date}
  - {field_key: actual_date, field_label: 实际交付日期, data_type: date}
  - {field_key: review_comment, field_label: 评审意见, data_type: text}
  - {field_key: archive_no, field_label: 归档编号, data_type: string}
states: [planned, in_progress, submitted, reviewed, approved, rejected, delivered, archived]
workflows: [deliverable_review]
```

- [ ] **Step 3: 创建 pmocker/seed.yaml**

```yaml
dictionaries:
  - type: pm_deliverable_category
    name: 交付物类别
    details:
      - {label: 设计文档, value: design}
      - {label: 代码, value: code}
      - {label: 文档, value: document}
      - {label: 测试报告, value: test_report}
      - {label: 用户手册, value: user_manual}
      - {label: 部署包, value: deployment_package}
      - {label: 其他, value: other}
  - type: pm_deliverable_quality
    name: 交付物质量等级
    details:
      - {label: 优秀, value: A, extend: green}
      - {label: 良好, value: B, extend: blue}
      - {label: 合格, value: C, extend: yellow}
      - {label: 不合格, value: D, extend: red}
```

- [ ] **Step 4: 创建 pmocker/menu.yaml**

```yaml
menus:
  - path: /pmocker/deliverable
    name: pmockerDeliverable
    component: view/routerHolder
    sort: 70
    title: 交付物管理
    icon: document
  - path: list
    name: pmockerDeliverableList
    parent_name: pmockerDeliverable
    component: plugin/pmocker_deliverable/view/list
    sort: 1
    title: 交付物列表
    icon: list
  - path: board
    name: pmockerDeliverableBoard
    parent_name: pmockerDeliverable
    component: plugin/pmocker_deliverable/view/board
    sort: 2
    title: 看板视图
    icon: grid
```

- [ ] **Step 5: 创建 pmocker/api.yaml**

```yaml
apis:
  - {path: /pmocker/deliverable/create, description: 创建交付物, api_group: 交付物管理, method: POST}
  - {path: /pmocker/deliverable/delete, description: 删除交付物, api_group: 交付物管理, method: DELETE}
  - {path: /pmocker/deliverable/update, description: 更新交付物, api_group: 交付物管理, method: PUT}
  - {path: /pmocker/deliverable/find, description: 查询交付物, api_group: 交付物管理, method: GET}
  - {path: /pmocker/deliverable/list, description: 交付物列表, api_group: 交付物管理, method: GET}
  - {path: /pmocker/deliverable/submit, description: 提交评审, api_group: 交付物管理, method: POST}
  - {path: /pmocker/deliverable/review, description: 评审交付物, api_group: 交付物管理, method: POST}
  - {path: /pmocker/deliverable/approve, description: 批准交付物, api_group: 交付物管理, method: POST}
  - {path: /pmocker/deliverable/reject, description: 驳回交付物, api_group: 交付物管理, method: POST}
  - {path: /pmocker/deliverable/deliver, description: 交付交付物, api_group: 交付物管理, method: POST}
  - {path: /pmocker/deliverable/archive, description: 归档交付物, api_group: 交付物管理, method: POST}
  - {path: /pmocker/deliverable/progress, description: 交付物完成度, api_group: 交付物管理, method: GET}
```

- [ ] **Step 6: 创建 pmocker/workflows/deliverable_review.yaml**

```yaml
code: deliverable_review
name: 交付物评审流
entity_type: deliverable
trigger: manual
nodes:
  - {code: start, type: start}
  - {code: develop, type: task, assignee: "owner", sla_hours: 0}
  - {code: review, type: approval, approvers: "reviewer", quorum: 1}
  - {code: approve, type: approval, approvers: "approver", quorum: 1}
  - {code: deliver, type: auto}
  - {code: end, type: end}
transitions:
  - {from: start, to: develop, on: create, status: in_progress}
  - {from: develop, to: review, on: submit, status: submitted}
  - {from: review, to: approve, on: review_pass, status: reviewed}
  - {from: review, to: develop, on: review_fail, status: rejected}
  - {from: approve, to: deliver, on: approve, status: approved}
  - {from: approve, to: develop, on: reject, status: rejected}
  - {from: deliver, to: end, on: deliver, status: delivered}
```

- [ ] **Step 7: 创建 service/deliverable.go**

```go
package deliverable

import (
    "context"
    "fmt"

    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    pmservice "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
)

type Service struct{}
var DeliverableService = new(Service)

// ProgressResult 交付物完成度统计结果
type ProgressResult struct {
    Total     int64   `json:"total"`
    Delivered int64   `json:"delivered"`
    Archived  int64   `json:"archived"`
    Rate      float64 `json:"rate"` // 交付率 = delivered / total
}

func (s *Service) CreateDeliverable(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    return pmservice.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID: projectID, EntityType: "deliverable", Title: title, Status: "planned", CreatedBy: creatorID, Attrs: attrs,
    })
}

func (s *Service) GetDeliverable(ctx context.Context, id uint) (*eavtypes.Entity, error) {
    e, err := pmservice.ServiceGroupApp.GetEntity(ctx, id)
    if err != nil {
        return nil, err
    }
    if e.EntityType != "deliverable" {
        return nil, fmt.Errorf("entity %d is not a deliverable", id)
    }
    return e, nil
}

func (s *Service) ListDeliverables(ctx context.Context, projectID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "deliverable", offset, limit)
}

func (s *Service) UpdateDeliverable(ctx context.Context, e eavtypes.Entity) error {
    if e.EntityType != "deliverable" {
        return fmt.Errorf("not a deliverable")
    }
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, e)
}

func (s *Service) DeleteDeliverable(ctx context.Context, id uint) error {
    return pmservice.ServiceGroupApp.DeleteEntity(ctx, id)
}

// Submit 提交评审
func (s *Service) Submit(ctx context.Context, id uint) error {
    e, err := s.GetDeliverable(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "planned" && e.Status != "in_progress" && e.Status != "rejected" {
        return fmt.Errorf("deliverable %d in status %s cannot be submitted", id, e.Status)
    }
    e.Status = "submitted"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Review 评审（pass=true 通过，false 不通过）
func (s *Service) Review(ctx context.Context, id uint, pass bool, comment string) error {
    e, err := s.GetDeliverable(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "submitted" {
        return fmt.Errorf("deliverable %d not in submitted status", id)
    }
    if e.Attrs == nil {
        e.Attrs = map[string]interface{}{}
    }
    e.Attrs["review_comment"] = comment
    if pass {
        e.Status = "reviewed"
    } else {
        e.Status = "rejected"
    }
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Approve 批准交付物
func (s *Service) Approve(ctx context.Context, id uint) error {
    e, err := s.GetDeliverable(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "reviewed" {
        return fmt.Errorf("deliverable %d not in reviewed status", id)
    }
    e.Status = "approved"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Reject 驳回（退回开发）
func (s *Service) Reject(ctx context.Context, id uint, reason string) error {
    e, err := s.GetDeliverable(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "reviewed" && e.Status != "submitted" {
        return fmt.Errorf("deliverable %d cannot be rejected from %s", id, e.Status)
    }
    if e.Attrs == nil {
        e.Attrs = map[string]interface{}{}
    }
    e.Attrs["review_comment"] = reason
    e.Status = "rejected"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Deliver 交付（设置实际交付日期）
func (s *Service) Deliver(ctx context.Context, id uint, actualDate string) error {
    e, err := s.GetDeliverable(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "approved" {
        return fmt.Errorf("deliverable %d not approved yet", id)
    }
    if e.Attrs == nil {
        e.Attrs = map[string]interface{}{}
    }
    e.Attrs["actual_date"] = actualDate
    e.Status = "delivered"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Archive 归档（设置归档编号）
func (s *Service) Archive(ctx context.Context, id uint, archiveNo string) error {
    e, err := s.GetDeliverable(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "delivered" {
        return fmt.Errorf("deliverable %d not delivered yet", id)
    }
    if e.Attrs == nil {
        e.Attrs = map[string]interface{}{}
    }
    e.Attrs["archive_no"] = archiveNo
    e.Status = "archived"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// DeliverableProgress 计算项目交付物完成度
func (s *Service) DeliverableProgress(ctx context.Context, projectID uint) (ProgressResult, error) {
    entities, total, err := pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "deliverable", 0, 100000)
    if err != nil {
        return ProgressResult{}, err
    }
    var delivered, archived int64
    for _, e := range entities {
        if e.Status == "delivered" {
            delivered++
        } else if e.Status == "archived" {
            delivered++
            archived++
        }
    }
    rate := 0.0
    if total > 0 {
        rate = float64(delivered) / float64(total) * 100
    }
    return ProgressResult{
        Total:     total,
        Delivered: delivered,
        Archived:  archived,
        Rate:      rate,
    }, nil
}
```

- [ ] **Step 8: 创建 service/deliverable_test.go**

```go
package deliverable

import "testing"

func TestDeliverableStates(t *testing.T) {
    states := []string{"planned", "in_progress", "submitted", "reviewed", "approved", "rejected", "delivered", "archived"}
    stateSet := map[string]bool{}
    for _, s := range states {
        stateSet[s] = true
    }
    required := []string{"planned", "submitted", "reviewed", "approved", "rejected", "delivered", "archived"}
    for _, s := range required {
        if !stateSet[s] {
            t.Errorf("required state %s missing", s)
        }
    }
}

func TestProgressResult(t *testing.T) {
    // 5 个交付物，2 个已交付，1 个已归档
    p := ProgressResult{Total: 5, Delivered: 3, Archived: 1}
    p.Rate = float64(p.Delivered) / float64(p.Total) * 100
    if p.Rate != 60.0 {
        t.Errorf("rate = %v, want 60.0", p.Rate)
    }
}

func TestProgressEmpty(t *testing.T) {
    p := ProgressResult{}
    p.Rate = 0.0
    if p.Rate != 0 {
        t.Errorf("empty rate = %v, want 0", p.Rate)
    }
}
```

- [ ] **Step 9-17: 复用 Task 6 模板创建剩余文件（service/enter.go、api/deliverable.go、api/enter.go、router/deliverable.go、router/enter.go、initialize/init.go、plugin.go、doc.go），并修改 pmocker_register.go 追加导入**

api/deliverable.go 提供 Create/Delete/Update/Find/List/Submit/Review/Approve/Reject/Deliver/Archive/Progress 12 个处理器；plugin.go 包名 `pmocker_deliverable`，所有 `pmocker_cost` 字样替换为 `pmocker_deliverable`。

- [ ] **Step 18: 编译并运行测试**

Run: `cd gva/server && go build ./... && go test ./plugin/pmocker_deliverable/... -v`
Expected: 编译通过，3 个交付物测试通过

- [ ] **Step 19: 提交**

```bash
git add gva/server/plugin/pmocker_deliverable/ gva/server/plugin/pmocker_register.go
git commit -m "feat(deliverable): add pmocker_deliverable plugin with review workflow and progress stats"
```

---

### Task 11: 变更管理插件（pmocker_change）

**Files:**
- Create: `gva/server/plugin/pmocker_change/` 同 Task 6 结构
- Test: `gva/server/plugin/pmocker_change/service/change_test.go`

**Interfaces:**
- Produces: entity_type=`change_request`、workflow=`change_control`
- Produces: `change.Service.AnalyzeImpact(ctx, id, scope, schedule, cost, quality)` 变更影响分析
- Produces: `change.ImpactLevel` 常量（`ImpactLow`/`ImpactMedium`/`ImpactHigh`/`ImpactCritical`）

**行业最佳实践参考：** 禅道变更（变更分类+评审）、PMBOK 变更控制委员会（CCB）流程、华天PLM ECN 工程变更通知、青翼PLM 变更管理（影响范围评估）、MS Project 基线对比（变更前后基线）。

- [ ] **Step 1: 创建 pmocker/manifest.yaml**

```yaml
code: pmocker_change
name: 变更管理
version: 1.0.0
description: 项目变更管理，含变更申请、CCB 审批、影响分析、基线对比全流程
dependencies:
  - pmocker_core
```

- [ ] **Step 2: 创建 pmocker/schema.yaml**

```yaml
entity_type: change_request
module: change
name: 变更
icon: switch
icon_color: "#E6A23C"
fields:
  - {field_key: code, field_label: 变更编号, data_type: string}
  - {field_key: change_type, field_label: 变更类型, data_type: enum, options_json: '["scope","schedule","cost","quality","resource","requirement","other"]'}
  - {field_key: priority, field_label: 优先级, data_type: enum, options_json: '["urgent","high","medium","low"]', default_value: medium}
  - {field_key: originator_id, field_label: 提出人, data_type: ref}
  - {field_key: originator_date, field_label: 提出日期, data_type: date}
  - {field_key: reason, field_label: 变更原因, data_type: text}
  - {field_key: description, field_label: 变更描述, data_type: text}
  - {field_key: impact_scope, field_label: 范围影响(1-5), data_type: int, default_value: "1"}
  - {field_key: impact_schedule, field_label: 进度影响(天), data_type: int, default_value: "0"}
  - {field_key: impact_cost, field_label: 成本影响(元), data_type: decimal, default_value: "0"}
  - {field_key: impact_quality, field_label: 质量影响(1-5), data_type: int, default_value: "1"}
  - {field_key: impact_score, field_label: 综合影响分值, data_type: decimal}
  - {field_key: impact_level, field_label: 影响等级, data_type: enum, options_json: '["low","medium","high","critical"]'}
  - {field_key: affected_items, field_label: 受影响项, data_type: json}
  - {field_key: baseline_before, field_label: 变更前基线, data_type: ref}
  - {field_key: baseline_after, field_label: 变更后基线, data_type: ref}
  - {field_key: ccb_members, field_label: CCB成员, data_type: json}
  - {field_key: approved_by, field_label: 批准人, data_type: ref}
  - {field_key: approved_date, field_label: 批准日期, data_type: date}
  - {field_key: implemented_by, field_label: 实施人, data_type: ref}
  - {field_key: implemented_date, field_label: 实施完成日期, data_type: date}
  - {field_key: verified_by, field_label: 验证人, data_type: ref}
  - {field_key: verified_date, field_label: 验证日期, data_type: date}
  - {field_key: implementation_notes, field_label: 实施记录, data_type: text}
states: [draft, submitted, under_review, approved, rejected, implementing, implemented, verified, closed]
workflows: [change_control]
```

- [ ] **Step 3: 创建 pmocker/seed.yaml**

```yaml
dictionaries:
  - type: pm_change_type
    name: 变更类型
    details:
      - {label: 范围变更, value: scope}
      - {label: 进度变更, value: schedule}
      - {label: 成本变更, value: cost}
      - {label: 质量变更, value: quality}
      - {label: 资源变更, value: resource}
      - {label: 需求变更, value: requirement}
      - {label: 其他, value: other}
  - type: pm_change_priority
    name: 变更优先级
    details:
      - {label: 紧急, value: urgent, extend: red}
      - {label: 高, value: high, extend: orange}
      - {label: 中, value: medium, extend: yellow}
      - {label: 低, value: low, extend: green}
  - type: pm_change_impact_level
    name: 变更影响等级
    details:
      - {label: 轻微, value: low, extend: green}
      - {label: 一般, value: medium, extend: yellow}
      - {label: 较大, value: high, extend: orange}
      - {label: 重大, value: critical, extend: red}
```

- [ ] **Step 4: 创建 pmocker/menu.yaml**

```yaml
menus:
  - path: /pmocker/change
    name: pmockerChange
    component: view/routerHolder
    sort: 80
    title: 变更管理
    icon: switch
  - path: list
    name: pmockerChangeList
    parent_name: pmockerChange
    component: plugin/pmocker_change/view/list
    sort: 1
    title: 变更列表
    icon: list
  - path: board
    name: pmockerChangeBoard
    parent_name: pmockerChange
    component: plugin/pmocker_change/view/board
    sort: 2
    title: 看板视图
    icon: grid
```

- [ ] **Step 5: 创建 pmocker/api.yaml**

```yaml
apis:
  - {path: /pmocker/change/create, description: 创建变更, api_group: 变更管理, method: POST}
  - {path: /pmocker/change/delete, description: 删除变更, api_group: 变更管理, method: DELETE}
  - {path: /pmocker/change/update, description: 更新变更, api_group: 变更管理, method: PUT}
  - {path: /pmocker/change/find, description: 查询变更, api_group: 变更管理, method: GET}
  - {path: /pmocker/change/list, description: 变更列表, api_group: 变更管理, method: GET}
  - {path: /pmocker/change/submit, description: 提交评审, api_group: 变更管理, method: POST}
  - {path: /pmocker/change/analyze, description: 影响分析, api_group: 变更管理, method: POST}
  - {path: /pmocker/change/approve, description: 批准变更, api_group: 变更管理, method: POST}
  - {path: /pmocker/change/reject, description: 驳回变更, api_group: 变更管理, method: POST}
  - {path: /pmocker/change/implement, description: 实施变更, api_group: 变更管理, method: POST}
  - {path: /pmocker/change/verify, description: 验证变更, api_group: 变更管理, method: POST}
  - {path: /pmocker/change/close, description: 关闭变更, api_group: 变更管理, method: POST}
```

- [ ] **Step 6: 创建 pmocker/workflows/change_control.yaml**

```yaml
code: change_control
name: 变更控制流
entity_type: change_request
trigger: manual
nodes:
  - {code: start, type: start}
  - {code: submit, type: task, assignee: "originator", sla_hours: 0}
  - {code: analyze, type: auto}
  - {code: ccb_review, type: approval, approvers: "ccb_members", quorum: 2}
  - {code: implement, type: task, assignee: "implementer", sla_hours: 0}
  - {code: verify, type: approval, approvers: "verifier", quorum: 1}
  - {code: end, type: end}
transitions:
  - {from: start, to: submit, on: create, status: draft}
  - {from: submit, to: analyze, on: submit, status: submitted}
  - {from: analyze, to: ccb_review, on: analyzed, status: under_review}
  - {from: ccb_review, to: implement, on: approve, status: approved}
  - {from: ccb_review, to: end, on: reject, status: rejected}
  - {from: implement, to: verify, on: implemented, status: implemented}
  - {from: verify, to: end, on: verify_pass, status: closed}
  - {from: verify, to: implement, on: verify_fail, status: implementing}
```

- [ ] **Step 7: 创建 service/impact.go（影响分析算法）**

```go
package change

import "math"

// ImpactLevel 影响等级
type ImpactLevel string

const (
    ImpactLow      ImpactLevel = "low"
    ImpactMedium   ImpactLevel = "medium"
    ImpactHigh     ImpactLevel = "high"
    ImpactCritical ImpactLevel = "critical"
)

// ImpactInput 影响分析输入
type ImpactInput struct {
    Scope    int     // 范围影响 1-5
    Schedule int     // 进度影响（天）
    Cost     float64 // 成本影响（元）
    Quality  int     // 质量影响 1-5
}

// ImpactResult 影响分析结果
type ImpactResult struct {
    ScopeScore    float64     `json:"scope_score"`
    ScheduleScore float64     `json:"schedule_score"`
    CostScore     float64     `json:"cost_score"`
    QualityScore  float64     `json:"quality_score"`
    TotalScore    float64     `json:"total_score"` // 0-5
    Level         ImpactLevel `json:"level"`
}

// AnalyzeImpact 变更影响分析
// 算法：将四维影响归一化到 0-5，取均值得到综合分值，再映射到等级
//   - scope_score = clamp(scope, 1, 5)
//   - schedule_score = min(schedule/10, 5)  每 10 天 1 分，上限 5
//   - cost_score = min(cost/10000, 5)       每 1 万元 1 分，上限 5
//   - quality_score = clamp(quality, 1, 5)
//   - total = (scope + schedule + cost + quality) / 4
//   - level: >=4 critical, >=3 high, >=2 medium, else low
func AnalyzeImpact(in ImpactInput) ImpactResult {
    scopeScore := clampScore(float64(in.Scope))
    scheduleScore := math.Min(float64(in.Schedule)/10.0, 5.0)
    if scheduleScore < 0 {
        scheduleScore = 0
    }
    costScore := math.Min(in.Cost/10000.0, 5.0)
    if costScore < 0 {
        costScore = 0
    }
    qualityScore := clampScore(float64(in.Quality))

    total := (scopeScore + scheduleScore + costScore + qualityScore) / 4.0
    return ImpactResult{
        ScopeScore:    scopeScore,
        ScheduleScore: scheduleScore,
        CostScore:     costScore,
        QualityScore:  qualityScore,
        TotalScore:    total,
        Level:         scoreToLevel(total),
    }
}

// IsCritical 是否重大变更（需 CCB 审批）
func (r ImpactResult) IsCritical() bool {
    return r.Level == ImpactCritical
}

func clampScore(v float64) float64 {
    if v < 1 {
        return 1
    }
    if v > 5 {
        return 5
    }
    return v
}

func scoreToLevel(score float64) ImpactLevel {
    switch {
    case score >= 4.0:
        return ImpactCritical
    case score >= 3.0:
        return ImpactHigh
    case score >= 2.0:
        return ImpactMedium
    default:
        return ImpactLow
    }
}
```

- [ ] **Step 8: 创建 service/change.go**

```go
package change

import (
    "context"
    "fmt"

    eavtypes "github.com/pmocker-io/pmocker/pkg/pmocker/eav"
    pmservice "github.com/flipped-aurora/gin-vue-admin/server/service/pmocker"
)

type Service struct{}
var ChangeService = new(Service)

func (s *Service) CreateChange(ctx context.Context, projectID uint, title string, attrs map[string]interface{}, creatorID uint) (uint, error) {
    return pmservice.ServiceGroupApp.CreateEntity(ctx, eavtypes.Entity{
        ProjectID: projectID, EntityType: "change_request", Title: title, Status: "draft", CreatedBy: creatorID, Attrs: attrs,
    })
}

func (s *Service) GetChange(ctx context.Context, id uint) (*eavtypes.Entity, error) {
    e, err := pmservice.ServiceGroupApp.GetEntity(ctx, id)
    if err != nil {
        return nil, err
    }
    if e.EntityType != "change_request" {
        return nil, fmt.Errorf("entity %d is not a change_request", id)
    }
    return e, nil
}

func (s *Service) ListChanges(ctx context.Context, projectID uint, offset, limit int) ([]eavtypes.Entity, int64, error) {
    return pmservice.ServiceGroupApp.ListEntities(ctx, projectID, "change_request", offset, limit)
}

func (s *Service) UpdateChange(ctx context.Context, e eavtypes.Entity) error {
    if e.EntityType != "change_request" {
        return fmt.Errorf("not a change_request")
    }
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, e)
}

func (s *Service) DeleteChange(ctx context.Context, id uint) error {
    return pmservice.ServiceGroupApp.DeleteEntity(ctx, id)
}

// Submit 提交变更申请
func (s *Service) Submit(ctx context.Context, id uint) error {
    e, err := s.GetChange(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "draft" {
        return fmt.Errorf("change %d not in draft status", id)
    }
    e.Status = "submitted"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// AnalyzeImpact 对变更进行影响分析并持久化结果
func (s *Service) AnalyzeImpact(ctx context.Context, id uint, in ImpactInput) (ImpactResult, error) {
    e, err := s.GetChange(ctx, id)
    if err != nil {
        return ImpactResult{}, err
    }
    if e.Status != "submitted" && e.Status != "under_review" {
        return ImpactResult{}, fmt.Errorf("change %d not submitted yet", id)
    }
    result := AnalyzeImpact(in)
    if e.Attrs == nil {
        e.Attrs = map[string]interface{}{}
    }
    e.Attrs["impact_scope"] = in.Scope
    e.Attrs["impact_schedule"] = in.Schedule
    e.Attrs["impact_cost"] = in.Cost
    e.Attrs["impact_quality"] = in.Quality
    e.Attrs["impact_score"] = result.TotalScore
    e.Attrs["impact_level"] = string(result.Level)
    e.Status = "under_review"
    if err := pmservice.ServiceGroupApp.UpdateEntity(ctx, *e); err != nil {
        return ImpactResult{}, err
    }
    return result, nil
}

// Approve 批准变更（CCB 通过）
func (s *Service) Approve(ctx context.Context, id uint, approverID uint, approveDate string) error {
    e, err := s.GetChange(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "under_review" {
        return fmt.Errorf("change %d not under review", id)
    }
    if e.Attrs == nil {
        e.Attrs = map[string]interface{}{}
    }
    e.Attrs["approved_by"] = approverID
    e.Attrs["approved_date"] = approveDate
    e.Status = "approved"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Reject 驳回变更
func (s *Service) Reject(ctx context.Context, id uint) error {
    e, err := s.GetChange(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "under_review" {
        return fmt.Errorf("change %d not under review", id)
    }
    e.Status = "rejected"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Implement 实施变更
func (s *Service) Implement(ctx context.Context, id uint, implementerID uint, implDate, notes string) error {
    e, err := s.GetChange(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "approved" {
        return fmt.Errorf("change %d not approved", id)
    }
    if e.Attrs == nil {
        e.Attrs = map[string]interface{}{}
    }
    e.Attrs["implemented_by"] = implementerID
    e.Attrs["implemented_date"] = implDate
    e.Attrs["implementation_notes"] = notes
    e.Status = "implemented"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Verify 验证变更
func (s *Service) Verify(ctx context.Context, id uint, verifierID uint, verifyDate string, pass bool) error {
    e, err := s.GetChange(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "implemented" && e.Status != "implementing" {
        return fmt.Errorf("change %d not implemented", id)
    }
    if e.Attrs == nil {
        e.Attrs = map[string]interface{}{}
    }
    e.Attrs["verified_by"] = verifierID
    e.Attrs["verified_date"] = verifyDate
    if pass {
        e.Status = "verified"
    } else {
        e.Status = "implementing" // 退回实施
    }
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}

// Close 关闭变更
func (s *Service) Close(ctx context.Context, id uint) error {
    e, err := s.GetChange(ctx, id)
    if err != nil {
        return err
    }
    if e.Status != "verified" {
        return fmt.Errorf("change %d not verified yet", id)
    }
    e.Status = "closed"
    return pmservice.ServiceGroupApp.UpdateEntity(ctx, *e)
}
```

- [ ] **Step 9: 创建 service/change_test.go**

```go
package change

import "testing"

func TestAnalyzeImpact(t *testing.T) {
    cases := []struct {
        name string
        in   ImpactInput
        want ImpactLevel
    }{
        {"轻微变更", ImpactInput{Scope: 1, Schedule: 1, Cost: 100, Quality: 1}, ImpactLow},
        {"一般变更", ImpactInput{Scope: 2, Schedule: 10, Cost: 5000, Quality: 2}, ImpactMedium},
        {"较大变更", ImpactInput{Scope: 4, Schedule: 30, Cost: 30000, Quality: 3}, ImpactHigh},
        {"重大变更", ImpactInput{Scope: 5, Schedule: 60, Cost: 80000, Quality: 5}, ImpactCritical},
    }
    for _, c := range cases {
        t.Run(c.name, func(t *testing.T) {
            r := AnalyzeImpact(c.in)
            if r.Level != c.want {
                t.Errorf("AnalyzeImpact(%+v) level = %s, want %s (score=%.2f)", c.in, r.Level, c.want, r.TotalScore)
            }
        })
    }
}

func TestImpactClamp(t *testing.T) {
    // 超出范围应被截断到 1-5
    r := AnalyzeImpact(ImpactInput{Scope: 10, Schedule: 0, Cost: 0, Quality: 10})
    if r.ScopeScore != 5 {
        t.Errorf("scope score = %v, want 5", r.ScopeScore)
    }
    if r.QualityScore != 5 {
        t.Errorf("quality score = %v, want 5", r.QualityScore)
    }
    if r.ScheduleScore != 0 {
        t.Errorf("schedule score = %v, want 0", r.ScheduleScore)
    }
}

func TestIsCritical(t *testing.T) {
    r := AnalyzeImpact(ImpactInput{Scope: 5, Schedule: 60, Cost: 80000, Quality: 5})
    if !r.IsCritical() {
        t.Errorf("expected critical change, got %s", r.Level)
    }
    r2 := AnalyzeImpact(ImpactInput{Scope: 1, Schedule: 1, Cost: 100, Quality: 1})
    if r2.IsCritical() {
        t.Errorf("expected non-critical change, got %s", r2.Level)
    }
}

func TestChangeStates(t *testing.T) {
    states := []string{"draft", "submitted", "under_review", "approved", "rejected", "implementing", "implemented", "verified", "closed"}
    stateSet := map[string]bool{}
    for _, s := range states {
        stateSet[s] = true
    }
    required := []string{"draft", "submitted", "under_review", "approved", "rejected", "implemented", "closed"}
    for _, s := range required {
        if !stateSet[s] {
            t.Errorf("required state %s missing", s)
        }
    }
}
```

- [ ] **Step 10-18: 复用 Task 6 模板创建剩余文件（service/enter.go、api/change.go、api/enter.go、router/change.go、router/enter.go、initialize/init.go、plugin.go、doc.go），并修改 pmocker_register.go 追加导入**

api/change.go 提供 Create/Delete/Update/Find/List/Submit/AnalyzeImpact/Approve/Reject/Implement/Verify/Close 12 个处理器；plugin.go 包名 `pmocker_change`，所有 `pmocker_cost` 字样替换为 `pmocker_change`。

- [ ] **Step 19: 编译并运行测试**

Run: `cd gva/server && go build ./... && go test ./plugin/pmocker_change/... -v`
Expected: 编译通过，4 个变更测试通过（含影响分析算法）

- [ ] **Step 20: 提交**

```bash
git add gva/server/plugin/pmocker_change/ gva/server/plugin/pmocker_register.go
git commit -m "feat(change): add pmocker_change plugin with CCB workflow and impact analysis"
```

---

## Self-Review

### 1. Spec coverage（需求规格覆盖）

逐项对照需求文档 M3 阶段范围（9 个项目管理模块 + 公共支撑）：

| 需求文档条目 | 对应 Task | 状态 |
|---|---|---|
| 公共 YAML Loader（schema/seed/menu/api/workflow 加载） | Task 1 | 已覆盖 |
| pmocker_core 触发业务插件初始化 | Task 2 | 已覆盖 |
| 需求管理（pmocker_requirement） | Task 3 | 已覆盖 |
| 范围管理（pmocker_scope，WBS+基线） | Task 4 | 已覆盖 |
| 进度管理（pmocker_schedule，CPM 算法） | Task 5 | 已覆盖 |
| 成本管理（pmocker_cost，EVM 算法） | Task 6 | 已覆盖 |
| 风险管理（pmocker_risk，风险矩阵） | Task 7 | 已覆盖 |
| 问题管理（pmocker_issue，闭环工作流） | Task 8 | 已覆盖 |
| 组织级 EPS（pmocker_eps，EPS 树） | Task 9 | 已覆盖 |
| 交付物管理（pmocker_deliverable，评审+归档） | Task 10 | 已覆盖 |
| 变更管理（pmocker_change，CCB+影响分析） | Task 11 | 已覆盖 |

**约束项核对：**

- [x] gva 插件目录结构 `gva/server/plugin/pmocker_<mod>/` + `pmocker/` 子目录 — 所有 Task 3-11 遵循
- [x] PMockerPlugin 接口 + `InitPMocker(ctx)` 钩子 — Task 2 定义并在 Task 3-11 中实现
- [x] 自定义算法（CPM/EVM/风险矩阵/影响分析）放在 `service/` 目录 — Task 5/6/7/11 分别实现
- [x] 元数据 `schema/seed/menu/api/workflows` 位于插件 `pmocker/` 子目录 — 所有业务 Task 遵循
- [x] .pmi 镜像 4 类层（plugins/schema 聚合、theme、assets）— manifest.yaml 由打包工具读取，运行期不灌表（Task 3 注释说明）
- [x] CLI v1.0 排除 pull/push/build — 不在本计划范围（M4 阶段）

**无遗漏。**

### 2. Placeholder scan（占位符扫描）

扫描计划中的红旗模式：

- `TBD` / `TODO` / `implement later` / `fill in details` — **无**
- `Add appropriate error handling` / `add validation` / `handle edge cases` — **无**，所有错误处理已显式写出
- `Write tests for the above`（无实际测试代码）— **无**，每个 Task 都包含完整测试代码
- `Similar to Task N`（未重复代码）— Task 8/9/10/11 的"复用 Task 6 模板"步骤明确列出了需要创建的文件清单和替换规则（包名、处理器列表、导入路径），未省略关键信息
- 未定义的类型/函数引用 — 已检查，所有跨 Task 引用的类型（如 `eavtypes.Entity`、`pmservice.ServiceGroupApp`、`PMockerPlugin` 接口）均在 Task 1/2 或 M2 中定义

**无占位符。**

### 3. Type consistency（类型一致性检查）

跨 Task 类型/签名核对：

| 类型/函数 | 定义位置 | 使用位置 | 一致性 |
|---|---|---|---|
| `PMockerPlugin.InitPMocker(ctx context.Context) error` | Task 2 | Task 3-11 plugin.go | 一致 |
| `loader.Loader.LoadSchema/LoadSeed/LoadMenu/LoadAPI/LoadWorkflowDir` | Task 1 | Task 3-11 InitPMocker | 一致 |
| `eavtypes.Entity{ProjectID, EntityType, Title, Status, CreatedBy, Attrs}` | M2 已存在 | Task 3-11 service | 一致 |
| `pmservice.ServiceGroupApp.CreateEntity/GetEntity/ListEntities/UpdateEntity/DeleteEntity` | M2 已存在 | Task 3-11 service | 一致 |
| `workflow.Engine.LoadDefinition` | M2 已存在 | Task 1 LoadWorkflow 间接调用 | 一致 |
| `ProgressResult{Total, Delivered, Archived, Rate}` | Task 10 | Task 10 测试 | 一致 |
| `ImpactInput{Scope, Schedule, Cost, Quality}` | Task 11 | Task 11 service/test | 一致 |
| `ImpactLevel` 常量（low/medium/high/critical） | Task 11 | Task 11 schema.yaml 的 impact_level 枚举 | 一致 |
| 各插件 `Service` 单例变量名 | 各 Task | api 包引用 | 命名规则一致（`XxxService = new(Service)`） |

**类型一致性检查通过。**

### 4. 顺序依赖检查

任务执行顺序合理：

1. **Task 1（Loader）→ Task 2（core 钩子）** 必须先完成，后续插件依赖
2. **Task 3-11（业务插件）** 互相独立，可任意顺序执行，但均依赖 Task 1/2
3. 每个插件 Task 内部步骤：先创建元数据 YAML → service → 测试 → 复用模板创建剩余文件 → 编译测试 → 提交，顺序自洽

---

## Execution Handoff

计划已完成并保存至 `docs/superpowers/plans/2026-07-31-m3-plugins.md`。两种执行方式：

**1. Subagent-Driven（推荐）** — 每个 Task 派发独立子代理执行，Task 间进行两阶段审查，迭代快、上下文隔离

**2. Inline Execution** — 在当前会话内按 executing-plans 批量执行，带检查点审查

**请选择执行方式？**