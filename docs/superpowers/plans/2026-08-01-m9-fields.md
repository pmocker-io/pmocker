# M9 字段与工作流实用性增强 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 9 大模块从「字段太简单、不具备实用性」提升到 PMBOK 第六版最佳实践水平。当前 9 模块共 123 字段、6/9 工作流为三节点极简流、auto 自动节点 0/9 缺失；M9 目标补全约 90 字段 + 重构工作流，使字段总数达到 ~215、EVM 闭环可算、WBS 结构化、依赖类型完整、状态机与工作流对齐。

**Architecture:** 保持 EAV 数据模型不变。所有改动集中在三类元数据文件：`schema.yaml`（字段定义）、`seed.yaml`（字典/种子）、`workflows/*.yaml`（声明式工作流）。插件 `InitPMocker()` 钩子启动时灌入 EAV 元表，无需改业务代码。前端表单适配留待验证后按需调整（M9 聚焦元数据层）。

**Tech Stack:** YAML（schema/seed/workflow）、Go 1.22+（EAV 加载器）、gin-vue-admin v3.0.0、SQLite

## Global Constraints

- 文件路径统一：`gva/server/plugin/pmocker_<mod>/pmocker/{schema,seed}.yaml` + `workflows/*.yaml`
- 字段 `field_key` 用 snake_case，`data_type` 取值：`string|text|int|decimal|date|datetime|bool|enum|ref|json`
- `enum` 字段必须在 `options` 列全可选值；`ref` 字段必须指明 `ref_to` 实体类型
- 基线字段统一命名：`baseline_version`(string) + `baseline_date`(date) + `baseline_by`(ref→sys_users)
- 新增字典统一放对应模块 `seed.yaml` 的 `dictionaries` 节，`dict_code` 用 `pm_<mod>_<name>` 前缀
- 工作流节点类型必须用：`start|approval|auto|task|end`；`auto` 节点必须有 `handler` 指向内核函数
- schema 的 `states` 必须与 workflows 的流转状态一一对应，禁止「死状态」（schema 定义了但工作流不可达）
- 幂等：`InitPMocker()` 钩子用 `FirstOrCreate`，重复初始化不产生重复记录
- 改动后必须重建默认镜像 `pmbok6-hybrid.pmi` 才能在实例中生效

## 优先级总览

| 批次 | Task | 模块 | 当前字段 | 新增字段 | 优先级 | 薄弱原因 |
|------|------|------|------|------|------|------|
| P0 | T1 | cost 成本 | 9 | +16 | 极高 | EVM 体系完全残缺（有 PV/EV/AC 无派生指标） |
| P0 | T2 | scope 范围 | 9 | +12 | 极高 | WBS 贫血、无 RACI、无基线 |
| P1 | T3 | requirement 需求 | 9 | +12 | 高 | 无 MoSCoW、无追踪、无状态字段 |
| P1 | T4 | schedule 进度 | 15 | +11 | 高 | 依赖类型 FS/FF/SS/SF 缺失（致命） |
| P2 | T5 | eps 组织 | 13 | +11 | 中 | 无阶段-门、无项目集/组合、无 RAG |
| P2 | T6 | risk 风险 | 13 | +9 | 中 | 缺正面风险策略、EMV、储备金额 |
| P3 | T7 | issue 问题 | 12 | +7 | 中低 | 缺关闭条件、关联风险、验证人 |
| P3 | T8 | deliverable 交付物 | 19 | +5 | 中低 | 缺配置管理、评审状态独立化 |
| P4 | T9 | change 变更 | 24 | +6 | 低 | 最完整，仅缺 PMBOK 四分类与 CCB 细节 |
| — | T10 | 工作流重构 | — | — | 高 | auto 节点 0/9、6/9 极简流、死状态 |
| — | T11 | 重建镜像 + 验证 | — | — | — | 闭环 |

**预期结果**：字段总数 123 → ~215；工作流 auto 节点引入、死状态消除、状态机与工作流对齐。

---

### Task 1: cost 成本模块 EVM 字段补全（P0）

**Files:**
- Modify: `gva/server/plugin/pmocker_cost/pmocker/schema.yaml`
- Modify: `gva/server/plugin/pmocker_cost/pmocker/seed.yaml`
- Modify: `gva/server/plugin/pmocker_cost/pmocker/workflows/*.yaml`

**Interfaces:**
- Produces: cost_item 实体新增 16 字段，EVM 指标闭环可算（CV/SV/CPI/SPI/EAC/ETC/TCPI/VAC），新增估算方法字典与储备类型字典

**新增字段清单：**

| field_key | data_type | 用途 |
|------|------|------|
| bac | decimal | 完工预算 BAC，EVM 基准 |
| cv | decimal | 成本偏差 CV=EV−AC（可 auto 计算） |
| sv | decimal | 进度偏差 SV=EV−PV |
| cpi | decimal | 成本绩效指数 CPI=EV/AC |
| spi | decimal | 进度绩效指数 SPI=EV/PV |
| eac | decimal | 完工估算 EAC |
| etc | decimal | 完工尚需估算 ETC |
| vac | decimal | 完工偏差 VAC=BAC−EAC |
| tcpi | decimal | 完工尚需绩效指数 |
| estimation_method | enum | 估算方法：analogous/parametric/bottom_up/three_point |
| contingency_reserve | decimal | 应急储备（已知未知风险） |
| management_reserve | decimal | 管理储备（未知未知） |
| baseline_version | string | 成本基线版本（统一命名） |
| baseline_date | date | 基线日期 |
| actual_date | date | 实际发生日期 |
| variance_reason | text | 偏差原因分析 |

- [ ] **Step 1: 补充 schema.yaml 字段**
  在 cost_item 实体下新增上表 16 字段，`enum` 字段填 `options`，参照已有字段格式。
- [ ] **Step 2: 补充 seed.yaml 字典**
  新增 `pm_cost_estimation_method`（4 选项）、`pm_reserve_type`（contingency/management）字典。
- [ ] **Step 3: 重构 cost 工作流**
  当前 `cost_baseline` 仅 3 节点覆盖到 planned，补全 in_progress→closed/over_budget 流转；新增 `auto` 节点自动算 EVM 派生指标（handler: `pmocker.cost.evm_calc`）。
- [ ] **Step 4: 编译验证**
  Run: `cd gva/server && go build ./...`（确保 schema 加载不报错）
- [ ] **Step 5: Commit**
  `git commit -m "feat(m9): cost 模块补全 EVM 指标 + 储备 + 估算方法字段"`

---

### Task 2: scope 范围模块 WBS/RACI 字段补全（P0）

**Files:**
- Modify: `gva/server/plugin/pmocker_scope/pmocker/schema.yaml`
- Modify: `gva/server/plugin/pmocker_scope/pmocker/seed.yaml`
- Modify: `gva/server/plugin/pmocker_scope/pmocker/workflows/*.yaml`

**Interfaces:**
- Produces: scope_item 新增 12 字段（WBS 编码 + RACI + 基线 + 范围说明书要素），wbs_node 补血（name/description/weight/planned_start/planned_finish/cost/estimated_effort）

**scope_item 新增字段：**

| field_key | data_type | 用途 |
|------|------|------|
| wbs_code | string | 结构化 WBS 编码（如 1.2.3） |
| is_work_package | bool | 是否工作包（8/80 规则叶子节点） |
| raci_responsible | ref | RACI-R 执行人 → sys_users |
| raci_accountable | ref | RACI-A 负责人 |
| raci_consulted | json | RACI-C 咨询人列表 |
| raci_informed | json | RACI-I 知会人列表 |
| baseline_version | string | 范围基线版本 |
| baseline_date | date | 基线日期 |
| acceptance_status | enum | 范围确认状态：pending/accepted/rejected |
| assumptions | text | 假设条件 |
| constraints | text | 制约因素 |
| exclusions | text | 范围排除项（范围说明书必备） |

**wbs_node 补血字段：** name(string) / description(text) / weight(decimal) / planned_start(date) / planned_finish(date) / cost(decimal) / estimated_effort(decimal)

- [ ] **Step 1: scope_item 新增 12 字段 + wbs_node 补血 7 字段**
- [ ] **Step 2: seed.yaml 新增 `pm_acceptance_status` 字典**
- [ ] **Step 3: 重构 scope 工作流**（当前 3 节点，changed 状态无流转，补全 draft→baselined→changed 闭环）
- [ ] **Step 4: 编译 + Commit**
  `git commit -m "feat(m9): scope 模块补全 WBS 编码 + RACI + 范围说明书字段"`

---

### Task 3: requirement 需求模块字段补全（P1）

**Files:**
- Modify: `gva/server/plugin/pmocker_requirement/pmocker/schema.yaml`
- Modify: `gva/server/plugin/pmocker_requirement/pmocker/seed.yaml`
- Modify: `gva/server/plugin/pmocker_requirement/pmocker/workflows/*.yaml`

**新增字段清单：**

| field_key | data_type | 用途 |
|------|------|------|
| moscow_priority | enum | MoSCoW：must/should/could/wont |
| requirement_type | enum | business/user/system/feature/non_functional |
| verification_method | enum | inspection/demonstration/test/analysis |
| traceability_links | json | 追踪关系：关联设计/测试用例/交付物 |
| baseline_version | string | 需求基线版本 |
| baseline_date | date | 基线日期 |
| status | enum | 需求状态字段（补齐 states 对应） |
| requested_by | ref | 需求提出人 → sys_users |
| requested_date | date | 提出日期 |
| agile_level | enum | epic/story/task |
| story_points | decimal | 故事点 |
| stakeholder_id | ref | 干系人 → sys_users |

- [ ] **Step 1: 新增 12 字段**
- [ ] **Step 2: seed.yaml 新增 `pm_moscow_priority`、`pm_requirement_type`、`pm_verification_method`、`pm_agile_level` 字典**
- [ ] **Step 3: 重构 requirement 工作流**（当前仅覆盖 draft→approved/rejected，补全 reviewing/fulfilled 流转）
- [ ] **Step 4: 编译 + Commit**
  `git commit -m "feat(m9): requirement 模块补全 MoSCoW + 追踪 + 基线 + 敏捷字段"`

---

### Task 4: schedule 进度模块字段补全（P1）

**Files:**
- Modify: `gva/server/plugin/pmocker_schedule/pmocker/schema.yaml`
- Modify: `gva/server/plugin/pmocker_schedule/pmocker/seed.yaml`
- Modify: `gva/server/plugin/pmocker_schedule/pmocker/workflows/*.yaml`

**task 新增字段（依赖类型为致命缺失）：**

| field_key | data_type | 用途 |
|------|------|------|
| dependency_type | enum | FS/FF/SS/SF（最致命缺失） |
| lead_lag | int | 提前量/滞后量（天） |
| resource_assignments | json | 资源加载（资源+数量+工时） |
| is_critical_path | bool | 是否关键路径任务 |
| total_float | decimal | 总浮动时间 |
| free_float | decimal | 自由浮动时间 |
| baseline_start | date | 进度基线开始 |
| baseline_finish | date | 进度基线完成 |
| actual_start | date | 实际开始 |
| actual_finish | date | 实际完成 |
| task_type | enum | fixed_duration/fixed_work/fixed_units |

**milestone 补血字段：** description(text) / owner_id(ref) / baseline_date(date) / actual_date(date)

- [ ] **Step 1: task 新增 11 字段 + milestone 补血 4 字段**
- [ ] **Step 2: seed.yaml 新增 `pm_dependency_type`、`pm_task_type` 字典**
- [ ] **Step 3: 重构 schedule 工作流**（当前仅审批到 planned，补全 in_progress/completed/overdue 流转；新增 `auto` 节点自动算 CPM 关键路径 handler: `pmocker.schedule.cpm`）
- [ ] **Step 4: 编译 + Commit**
  `git commit -m "feat(m9): schedule 模块补全依赖类型 + 关键路径 + 浮动时间字段"`

---

### Task 5: eps 组织模块治理字段补全（P2）

**Files:**
- Modify: `gva/server/plugin/pmocker_eps/pmocker/schema.yaml`
- Modify: `gva/server/plugin/pmocker_eps/pmocker/seed.yaml`
- Modify: `gva/server/plugin/pmocker_eps/pmocker/workflows/*.yaml`

**eps_node 新增字段：**

| field_key | data_type | 用途 |
|------|------|------|
| governance_type | enum | portfolio/program/project |
| lifecycle_phase | enum | initiating/planning/executing/monitoring/closing |
| phase_gate | json | 阶段-门定义 |
| priority | enum | high/medium/low |
| start_date | date | 项目开始日期 |
| end_date | date | 项目结束日期 |
| budget | decimal | 项目预算 |
| health_status | enum | RAG：red/amber/green |
| methodology | enum | agile/waterfall/hybrid |
| sponsor_id | ref | 项目发起人 → sys_users |
| level | int | EPS 层级 |

**eps_member 新增：** allocation_percent(decimal) 投入度%

- [ ] **Step 1: eps_node 新增 11 字段 + eps_member 新增 1 字段**
- [ ] **Step 2: seed.yaml 新增 `pm_governance_type`、`pm_lifecycle_phase`、`pm_health_status`、`pm_methodology` 字典**
- [ ] **Step 3: 重构 eps 工作流**（inactive 状态无流转，补全 draft→active→inactive 闭环）
- [ ] **Step 4: 编译 + Commit**
  `git commit -m "feat(m9): eps 模块补全治理类型 + 阶段门 + RAG 健康度字段"`

---

### Task 6: risk 风险模块字段补全（P2）

**Files:**
- Modify: `gva/server/plugin/pmocker_risk/pmocker/schema.yaml`
- Modify: `gva/server/plugin/pmocker_risk/pmocker/seed.yaml`
- Modify: `gva/server/plugin/pmocker_risk/pmocker/workflows/*.yaml`

**新增字段清单：**

| field_key | data_type | 用途 |
|------|------|------|
| opportunity_strategy | enum | 正面风险策略：exploit/share/enhance |
| expected_monetary_value | decimal | EMV 预期货币值 |
| impact_amount | decimal | 风险影响金额 |
| contingency_reserve | decimal | 风险应急储备 |
| secondary_risk_id | ref | 次生风险关联 → risk |
| residual_risk | text | 残余风险描述 |
| identified_date | date | 风险识别日期 |
| reassessment_date | date | 再评估日期 |
| analysis_type | enum | qualitative/quantitative |
| response_cost | decimal | 应对成本 |

- [ ] **Step 1: 新增 10 字段**
- [ ] **Step 2: seed.yaml 新增 `pm_opportunity_strategy`、`pm_analysis_type` 字典 + `pm_risk_pi_matrix`（5×5 概率影响矩阵配置）**
- [ ] **Step 3: 重构 risk 工作流**（当前仅 identified→assessed，补全 responding/closed/realized 流转；新增 `auto` 节点自动算 risk_score=probability×impact handler: `pmocker.risk.score`）
- [ ] **Step 4: 编译 + Commit**
  `git commit -m "feat(m9): risk 模块补全正面风险策略 + EMV + 储备金额字段"`

---

### Task 7: issue 问题模块闭环字段补全（P3）

**Files:**
- Modify: `gva/server/plugin/pmocker_issue/pmocker/schema.yaml`
- Modify: `gva/server/plugin/pmocker_issue/pmocker/seed.yaml`
- Modify: `gva/server/plugin/pmocker_issue/pmocker/workflows/*.yaml`

**新增字段清单：**

| field_key | data_type | 用途 |
|------|------|------|
| closure_criteria | text | 关闭条件 |
| affected_scope | ref | 影响范围/交付物 → deliverable |
| resolution_type | enum | workaround/permanent_fix |
| related_risk_id | ref | 关联风险 → risk |
| resolved_date | date | 实际解决日期 |
| resolution_effort | decimal | 解决工时 |
| verified_by | ref | 验证人 → sys_users |
| verified_date | date | 验证日期 |
| reopen_count | int | 重开次数 |

- [ ] **Step 1: 新增 9 字段 + category 枚举扩展（增加 process/schedule/resource/communication）**
- [ ] **Step 2: seed.yaml 更新 `pm_issue_category` 字典 + 新增 `pm_resolution_type` 字典**
- [ ] **Step 3: 重构 issue 工作流**（当前较完整，仅补全 reopened 状态流转）
- [ ] **Step 4: 编译 + Commit**
  `git commit -m "feat(m9): issue 模块补全关闭条件 + 关联风险 + 验证人字段"`

---

### Task 8: deliverable 交付物模块配置管理字段补全（P3）

**Files:**
- Modify: `gva/server/plugin/pmocker_deliverable/pmocker/schema.yaml`
- Modify: `gva/server/plugin/pmocker_deliverable/pmocker/seed.yaml`
- Modify: `gva/server/plugin/pmocker_deliverable/pmocker/workflows/*.yaml`

**新增字段清单：**

| field_key | data_type | 用途 |
|------|------|------|
| config_item_id | string | 配置管理项 ID（CMDB 关联） |
| review_status | enum | 评审状态（独立于验收状态） |
| wbs_node_id | ref | 关联 WBS 节点 → scope_item |
| defect_count | int | 质量缺陷数 |
| security_classification | enum | 保密级别：public/internal/confidential/secret |

- [ ] **Step 1: 新增 5 字段 + 将 related_requirements 从 string 改为 json 多对多**
- [ ] **Step 2: seed.yaml 新增 `pm_review_status`、`pm_security_classification` 字典**
- [ ] **Step 3: 重构 deliverable 工作流**（补全 baselined/obsolete 状态流转）
- [ ] **Step 4: 编译 + Commit**
  `git commit -m "feat(m9): deliverable 模块补全配置管理 + 评审状态 + WBS 关联字段"`

---

### Task 9: change 变更模块 PMBOK 四分类补全（P4）

**Files:**
- Modify: `gva/server/plugin/pmocker_change/pmocker/schema.yaml`
- Modify: `gva/server/plugin/pmocker_change/pmocker/seed.yaml`
- Modify: `gva/server/plugin/pmocker_change/pmocker/workflows/*.yaml`

**新增字段清单：**

| field_key | data_type | 用途 |
|------|------|------|
| change_nature | enum | PMBOK 四分类：corrective/preventive/defect_repair/enhancement |
| ccb_decision_date | date | CCB 决议日期 |
| ccb_decision_maker | ref | CCB 决议人 → sys_users |
| impact_analyzed_by | ref | 影响分析责任人 → sys_users |
| validated_by | ref | 验证人 → sys_users |
| is_emergency | bool | 紧急变更标识 |
| affected_baselines | json | 受影响基线：scope/schedule/cost |

- [ ] **Step 1: 新增 7 字段**
- [ ] **Step 2: seed.yaml 新增 `pm_change_nature` 字典**
- [ ] **Step 3: 修复 change 工作流自环**（删除 `end→end on close` 异常转换）
- [ ] **Step 4: 编译 + Commit**
  `git commit -m "feat(m9): change 模块补全 PMBOK 四分类 + CCB 评审记录字段"`

---

### Task 10: 工作流横切重构（auto 节点 + 状态机对齐）

**Files:**
- Modify: 9 个模块的 `workflows/*.yaml`
- Reference: `pkg/pmocker/workflow/engine.go`（确认 auto 节点 handler 调度机制）

**Interfaces:**
- Produces: 9 模块工作流全部含完整状态机流转，auto 节点引入用于自动计算

**问题清单：**
1. `auto` 自动节点 0/9 全部缺失 → 新增用于：风险分值自动算、EVM 派生指标自动算、状态自动跃迁（任务 100% 自动 completed）
2. 6/9 工作流是「start→单审批→end」三节点极简流，schema states 多于工作流流转（死状态）
3. change 工作流存在 `end→end on close` 异常自环

- [ ] **Step 1: 审阅 `pkg/pmocker/workflow/engine.go`** 确认 `auto` 节点 `handler` 调度机制是否已实现；若未实现，先补 engine 对 auto 节点的支持
- [ ] **Step 2: 逐模块对齐 states 与工作流流转**
  - requirement：补 reviewing/fulfilled
  - scope：补 changed
  - schedule：补 in_progress/completed/overdue
  - cost：补 in_progress/closed/over_budget
  - risk：补 responding/closed/realized
  - eps：补 inactive
  - deliverable：补 baselined/obsolete
  - issue：补 reopened
  - change：删除 end 自环
- [ ] **Step 3: 新增 auto 节点**
  - cost 工作流：`auto` handler `pmocker.cost.evm_calc` 自动算 CV/SV/CPI/SPI
  - risk 工作流：`auto` handler `pmocker.risk.score` 自动算 risk_score
  - schedule 工作流：`auto` handler `pmocker.schedule.cpm` 自动标关键路径
- [ ] **Step 4: 编译 + Commit**
  `git commit -m "feat(m9): 工作流重构——auto 节点 + 状态机对齐 + 修复 change 自环"`

---

### Task 11: 重建默认镜像 + 端到端验证

**Files:**
- Run: `images/pmbok6-hybrid/build.go`

**Interfaces:**
- Produces: 新版 `pmbok6-hybrid.pmi` 镜像含全部新字段，`pmocker run` 启动后字段在 EAV 元表生效

- [ ] **Step 1: 重建镜像**
  Run: `cd images/pmbok6-hybrid && go run build.go`
- [ ] **Step 2: 停旧实例 + 启动新实例**
  Run: `pmocker run -i images/pmbok6-hybrid/pmbok6-hybrid.pmi -p 8080`
- [ ] **Step 3: 验证字段灌入**
  查询 `pm_field_defs` 表确认 cost 模块字段数 ≥ 25（9+16），scope ≥ 21（9+12），requirement ≥ 21（9+12），总计 ≥ 210
- [ ] **Step 4: 验证 EVM 闭环**
  创建 cost_item，填 PV/EV/AC，确认 auto 节点算出 CV/SV/CPI/SPI
- [ ] **Step 5: 验证工作流状态机**
  对 schedule/cost/risk 模块触发状态流转，确认无死状态、auto 节点执行
- [ ] **Step 6: 验证 MCP 工具仍可用**
  调用 `pmocker_create_requirement` 确认新字段（moscow_priority 等）可传入
- [ ] **Step 7: Commit + Push**
  `git commit -m "chore(m9): 重建默认镜像含全量字段" && git push`

---

## 验收标准

1. **字段总数**：`pm_field_defs` 表记录数 ≥ 210（当前 123）
2. **EVM 闭环**：cost_item 填 PV=1000/EV=800/AC=900，auto 节点算出 CV=-100/SV=-200/CPI=0.89/SPI=0.8
3. **WBS 结构化**：scope_item 有 wbs_code（如 1.2.3）+ RACI 四角色
4. **依赖类型**：schedule task 有 dependency_type=FS/FF/SS/SF
5. **工作流**：9 模块无死状态，auto 节点 ≥ 3 个（cost/risk/schedule），change 无 end 自环
6. **幂等**：重复 `pmocker run` 初始化不产生重复字段记录
7. **MCP 兼容**：78 动态工具仍可调用，新字段可作参数传入
8. **回归**：M1-M8 既有功能不受影响（菜单权限、自动初始化、CLI 命令）

## 执行建议

- **批次推进**：按 P0→P1→P2→P3→P4 顺序，每批完成后重建镜像验证，避免一次性改动过大
- **P0（T1+T2）优先**：cost 的 EVM 与 scope 的 WBS 是项目基线之基，解决后实用性提升最明显
- **T10 工作流重构可与 T1-T9 并行**：每个模块改 schema 时同步改 workflow，最后 T10 统一审阅 auto 节点
- **前端表单适配留待后续**：M9 聚焦元数据层；前端表单是否需同步加字段输入框，待验证后按需开 M10
